# plans/system-review/smoke 全文深读记录（核证日期 2026-08-13）

> 对象：`docs/superpowers/plans/` 全部 149 篇实施计划全文、`docs/system-review/` 全部 26 个文件全文、`docs/smoke/` 全部 5 个文件全文。本记录为"100% 理解工程"第二轮补盲任务 20 号，逐篇全文读取（无跳读），所有断言标注出处（文件名+段落/小节）；无法对码之处标注"文档声称"。
>
> 阅读顺序：plans 按文件名日期序分批全读 → system-review 26 文件（含 4 个超大台账分块全读）→ smoke 5 文件。边读边落盘，分节追加。

---

## 1. plans 编目与"计划≠规格"偏差清单（哪些计划偏离/放弃/超出了 spec）

> 逐篇编目。每篇给出：日期-主题｜性质（对应 spec 或独立）｜与 spec 的偏差/放弃/变更条目（若有）。仅罗列计划文本自身声称的内容；"计划声称"字样表示未对码。

### 批次 1（2026-06-02 ～ 06-04）

1. **06-02 knowledge-closed-loop-trajectory**｜测试计划（知识闭环轨迹确定性测试，5 断言门 + Q2 泛化门抽纯函数）｜**范围窄于 spec**：Self-Review 明言"门 2/门 3（真 LLM 辅助门）本计划不含"，与 spec 第 5 节"本轮做门 1 + Q2 抽取；门 2/3 视 CI 预算"一致——门 2/3 留作后续计划（未见后续同名计划，疑似未落地，待对码）。计划自认风险：`KnowledgeVerifyRequest` 字段与 verify 返回体 schema 未逐字核到，靠执行时 grep 兜底。
2. **06-04 frontend-ui-refactor**｜前端架构+视觉重构计划（13165 行 App.tsx + 7488 行 styles.css → features 切片 + components/ui 原子库 + CSS Modules + 4 Zustand store + channels 注册表）｜Self-Review 声称对 spec §2.1-§7 无遗漏。计划内决策：迁移顺序二选一（先搬 feature 再切入口 vs 先切入口）计划自行选定前者；group-ops/moment-ops 明确为"占位页，Phase 1 不实现"；overview 的 pendingTasks/latestEvent 先占位后回填（跨 feature 派生）。

### 批次 2（2026-06-05）

3. **06-05 principal-decision-channel**（130KB，25 Task/9 Phase，全 plans 最大篇）｜对应 spec `docs/superpowers/specs/2026-06-05-principal-decision-channel-design.md`｜**这是"计划改设计"最重的一篇**，plan 内记载的对 spec 的偏差/修正：
   - **统一占位模型（2026-06-06 用户拍板，推翻原 Held 挂起设计）**：触发请示的 run 始终 `Approved`，decision Agent 输出"安全占位 reply"走正常 outbox 发客户；请示（推卡+落台账）只是 approved 路径末尾（当时 gateway L1534）的副作用。**run 不进 Held 分支、不设 hold category、完全不碰 review.rs**——原 Task 18 对 review.rs 的 hold 分支改动被删除，"冲突面归零"。
   - **两处 spec 锚点漂移修正**（plan 核验后声明写码以 plan 为准）：① `mcp.rs` 无 `message_send_text` 函数——那是 MCP 工具名字符串，调用走 `mcp::logged_call_for_account`；② 无 `guards/` 子目录（是单文件 guards.rs），spec 说的"五闸门"已删除，当时实况是 knowledge_grounding / hallucination / run_budget 三闸门 + review 评分体系。
   - **授权过期窗从"硬编码 24h"改为"领导说了算"**：LLM 解读领导明确说的时限填 `authorization_window_hours`；领导没提 → None = 不设过期窗长期有效（Self-Review 第 5 节明言"已删 24h 硬编码"）。
   - **跨 workspace 串扰修正**：`lookup_principal_config` 必须用入站消息自身 workspace_id 约束查询（原计划全局查，会把 A 工作区领导误路由进 B 的请示流）。
   - **spec §9.1 接触级归属被 MVP 显式缩窄**：spec 说真人决策"立即写该客户 memory/state（接触级）"；plan 拍板 MVP **不写 memory**（转述回复天然落聊天历史 + 台账是授权过期判定权威源），并注明"跨多轮聊天历史滑窗后授权上下文可能丢失——有意接受的 MVP 边界，不是 bug，执行时别顺手加 memory 写入"。
   - **spec §9.4 领导无未决时的主动指令**：spec 要落"待 admin 确认的真人主动指令"；plan MVP 缩窄为"只记日志，不自动生效"。
   - **Task 22 卡死信号注入接线声明为可放弃项**："若现成信号不足，本 Task 仅落地纯函数 + 阈值常量，注入接线记为后续增强（不阻塞 MVP）"——即 stuck_or_undelivered 的 prompt 注入可能未实施（待对码）。
   - plan 补的 spec 没有的关键机制：relay 哨兵 `__PRINCIPAL_RELAY__` + decision prompt "转述模式"输入契约成对出现（Self-Review 称"闭环命门，没这对契约闭环跑不通"）。
   - 6 个用户拍板业务决策（06-05）：high_risk_escalation_mode 可配 all/decision_only（默认 decision_only 保守）；超时无限等待永不自动代决；泛化沉淀 agent 自判；真人回复不带短码单条直配/多条反问；卡死=N 轮（默认 3）未推进∧负面反应；等待期非越权部分照常答。

### 批次 3（2026-06-06 ～ 06-07）

4. **06-06 knowledge-grounding-and-verify-gate-fixes**｜双链修复计划（A=发送侧 grounding 漏判兜底、B=写入侧 create/PUT 绕过 D2 verify 闸收口）｜对应 spec 同名 design。Self-Review 声称 spec 全覆盖、"未新增闭集枚举、未动 apply_chunk_integrity 本体"。承诺词硬闸有意收窄：仅 5 个效果/数据词（成功率/见效/回款/百分之/百分百）触发 block，3 个语气词（保证/一定能/绝对）仅观测防误杀情感承诺。
5. **06-07 knowledge-routes-split**｜纯机械重构计划（9378 行 216 函数的 routes/knowledge.rs → 9 子文件 + facade mod.rs），承诺"行为零改动、函数体一字不改、对外路径不变"｜计划内记录了执行中发现的三条可见性铁律（pub(super) 深一级窄化、E0364/E0365 受限 glob 无法 re-export pub 项、E0616 父模块读子模块字段需 pub(super)）——这些是执行反馈回写进 plan 的。当时 lib 基线实测 **899 passed**（通用步骤第 6 步硬性要求恒等）。测试分发自检：迁出 37 + 留 mod.rs 共享层 17 = 54。

### 批次 4（2026-06-07 ～ 06-15）

6. **06-07 knowledge-trust-cockpit-frontend**｜前端知识治理驾驶舱计划（cockpit/review 双栏/autoVerify 三屏）｜对应 spec 同名 design + brainstorm mockup。**计划的地基阶段本身是"契约漂移修复"证据**：前端旧 `CompletenessView` 只认 `perWikiType/overall`（后端真实响应是 answeringMode+5 维 coverage，"全错"）；`IntegrityReportView` 认 `contested/sourceOrphan` 两个后端不存在的字段——前端曾长期拿旧 key 读新响应静默吃数据。在独立 worktree `E:/yw/wa-kb-cockpit` 执行。
7. **06-08 escalation-split**｜纯结构重构（escalation.rs 1274 行 → escalation/{mod,logic,ledger}）；声明"原子重构，不追求中间步可编译"，38 个测试作回归网，明言"不是 TDD"。**考古价值**：其行号映射表显示 06-05 计划落地后 3 天内 escalation.rs 已长出 06-05 plan 未规划的函数——`escalate_held_decision`（408-503）、`should_escalate_held`、`is_principal_relay_trigger`、`relay_output_leaks_internal_payload`、`build_decision_signals_text`、`consecutive_unprogressed_turns`——即"统一占位模型"（run 不进 Held）落地后又补了 **Held 决策升级请示**路径（escalate_held_decision + ESCALATION_CATEGORY_HIGH_RISK_GATED），两模型并存。
8. **06-08 knowledge-frontend-split**｜纯结构重构（knowledge/index.tsx 6582 行 46 组件 → 主壳+shared+today/explore/steward/atlas）｜关键架构事实：组件间靠 `window` CustomEvent 通信（`wikiFocusChunk`/`wikiOpenCockpit` 两个事件桥，"零 prop 耦合"，重构唯一隐性风险点要求人工点验）；死代码 `tryLlmError`（`void tryLlmError;` 自我消费）确认删除；唯一对外契约=默认导出 KnowledgeFeature。
9. **06-15 h17-memory-dimensions-universal**｜通用化改造计划（memoryCard 记忆维度从销售写死改为 DomainProfile.memory_dimensions 可配），格式为简洁版（无 checkbox）。核心护栏"DEFAULT 逐字复刻当前销售槽位、prompt 字节等价快照锁死"。**范围决策**：typed 三数组 coreFacts/recentFacts/deprecatedFacts 固定 cap **6/10/20** 不纳入维度化；倾向不做迁移（serde default 回落，"DomainProfile 不像 OperatingMemory 有海量历史文档"）；不动 intent_trajectory.objection_type（"另一条轴"）。当时 lib 基线实测 1071。

### 批次 5（2026-06-19 ～ 06-20）

10. **06-19 eval-overhaul-phase1-judge-context**｜测试域计划（LLM 裁判底料注入：JudgeContext{transcript/knowledge/memory/commitments/profile_brief}，根治 J1 判编造看不到知识库/J2 判一致性看不到记忆/J5 情绪单句判）｜Global Constraints 明言"只改 tests/ 绝不碰 src/，违反即范式作废"。**阶段划分线索**：收尾 commit 写"待阶段2红线/阶段3对话级"——plans 目录中无 eval-overhaul-phase2/phase3 文件，阶段 2/3 疑未以独立 plan 落地（或并入他篇，待对码）。引用 findings 文档 `.kiro/specs/universal-test-coverage/real-llm-findings-2026-06-18.md`。
11. **06-19 universal-residuals-completion**｜通用化三残留收口（H18 debounce 随 profile / H17 轨迹维度容器化 / H13 状态机本体随 profile）｜**含"终审追加"节（06-20 whole-branch review 后用户拍板"彻底做"补 Task 8/9）**：Task 8 修 forbidsProactive 静默失效（publish 状态机时联动重派生 operation_state_policies；根因=该表仅 m013 seed，"生成 prompt 却邀运营声明此旗标=承诺了静默忽略的能力"）；Task 9 修 reaction prompt 不产非销售维度 key（"spec H17.4 列了，本批漏做"——plan 自认漏项后补）。H13 核心设计：`DomainProfile.generated_state_machine` 仅是 draft 暂存料，activate 时 publish 到 operation_domain_configs，**发布后运行时不读 profile 字段（不造双真相源）**。
12. **06-20 universal-audit-remediation**｜通用化审查修复批（4 必修 Medium：G21 多租户首屏画像取错 workspace / G13 五闸阈值无 clamp / G01 review_passed 漏 grounding bypass / G07 负反应率未 profile 化+假声明注释；另 G06/G11/G12/G31/G03/G04/G08/G32/G16/G24/G09/G10/G18/G05 等 Low）｜**明确未实施条目**：收尾声明"**不含 G02**——终审列必修但本批用户范围是 4 必修，G02 归可缓但 spec 未展开为独立 task；若执行时有余力可补，否则记路线图"。**结果开放的任务**：G16 taxonomy 候选双 upsert "必须先核实再决定改不改，证伪则记报告不强改"（decision pre-review confidence=0 vs gateway post-review confidence=50 可能非冗余）；G09 若断言全 DB 依赖则不强拆。**痕迹**：提交边界排除清单里出现 `docs/superpowers/plans/2026-06-18-*`——曾有 06-18 计划文件（并行会话产物），现 plans 目录无任何 2026-06-18 文件（未收录/未提交）。G05 订正确认"数字分身口吻分化（per_relationship 各异 soul/tone）当前未实现，OperationMode 无 voice/tone/soul 字段"=另一明确未实现能力。

### 批次 6（2026-06-21）

13. **06-21 agent-send-ledger**｜发送事实表（素材+名片共用 agent_send_ledger 集合：写入+回扫+3 只读 API+前端 sendAnalytics 频道）｜对应 spec 同名 design。**两处计划改 spec 落点**：① 写入点从 spec 的"发送函数内"改为 **dispatcher 成功分支**（send_outbound_media/namecard 签名拿不到 run_id，dispatcher 持有 entry+contact，一处写入对称覆盖两功能）；② spec §6.3"统一从 ledger 取已发历史单一事实源"被缩窄为**只做素材侧新增**，名片侧保留旧 AlreadyReferred 数据源（contact.domain_attributes[REFERRED_CARD_ID_ATTR]），"统一收敛"标记为未来项 YAGNI 不强做——即**已发历史存在双数据源并存的既知状态**。

14. **06-21 referral-card-push**｜专属顾问名片引荐（辅助模式受控例外）｜对应 spec 同名 design。**含"实现期对齐"节（明文取代设计文档假设）**：① 架构决策——OutboxEntry **并列加 referral_card_id 而非复用 media_asset_id、不抽 entry_kind**（素材内核零改动零回归，代价是 dispatcher/outbox 轻度分支重复；"将来收敛抽 entry_kind 是独立重构专题不在本期"=已知重构债）；② ConversationMessage 的 msg_type/media_ref 字段素材侧已加，本计划直接复用不再加；③ 名片**不套用** requires_principal_approval→escalation 分支（spec D9：素材请领导核准 vs 名片主动引荐语义不同）；④ 名片条目崩溃恢复**跳过** post-hoc 核对（text 形态核对不适用，"重复推名片危害小"）；⑤ 计划自认修正一处漏判：media_eligible 门原只写 approved 漏了 revision_applied_approved。**已知未决**：MCP `message_send_namecard` "仓内零书面依据，仅用户口头确认"，入参字段占位待 server tools/list 对齐。Task 14 落地 CLAUDE.md"辅助模式受控例外"段（与现行 CLAUDE.md 一致，证实已实施）。

15. **06-21 sales-media-asset-send**｜销售素材文件发送内核（ContentAsset 扩展 13 字段 + media_storage 模块 + 上传/审核 API + AgentDecision.assetsToSend + dispatcher 分流）｜对应 spec 同名 design。关键计划级决策：grounding 边界"素材文件内容免 AI 核验（人类已把关），伴随 reply_text 照常五闸门"；`requires_principal_approval=true` 的素材不直接发、走 escalation 请示通道；expression_pref 两种偏好（file_primary/file_support）当前定序都是"先文字后文件"（TextThenMedia，抽函数留扩展点）；media_id 缓存 TTL 24h 过期重传（"不依赖 media_id 永久有效假设"）。**已知未决**：MCP media_upload_base64/发送工具入参字段名占位待 tools/list。**环境细节**：MEDIA_STORAGE_DIR 默认 ./media，注释写明"生产 117 为 /opt/wechatagent/media"（生产服务器代号 117）。

16. **06-21 annotation-quality-gate**｜标注质量三缺口收口（target_stages 归一 / 审核审计 / 客户级辅助模式 override 端点）｜**修复的缺陷本质**：运营手填 stage alias 不归一 → 与 contact canonical customer_stage `s == cs` 永不相等 → 素材/名片**静默不发**；`ASSIST_MODE_OVERRIDE_ATTR` 此前"全仓零写入路径"（判定函数支持但无人能写）。字典整个未配置时 fail-soft 放行原值（KindUnconfigured→Accept，"新部署照常能上传"）。前端坑记录：override 回显不能直接从 selected.domainAttributes 派生（refreshContacts 只重拉列表不刷 selected，会回显旧值），须走 hydrateSelected 受控链路。集成测试含"实现者决策点"：归一断言若种字典成本高，退而测"越界 400 + 未配置放行"两条。

17. **06-21 ask-human-unified-channel-phase1**｜请示通道后端地基（AskHumanPolicy 配置化 + admin 三 REST 端点 + 8 源只读聚合器 + 超时转备选扫描 + m025 回填迁移）｜**计划内声明的功能性欠账**：Task 9 接线处 `push_allowed` 的 `last_push_ms` 恒传 **None**——"dedupe_window 的精确 last-push 查询留待 Phase 优化，None 时 dedupe 分支不触发"，即 **dedupe_window_hours 配置项 Phase 1 配了也不生效**（骚扰去重实际只靠 pending 去重 + daily_cap）。Phase 1 完成定义明确"**不含任何前端**（P2/P3 另起）"。admin 裁决复用 relay 下游跳过 LLM interpret；resolved_via 二值审计（admin/wechat）；resolve 端点对非本 workspace/已 resolved 返"幂等成功"避免泄漏存在性；deferred 不起 relay。harness 纪律：seed operation_domain_configs 必须 replace_one(upsert)（ensure_prompt_pack_v2 已 seed 过）。

18. **06-21 ask-human-config-page（Phase 3）**｜请示通道前端配置页（askHumanConfig 频道：决策人链编辑器+4 escalate 开关+超时+频控折叠区）+ 后端补 operation_domain_json 序列化 askHumanPolicy 回显｜结尾声明"ask-human 三子项目（P1 后端→P2 收件箱→P3 配置页）全部交付"。**plans 覆盖缺口**：P2（askHuman 收件箱前端频道）在 plans 目录无独立计划文件（Task 6 提及"P2 已加过 askHuman"频道，证明 P2 已实施但未留 plan）。实证基线记录：OperationDomainConfig 无 rename_all（落库 snake_case）但 operation_domain_json 手写 json! 输出 camelCase——两套命名并存。

### 批次 7（2026-06-22）

19. **06-22 business-audit-fix-wave**｜业务审查 11 缺陷修复波（A 请示闭环②③⑩/B 知识时效①⑨/C 决策写入⑧⑥/D 跟进节奏⑦/E 账号健康⑪④/F 多模态地基⑤）｜对应 spec 同名 design。**明确的打桩/未实施**：F 组媒体下载 `fetch_inbound_media` 打桩返回 None（"MCP server 无下载入站媒体 tool，仓内零调用，实现前必打 tools/list"）；**语音 ASR 零能力待独立立项**；Minor 缺陷（已拒绝客户不唤醒等）"未单列任务，留待执行中按需附加"。哲学声明：护栏只兜客观边界（状态机字典/数字白名单/知识时效/账号在线），凡语义判断（"这句话是否做了承诺"）不加确定性硬拦——⑥承诺兑现降级为 prompt 强化+观测事件。执行分支为共享分支 feat/ask-human-phase1（多会话交错提交，"每次提交前 git rev-parse 核对真实 parent"）。

20. **06-22 digital-twin-relationship-closure**｜数字分身关系类型闭环（relationship_type customer/peer/friend 三子项目）｜关键决策：**口吻分化不建结构化字段**（per_relationship soul/voice 不做），复用 customAgentInstructions 最高优先级指令通道，仅改前端引导文案（"审查确证它已是口吻分化现成载体，唯一缺口是运营者不知道能这么用"）——与 06-20 G05 订正呼应；ChannelDef.visibleWhen 频道门控**只建机制零消费者**（"本期无频道使用，留作扩展点"）；example_sales_with_relationships_profile 三套范式（customer 漏斗开+日历/peer 关漏斗/friend 关漏斗+关承诺）seed 为 **draft/inactive** 运营手动激活；护栏 H8：DEFAULT profile 的 per_relationship_operation_mode 永远 None。

21. **06-22 media-asset-crud-completion（簇C）**｜素材库 CRUD 补全（edit 元数据/换文件/toggle/delete 四端点+前端）｜核心不变量：**换文件必清 media_id=None**（否则 ensure_media_uploaded 在 TTL 内复用旧 media_id，AI 发旧文件）+ **强制退 draft 重审**（发送物变了必须人类重核验）；改元数据不退审（描述词变不影响已核验文件）；物理删文件前查同 file_path 引用计数（upload 不去重、多记录共享物理文件；查询失败视为有引用保守不删）；物理删 fail-soft。**测试盲区自认**：tests crate 无法构造 Multipart，换文件端到端不可测，"由代码审查保证，在报告说明取舍"。
22. **06-22 structured-organization（簇D）**｜软增强注入（知识 render_chunk 注入 productTags/businessTopics；素材 tags 半死字段激活——upload 硬编码空/render 不渲染/list 不能筛；名片新增 tags）｜红线：**tags 不作发送硬门**（不进 filter_*_candidates，AI 综合 trigger_hint/stage/tags 自主判断）；空标签不渲染防 prompt 噪声；提示 chunk_type_routing PBT 若锁逐字全文需同步更新期望（"属本 task 合理改动"）。

23. **06-22 taxonomy-admin-crud**｜纯前端字典 CRUD（TaxonomiesAdmin 面板新增/编辑/废弃恢复）｜决策：`api.patch` 此前不存在需补（后端编辑走 PATCH）；D3 废弃语义=DELETE 软删（status→deprecated）+PATCH{deprecated:false} 恢复；D5 前端校验不超后端（不暴露 priority_weight/is_terminal）；D2 新增 customer_stage 时软提示指向状态机面板**不阻断**；409 重复条目当 info 不当 error。
### 批次 8（2026-06-23）

24. **06-23 progressive-prompt-three-tier**｜三档渐进提示词（Lean/Relational/Full）+ 充分性自评 + 两程循环 + reviewer 隐私维度｜核心机制：AgentDecision 加 sufficiency/missing_tier/clarification_intent；第一程 Lean → decide_tier_escalation → Enough 直接进闸/Escalate 升档二程/Clarify 走 ai_waiting_for_more_context；未知/空 sufficiency 保守判 Enough。ReviewScores 加 boundary_privacy_safety（0-10，≤3 触发改写；判泄露内部画像/AI 身份/幕后领导）。**部署依赖代码外手动操作**：Task 5 prompt 文本更新在 DB 模板（admin UI 手动改 user.reply.task/policy/reviewer 三模板），代码只 bump PROMPT_PACK_VERSION=3——"实际措辞需运营/产品审阅"，若手动步骤未做则自评契约不生效。Task 2 槽位搬运函数留 todo!() 骨架（"刻意，480 行逻辑分段搬运"）。

25. **06-23 reactivation-stage-universalization**｜再激活目标 stage 通用化（修审查#3：非销售域 reactivation 扫描器 DB 预筛恒空静默失效）｜与 is_terminal 逐处对称的 `is_reactivation_target` 字典标记 → PlannerStageConfig.reactivation_stages → filter 用 $in；DEFAULT 字节等价（单元素 $in ≡ ==，空字典回落 ["dormant_reactivation"]）；m006 seed 仅 dormant_reactivation 标 true + H6 对称护栏断言。方法论：字段是"编译期强约束源头"——Task 1/2 单独编译不全绿是预期（missing-field 错误强制下游补齐）。
26. **06-23 tag-trust-1-data-model**（标签可信度五部曲之 1）｜四层标签模型落地：manual_tags（人工权威层，AI 写路径代码层面不引用）/confirmed_tags（AI 确信层，压缩重判 replace 写回，每条带 Evidence{turn,msg_id} 引用不拷原文）/bayesian_signals（贝叶斯旁路 ≤6 槽，locked 才画走势，**永不驱动行为**）/personality_profile（大五 OCEAN 慢变量，不驱动行为）｜**裸 `Contact.tags` 字段直接删除**（设计称无存量）+ gateway 逐轮 tags 写入链拆除；update_profile_note 顺手修——AI 重生成标签**删写入不替换**（"confirmed 是压缩重判产物，不该被 note 旁路直接灌入"）；management.rs MCP 写 stage/intent 补维度校验（倾向 MachineWrite）。命名纪律：manual_tags 因 "manual" 不在禁词表、注释写"运营录入"避开"人工标签"——lint 逼出的命名规避。

27. **06-23 tag-trust-2-evidence**（五部曲之 2）｜证据绑定+强弱证据+stage 快通道｜关键机制：LLM 输出**窗口序位**（tagEvidenceTurns/stageEvidenceTurns/stageExplicitIntent），代码映射回消息 ObjectId hex（越界丢弃 fail-closed，"锚不上不放水"）；**强弱证据由代码客观判**——任一证据锚定 Inbound 消息 ∧ explicit_intent=true → Strong，"不靠 LLM 自称置信"；逐轮标签只写 memory_candidates（source="tag_observation"）暂定层；customer_stage 唯一例外：强证据实时写入（照常过状态机），弱证据剔除不写、只沉淀暂定层。设计取舍：整批标签共享一个证据序位集合（逐标签配对让 LLM 输出复杂度暴增、收益低）。
28. **06-23 tag-trust-3-consolidation**（五部曲之 3）｜压缩重判引擎（consolidate_contact_memory 同趟整体重判标签，replace 语义"纠错主力"）｜喂**原始宽窗口对话**（此前归并 prompt 不含原始对话）；窗口=字符预算（默认 6000，clamp[1000,16000]）+条数（默认 60，clamp[10,200]）双上限（"微信碎消息适配：字符预算保信息量下限，条数防垃圾号空耗"）；parse_reconfirmed_tags fail-closed（无证据标签丢弃，"宁可少，不要脑补"）；confirmed_tags 与 memory_card 同一次 OCC 写入。**跨子计划一致性要点**：逐轮窗口序位与压缩宽窗口序位是两个不同窗口，turn 只在各自窗口内有效，msg_id 才是跨窗口稳定锚。隔离铁律：只动 confirmed_tags，不碰 manual_tags/bayesian_signals。

29. **06-23 tag-trust-4-bayesian-personality**（五部曲之 4）｜贝叶斯旁路+大五人格｜占槽门默认 min_hits=3 ∧ min_strong_evidence=2（用户红线"不能因为一两句话就占槽"）；MAX_BAYESIAN_SLOTS=6、HISTORY_CAP=100、人格 snapshots 封顶 50；人格"诚实置信"——无有效证据维度 confidence 强制归 0；人格分析**搭车**归并同一次 LLM 调用（备选拆独立调用，"先试合并失败则拆"）。**明确未实现项**：`should_evict` 低置信淘汰"按 Option B / YAGNI 决策删除（未实现）——永不驱动旁路，槽僵化零业务影响，详见交叉验证 D5-F2"（函数体保留备查但不接线）。永不驱动铁律用契约测试守护（带/不带 bayesian_signals 的 contact 断言 planner filter 输出一致）。
30. **06-23 tag-trust-5-frontend**（五部曲之 5）｜前端三层标签面板+贝叶斯 SVG 走势图+OCEAN 人格画像｜决策：**手写 SVG 不引图表库**（package.json 仅 react/zustand/lucide）；AI 确信层/贝叶斯/人格用紫色系（AI 身份色纪律），人工层中性色；每组件独立 module.css 避 legacy.tsx tree-shake 坑；未占槽（locked=false）维度不画线。验证边界自认："若后端未部署用 mock 注入验渲染，明确报告业务数据流需后端联调"。

31. **06-23 tool-loop-dead-code-sunset**｜纯减法（删 W3-W5 灰度期 user-ops 多轮工具循环：tool_loop.rs 802 行整文件+dispatch_tool_call 半边+knowledgeRoutingMode/knowledgeMaxToolLoops 两 runtime 参数），兑现 sunset-plan D+21｜确认"生产链路统一 single-pass route_operation_knowledge + knowledge_agent，多轮循环从未接生产"。防误删护栏三条：`knowledge_max_tool_loops`（删）≠ `knowledge_max_tool_calls`（活）；`dispatch_tool_call`（删）≠ `dispatch_chat_tool_call`（chat 活路径）；exec_*/TOOL_* 常量 chat 共用全留。**明确顺延项**：`autonomyProtocolEnabled` 同为 D+21 删除项但"不在本次范围，需单独核实读点后另行下线"（后由 07-02 计划处理）。当时 lib 基线实测 1500。RuntimeParametersTyped 无 deny_unknown_fields → 删字段后旧文档残留静默忽略。

### 批次 9（2026-06-24 ～ 06-25）

32. **06-24 account-send-pacing-guard**｜账号级最小发送间隔闸（1-4 秒随机，防"连珠炮"机器化风控特征）｜pacing 纯函数随机由调用点注入（对称 backoff_with_jitter_seeded 模式）；命中 reschedule 不耗 attempt（仿 defer_account_offline）；dispatcher poll 5s 把实际间隔量化到 ≥ 配置值（"方向安全"）；查询失败 fail-soft 放行（"宁可漏限一次也不丢消息"）；新增 (account_id,status,sent_at:-1) 索引防内存 SORT。配置默认 1000/4000ms；**AppConfig 无 Default，加配置须同步 6 个字面量落点**（漏一处编译失败）。
33. **06-24 progressive-tier-hardening**｜三档机制加固（7-agent 交叉审查收口 5 敞口）｜**安全语义要点**：`used_knowledge_ids` 口径修正——纯 Lean-Enough 决策（未注入知识）不得记路由命中 id，"否则架空 grounding 硬闸（取 used∩verified 非空即放行）"；强升闸（enough ∧ coverage=missing ∧ 需知识 → 当场升 Full，最多一次）与 weak 观测正交；判据红线"全部用 == 正向匹配绝不用 != 否定"（防 not_required 寒暄轮兜底误命中）；missing_tier 非法值兜底从 Relational 改 **Full**（更保守）。`PROGRESSIVE_TIER_ENABLED` kill switch **默认 true**（与多数 *_ENABLED 相反），关时第一程直接 Full 并跳过强升。run tier 元信息走 `ptier_run_tier` 事件而非 run log 字段（"零签名改动、零跨函数变量穿透"）。**版本考古**：PROMPT_PACK_VERSION v10_2026_06_24_bayesian_obs → v11_2026_06_24_clarify_tighten。**并行会话教训**："上次被并行会话 git stash 卷走"→每块编译后立即 commit。

34. **06-25 taxonomy-label-wiring**｜取值字典三流接线（A prompt 取值指引/B 前端 labelFor 翻译/C AI 生成取值+override）｜**罕见结构：含"⚠️ 可行性审查必修修正"节**（4-agent 审查全判 FEASIBLE_WITH_FIXES，5 项必修 M1-M5 "以本节为准覆盖下方 Task 原文"）：M1[blocker] label 通路不通——upsert_candidate 无 label 参数、TaxonomyCandidate 无 label 字段、approve 硬编码 display_name=raw_value 英文 id，"不修则 AI 中文 label 全程丢失 defeat 工程目标"→加 suggested_display_name；**M2[blocker] 系统只有一种鉴权角色 AuthenticatedAdmin，计划原文的 AuthenticatedUser 不存在会编译失败**（架构事实：无运营态/管理态角色区分）；M3 端点 kind 集须 ∪ relationship_type（它不是 profile 维度）；M4 completeness 的 coverage_dimensions 与 profile_dimensions 是两套维度勿混；M5 profileStore 扩展不替换（loadActiveProfile 有 Shell.tsx visibleWhen 真实调用方）。labelFor 三情形分流（ok/unknown_value/no_dict），"诚实优先：字典查不到绝不显示错误销售标签"。前端 vitest 基线当时 168。

### 批次 10（2026-06-26）

35. **06-26 main-health-audit-batch1**｜main 健康度审查修复（SEC-1 evolution 三端点跨租户 IDOR→404 不暴露存在性；EVO-2 release/rollback 审计 actor 从常量 "admin" 改 admin.username；KNOW-1 知识预览合成 contact 误用 default_workspace_id 跨租户读泄漏；FE-1 前端 healthFromScores 坏函数伪造健康分——4-key 占位对 canonical 7 项，改由后端返回构建好的 health.items 复用 health_item 风险反转量纲）｜**残留线索**：Task 1 明确保留 `DEFAULT_RELEASE_ADMIN` 常量（evolution.rs:581 put_evolution_runtime_flag 仍用作 updated_by 回落）——EVO-2 只修三端点，runtime flag 端点的 updated_by 伪造面未修（与 12 号记录发现的 evolution.rs:742-746 updated_by 取请求体可伪造直接相关，见第 6 节）。**执行中重核**：Task 4 含"基线更新（PR#40 taxonomy-label-wiring 合入后重核）"节——确认 legacy.tsx:1950 本地 defaultHealthItems 已被 PR#40 修正为 canonical key，缩小本计划改动面（只动 store 侧）。

36. **06-26 frontend-backend-alignment-batch1**｜前后端对齐修复批 1（1 CRITICAL + 13 HIGH，主题="后端已有能力在前端不可达"）｜A1 CRITICAL：驾驶舱 loadMessages 调用两个**不存在的端点**（/contacts/:id/messages、/contacts/:id/decision-reviews；真实路由为 /conversations/:contact_id/messages、/decision-reviews?contactId=），且 Promise.all 全有或全无 → 5 面板全空 → 改 allSettled 单面板失败不拖垮其余。B2：InboxItem 把 category/questionForPrincipal/contactWxid 压扁成 title+summary 致"决策人盲裁"，补 4 可选字段、8 处构造点显式填 None（**有意不引入 Default "以免掩盖遗漏"**）。E1 红线级：设计 §6.3 承诺"已引荐态可撤销"但无端点——一旦引荐永久锁定被动答疑；补 clear-referral $unset 双键。B1：前端仅 2 verdict 按钮，后端 5 值闭集 3 种裁决不可达。**契约事实沉淀**：路由前缀分裂（outbox/principal-escalations/ask-human 带 /admin，evolution/evaluation-scenarios/accounts/conversations/decision-reviews 不带）；body 键 snake_case 与 camelCase 并存（mcp_api_key/to_wxid/session_id vs 多数 camelCase）；ChatWorkbench 用裸 fetch 非 lib/api。

37. **06-26 frontend-backend-alignment-batch2**｜对齐批 2（通用化前端断裂：前端写死销售语义→active profile 字典驱动，9 缺口三组）｜要点：E13 formatScores 写死 5-key 白名单静默丢弃 boundaryPrivacySafety→动态遍历+未知 key 回落原名"不吞"；C7/C8 抽 reviewLabels 共享闭集标签（FINAL_REVIEW_STATUS 10 项/HOLD_CATEGORY 3 项，实证 run_envelope.rs:67-78/types.rs:1227-1231）；C8 数据源决策——**否决**在 AgentDecisionReview 冗余写字段（"漂移风险"），改按 run_id 关联查 AgentRunLog（final_review_status 顶层 snake、hold_category 在 review doc 内 camelCase）；A5 conversation_mode 字典 seed（m028）与 profile.conversation_modes 声明解耦（"profile 声明本域用哪几种，字典声明 canonical→中文"）；D6 admin_taxonomies create 硬编码两 flag=false、patch 白名单不含→补齐（set_doc 键用 camelCase value.isTerminal 对齐既有 value.displayName）。**已知 YAGNI 遗留**：E10 关系建议把 contact_id 投进 contact_wxid 字段（"要真 wxid 需额外查 contact，本批不做"）；D7 trajectory_dimensions 只读展示不做逐项编辑、pressure_few_shot 不进本批 UI。**序列化三态并存事实**：DomainProfile snake_case（serde_json::to_value 无 rename_all）/decision_review_json 手写 camelCase/taxonomy 请求体 rename_all camelCase。批次 1=PR#44（merge 9d78282）；前端 vitest 基线 200。

38. **06-26 frontend-backend-alignment-batch3**｜对齐批 3（76 缺口路线图中 18 条 MEDIUM + 顺延 C6/C9）｜**计划自我修正**：原列 19 条，E11 management 高危确认流"写 plan 后复核最新 main 发现已完整实现"（confirm/reject 端点+乐观锁+前端按钮全落地）从批次移除。**重要红线缺口事实（E7）**：后端裸 `POST /api/operation-knowledge/chunks` **默认 status=active、不强制 draft**——手工新建切片须前端显式带 `status:"draft"+integrityStatus:"needs_review"` 否则"绕过 verify gate 直接落活跃池"（红线由前端自律护住，后端端点本身可绕过，列第 6 节待裁决）。E6 整替换陷阱：documents PUT 是 replace_one，前端必须回填 rawContent 等全字段否则清空、contentHash 丢失连带 D2 锚点回填。F23 疑似成交闭环（方案 B）：gateway fail-soft 提取 suspected_deal 信号→专表→list/approve/reject 三端点→approve 调 add_outcome_event_inner(verification="staff_confirmed")；verification 闭集中 conversation_inferred→400（"AI 永不直写 outcome_events"）。E9 口径消歧：D2 降级=active∧source_anchors 空（对齐 digest_inbox.rs:455）；知识缺口=knowledge_gap_signals pending 计数**不是** gaps.length。"serde rename 铁律（批次1/2 踩 5 次）"。批次 2=PR#46（ae54a8f）。

39. **06-26 prompt-pack-alignment-completion**｜prompt 种子机制重构（"版本盲三分支"→"空库分流"）｜**推翻此前机制**：PROMPT_PACK_VERSION 从"生效闸"**降级为仅 stamp 溯源**；非空库每次启动跑 delete_redundant（GC 上轮 archived）+ align_prompt_specs（逐 key 内容比对），"改 spec 重启必生效、不靠版本号"；ensure_missing_prompt_templates（只补缺不比内容）删除。顺序铁律：delete_redundant **先**、align **后**（颠倒会把刚归档的行立刻物删，破坏"归档而非删除"可回溯不变量）。Task 4 补 LRU 失效缺口：ensure/align 返回 bool（是否写入），运行时调用点（list 端点/ensure_default_souls）据此 fetch_add bump AtomicU64 prompt_pack_version，启动期 let _ 忽略（LRU 未建）；reset_prompt_pack_v2 签名不改（blast radius 大），空库/Err 臂手动返 Ok(true)。evolution_release/manual（seeded_by≠system）行 align 不动。

40. **06-26 main-health-audit-batch2**｜main 健康度审查批 2（并发写硬化 CONC-1/2/3 + GATE-1 动作闸复检 + KNOW-2 告警口径 + EVO-3 死字段接线）｜设计阶段"深度核实推翻两处原始方案"（CONC-2 弃 pipeline、CONC-1 不套整个 update）。CONC-3：load_or_create_operating_memory 首触并发 insert E11000 不透传、回落 find_one 取赢家。CONC-2：commitments 改原子 `$push+$slice:-8`（保最新 8 条），去重仍应用层快照判定——**明文接受并发重复代价**（planner 单选+按 id 幂等兜底）。CONC-1：memory_card 三字段拆出走 occ_memory_filter OCC 写，lost-race 静默跳过；门控外字段不能套版本谓词（否则永久 lost-race）。GATE-1：动作闸抽 apply_state_action_gate，初次 finalize 与 revision 后各调一次（revision 可迁移 operation_state 绕过初检）；taxonomy 软闸有意不复检。KNOW-2：unverified-warning 两处 count 补 status=active 对齐注入口径（归档已核验切片不再抑制告警）。EVO-3：threshold_auto_release_enabled 双闸（env 总闸 AND per-workspace 子闸，默认保守关），仅接 default workspace。**非目标明单**：evolution worker 多租户遍历（独立工程）、memory_summary 并发 OCC（接受 last-write-wins 纯文本自愈）、commitments 去重原子化、taxonomy 软闸复检。

41. **06-26 management-agent-thickening**（1203 行）｜指挥中心做厚：管理者自然语言操控全项目（NL→plan→确认→执行→核实闭环）｜**第一期权限放大**：dangerous 工具默认放行（dangerous_confirm_enabled=false 先跑通功能），但 **Irreversible（reset/delete）+ verify 类无视开关恒需确认**——verify 恒确认理由：verify.rs:101 写 source=Human/actor=admin.username，AI 调用会"被记成人确认"，守"AI 永不自动 verify"。ToolRisk 四档（Readonly/Low/Dangerous/Irreversible，未知工具保守 Low）；ToolOutcome 三态+`executed_unverified` 状态（"核实工具真实业务结果是第一期底线：调用返回 Ok≠业务成功（MCP success=false=账号离线），无法判定诚实标 Unverified 绝不假报成功"）；AgentToolCall.status 闭集 5 值。confirm/reject 乐观锁仿 principal resolve_escalation；execute_plan_tool_calls 抽公用（"项目历史踩过 dual-path drift"）。**Task 6.5 经 4 路 opus 实证核实重写**，修正原草案红线漏洞：旧锚闸只查 DEFAULT_MODE_GATE_POLICY，而该锚**故意不含反接管红线**（prompts.rs:2506-2511 测试坐实）——真红线在 user.reply.policy 正文 :1123/:1146，故先抽 DEFAULT_REPLY_REDLINE_ANCHORS 红线锚再扩锚闸；三层分级（Forbidden=evolution_critic_v1／ConstrainedEditable=user.reply.policy 等 4 key／FreelyEditable）+ 字面双闸（禁词+锚完整性，normalize 后比对防 CRLF 误拒）。**PR#42 冲突修正（最严重）**：update_prompt_template $set 必须置 `seeded_by="manual"`，否则启动对齐 align_prompt_specs 会把编辑行归档重种回 DEFAULT——"管理者改动活不过一次重启"。Task 6.6 LLM 第三闸：语义审查 diff 增量（堵"转给后台老师跟进"式插入型绕过），三态 Pass/Reject/NeedsHumanConfirm，LLM 不可用**降级人确认**（非 fail-closed 死路非 fail-open 放水），force=true 覆盖重提；管理 agent 路径与 REST PUT 路径同一判定函数。Task 9 生产部署冒烟：服务器 117.72.54.28、/opt/wechatagent、APP_PORT=3003、systemd Restart=always、RUST_MIN_STACK=8388608、USTC cargo 镜像（否则 crates.io 卡死）、冒烟范围=管理链路不真发微信、三闸真拦验证（含临时改坏 OPENAI_BASE_URL 验降级）。

42. **06-26 full-business-logic-test**（1237 行）｜全量真实业务逻辑测试基础设施（scripts/biz-test/ 的出生计划；19 号记录读过的脚本群即此产物）｜13 能力域≈30 生产 LLM 点，server 117 真 LLM 端到端。铁律：发送侧验证到 agent_send_outbox 为止**绝不真发微信**；绝不改 src/生产 prompt；**绝不假绿**（每域断言 llm_call_logs status=success 真调铁证，端点挂标 BLOCKED）；biztest_ 前缀隔离+幂等 cleanup；业务断言查 mongo，journalctl 只抓 llm 铁证+panic。批 A 销售域 11 脚本+批 B 行业域（心理/教育/医美 generate→draft 红线→activate→对话 canonical 值断言"非销售值"防假通用；切全局 active profile 必须最后跑+finally 恢复）。域②四阶段召回链（verified 命中→AI 改库降级→needs_review 召不到=红线预期非 bug→管理员 verify 恢复）；域⑪第三闸对抗样本（"安排后台老师亲自跟进"变相转介须拦+测后必须还原 user.reply.policy）；域④assist 关默认不发卡（critical：默认关发卡=全自治红线破）。run_all 单域失败不中断（"先全量出清单再修"）；findings.md 是测试产物快照。**实测确认接口档案**：webhook 不在 /api 下、HMAC X-MCP-Signature、import-preview 连字符路径等（纠正 spec 占位）。

43. **06-27 chunk-ai-repair-closure**｜F22 AI 修复闭环前端兑现（后端 propose/answer/applied 三端点此前空转）+F12 provenance 渲染｜纯前端零后端改动。红线：thenVerify 恒 false（落库只到 draft+needs_review，运营放行另走 verify 按钮）；防清空（PUT body 从原 chunk 值出发只覆盖勾选字段，复用批次3 E6 模式）；闭账失败不回滚落库（audit_failed 提示"已落库审计写入失败"）。**已实证修正节**（spec 表述≠真实代码）：propose 返回体 sessionId 在 body 非 header；BudgetExceeded 走 HTTP 错误非 200 字段；ChunkProvenanceView 前端仅定型 2/6 字段需补全。knowledge 频道用 plain css 非 module（避 tree-shake）。
44. **06-27 frontend-backend-alignment-batch4**｜对齐批 4 收尾（76→67 路线图剩余 9 条：D10/D11/F11/F14/F13/F15/F16/F17/D9）｜**gateway 终态 32 值闭集全清单**（run_envelope.rs:86-135 逐一对照）+ 中文标签避禁词（held_by_ai_policy=AI 策略主动暂缓）。**F16 关键技术分野**：`/knowledge/ask/stream` 是一次性 RPC 流，接自动重连=断连重发查询重复扣 LLM token（有害）**不接入**；只有 `/chat/sessions/:id/stream` 长连接监听流（幂等只触发 reload）接指数退避重连（base×2^attempt cap30s maxRetries6，任一业务事件重置 attempt）。F17 stale closure：error handler 读 resultRef 而非闭包 result（且必须在每个 setResult 处同步置 ref，不能用 useEffect 异步同步）。F14 移除 explore 死租户输入框（后端恒 admin.current_workspace，废弃字段 #[allow(dead_code)] 保留）。D9 domain_schemas 写表单复用后端现成 CRUD（create 落 is_active=false；FormDialog 撑不住嵌套动态数组故新建编辑器）。

45. **06-27 prompt-evolution-human-gated**｜prompt 自优化从半成品改造为"AI 提议+真模型证据+人工把关发布"闭环（堵 G1-G4 四缺口）｜阶段一（安全底线，独立可交付）：三闸从 routes/management_prompt_edit.rs 下沉到顶层 `src/prompt_guard.rs`（pub），原文件改 re-export 仅 3 个被用符号；release_prompt snippet 从**整篇覆盖（会删红线）改为末尾追加**（compose_appended_content：原文逐字保留→锚闸天然过），过三闸任一拒返回新错误变体 RedlineGateRejected→400；LLM 语义闸不可用时 release 中止（"请逐字核对后再发布"，不 fail-open 不死路）。阶段二（证据闭环）：PromptOverride 注入设计——不改 load_prompt 签名（纯数据访问保持纯净），decide_reply（14→15 入参）/review_decision（13→14 入参）加 `Option<&PromptOverride>`，**现有调用点全传 None 字节等价护栏**；prompt_shadow.rs 对每条历史失败 run 用"原+追加"跑真模型 Reply+Review 记新旧五闸对照存 proposal.eval_metrics；grade_prompt 改 **completed≥1 即 eligible_for_release（=证据就绪等人工），不再自动放行/拒绝**；修 G4 假基线（original 侧恒 None/false→取真实存的源 run scores）。LRU 零串味已核实（user.reply./review. 不在 llm_exact_cache_key 白名单，白名单仅 4 个 import/playbook/guide key）。**跟进项（PR#51 记录，均未阻断）**：Important#1 shadow LLM 消耗未回灌 EvolutionBudget 全局闸（倾向 fold-back 记账）；Minor-A dead config evolution_min_self_critique_delta 可清；Minor-B "恰好 2 次 LLM 调用"断言脆。阶段三前端仅骨架（待阶段二后细化）。

46. **06-28 campaign-targeted-push**｜活动定向推送引擎（campaigns/campaign_sends 新集合+两阶段圈人+create/preview/confirm_dispatch 生命周期+管理 AI 两工具）｜扇出=批量自建 kind=follow_up AgentTask（**不调 planner 私有 emit_planner_follow_up**），发送链一字不改（task worker→gateway→review→红线→outbox→MCP）。**真实 BSON 混合大小写陷阱**：Contact 无 rename_all（snake_case）但 OutcomeEvent 带 camelCase→成交事件真实路径 `outcome_events.productRef.productId`（索引/查询用错则建在空字段上恒空）。G4 红线：圈人只认 verification∈{staff_confirmed,payment_verified}+eventKind=deal（conversation_inferred 绝不进）；内存精筛复用 G4 净持有投影（买1退1不命中）。dispatch 恒确认（进 tool_always_requires_confirmation 而非 Dangerous 档——因 post_management_message 硬编码 dangerous_confirm_enabled=false）；preview=create+preview 合成一步（有意，让总控 AI 一次拿到预览）。campaign_sends (campaign_id,contact_wxid) unique=活动级去重闸；dispatch 重新圈人防预览后漂移；金额一律 Option<i64> 分。status 闭集 6 值+assert_campaign_status_valid。**YAGNI 明单**：前端频道、定时活动、任意布尔表达式引擎、效果统计仪表盘、支付闭环本期不做。唯一现场核实点=value_tier 阈值来源（复用现有不新造）。

47. **06-28 campaign-sends-report**｜活动漏推可观测：GET /campaigns/:id/sends 聚合端点（纯读零写链改动）｜7 桶分类（sent/pending/blocked/canceled/escalated/skipped/unknown）：**escalated=已请示（走请示通道待裁决后 AI 继续触达，非失败漏推）与 blocked（纯频控无后续）语义区分**；outbox_status=sent 最高优先命中即停（压过一切 status）；不认识的 status 诚实标 unknown"绝不强划进 sent"。关联键 agent_run_logs.source_event_id==taskId.hex + SOURCE_KIND_FOLLOW_UP_TASK 常量（不硬编码）；同 task 多条 retry 取 _id 最大；classify 收 Document 非 struct（outbox_status 是 dispatcher 动态 $set 字段，struct 序列化后可能缺 key，get_str().ok() 容错）。三集合三种 key 风格（campaign_sends camelCase / agent_run_logs snake_case / contacts snake_case）。
48. **06-28 campaign-frontend**｜活动推送结果看板（纯前端）：新一级频道 campaign+campaignStore（openReport 跨频道跳转）+7 桶→StatusBadge 5 tone 固定映射（sent→running/pending→scheduled/blocked→blocked/escalated→held/其余→inactive）｜escalated 标"已请示"非"转人工"（禁词红线）；command-center 跳转守卫 dispatchCampaignId（仅 status∈{succeeded,executed_unverified} 且 response.campaignId 为 string 才渲入口，dry_run/待确认防死链）；CSS Modules 绝不裸 import（Rollup tree-shake 历史坑）。

49. **06-28 contract-alignment-batch1-knowledge**｜前后端契约对齐机制地基（"投影函数级快照+前端 vitest 键集对账"双门）+知识域 5 投影首批落地｜机制：后端 #[cfg(test)] 构造全量 model→投影→递归排序键 canonicalize→写/读 `frontend/src/contracts/<name>.fixture.json`（**前后端唯一真相源**，UPDATE_SNAPSHOTS=1 bless）；前端 vitest 导入同一 fixture 与 CANONICAL_KEYS 双向对账（missingInFrontend/deadInFrontend）。**对账只断言键集合不断言语义/值/可选性**（避免误判有意设计为缺陷）；canonical 类型仅供对账不改业务类型。Task 6 刻意快照 chunk 详情端点裸 struct（snake_case+$oid，与列表投影 camelCase **形状冲突**）——"让统一与否成为可见的产品决策而非静默漂移"。防腐烂 lint：运行时扫 src/routes/** 找 `fn *_json -> Value` 投影，断言每个非豁免投影名出现在 assert_contract_fixture 的 ~600 字符窗口内（production 调用不算覆盖）；ALLOWLIST 含批次 2-5 未覆盖域投影 30+ 项（"批次铺开时逐项移除使 lint 渐紧"）。CI：移除 paths-ignore 的 frontend/**，dorny/paths-filter 前后端分流；backend filter 含 frontend/src/contracts/**（fixture 由后端 bless）。

50. **06-28 customer-reply-guarantee**｜全自治"客户必有回应"保障（堵 held/blocked/precheck 晾死漏洞）｜**头部实现期修正（计划被 CI 证伪后改写的罕见实例）**：原案 4 挂载点，CI 集成测试 full_flow_a3_no_reply_skips_review_and_outbox **证伪 A3 挂载**——no_reply 是 AI 主动判定沉默（非晾死），补占位破坏拟人→最终守卫只挂 3 处真晾死出口（两道 precheck+拦截分支），no_reply 移入豁免清单。黑名单语义全兜底：Inbound 且 status 不在 8 值豁免清单（cooldown/rate_limited/quiet_hours_deferred/expired/superseded_by_new_inbound/not_managed/context_changed/no_reply）即补确定性占位（fallback_holding_reply，agent-first 绝不 LLM 产占位）；FollowUp 任何状态不补（AI 主动触达客户没在等，"稍等我给你准信"是非所问）。幂等键 `{source_event_id}#ack-placeholder` 与真回复/分段 #seg{idx} 天然不碰撞；入队前 should_abort_send 复查（客户又发新消息→下轮真回，防去抖破坏）。**方案 B 解耦（根因修复）**：escalate_held_decision 移除客户占位补发——原耦合在一连串"领导骚扰门"之后，任一门命中 return 时客户占位一起被跳过=晾死根因；改后该函数回归"只推领导卡+落台账+写 awaiting"，客户占位统一由守卫负责。plan 补上 spec §3.1 遗漏的第一道 precheck 挂载点（daily_limit 决策前拦）。DEFAULT 行为变更明示：FollowUp+safety_guard 路径从"发占位"变"静默"（改进）。

51. **06-28 e4-document-repair-f21-task-list**｜E4 文档级批量修复（前端 DocumentRepairPanel 聚合复用 PR#49 ChunkRepairPanel，后端零改动）+F21 任务总览列表（chat_task_list 端点+TaskRail 列表化）｜limit clamp [1,200] 默认 50；列表项不带 plannedSteps/cards 全文（控 payload，详情走 GET /tasks/:id）；status 过滤非法值宽松忽略；列表拉取失败静默降级保留手工输入 fallback；不复活 operation_knowledge_items、不改 worker execute_step 占位桩。

52. **06-28 memory-conflict-and-reviewer-yield**｜⑨记忆冲突治上游+④reviewer 让位下沉｜⑨核心：注入给 consolidator 的当前卡先 auto_upgrade_plain_facts 带**稳定 id**，且与 prev-merge 用**同一升级实例**（否则 from_plain_text 每次 fresh UUID，LLM 引用的 id 在合并时匹配不上）；live_dimension_names 注入已有维度名引导跨轮同属性沿用同名（冷启动空清单不注入=字节等价）。consolidator prompt 三改：coreFacts schema 从空数组改 {id,text,dimension,importance} 对象示例（**"A/B 已证可选字段被 LLM 无视"教训**）；fact 原子化"一条只讲一个事实"（治累积巨型 summary 根因）；dimension 从可选升为**改口场景必填**（镜像⑥决策墙手法）。bump PROMPT_PACK_VERSION v16。④REVIEWER_ASSIST_YIELD_NOTE 共享常量（放 referral.rs 供 reply+review 复用防漂移）：解两条 hold 路径——引荐专属顾问不属"第三方角色红线"、引荐非产品声明不计 hallucination/factRisk；assist 关账号 reviewer prompt 字节等价；措辞「专属顾问/增配/我仍在场辅助」过禁词 lint。真测纪律：多 seed 变体防过拟合（预算改口变体验证同机制泛化）、端点污染标 BLOCKED 不假绿。

53. **06-28 memory-consolidation-guards**｜⑨记忆固化确定性兜底两件套（治 consolidator 偶发降级致改口新旧值并存）｜件一：compact 救回循环加 dimension 感知——previous 的旧 fact 带非空 dimension 且 incoming 已有同 dimension 新值时**不救回**（治 LLM 漏填 discarded 时旧值被 text 不等救回）；dimension=None 维持 text 去重字节等价。件二（方案 X）：`fact_is_non_atomic` 结构性非原子检测——**纯结构度量三判据 OR（≥2 换行/≥2 句界标点/>80 字），零关键词零数值实体**（"找 N 岁"是关键词模式违 agent-first 红线）；检测到 blob 重试一次（consolidator 不在 LLM 缓存白名单，重试即全新调用；v4 探针 6/6 干净故至多 1 次）；重试仍非原子→丢弃非原子条（下轮固化重新产出）。**明确不改 prompt**（v4 探针证明 prompt 正常无缺陷）、不碰硬闸阈值、不拆 consolidator。计划内修正决策：取消外部集成测试文件改 lib 内测试（避免 pub 放大可见性）。
54. **06-28 prompt-evolution-phase3**｜prompt 自优化阶段三：新旧对照证据透出到演化候选详情卡｜后端 shadow_replay_json 补 original5gateHit/originalSelfCritiqueAddressed 两字段（数据已在模型仅没透出）。**wire 风格差异明示**：后端 JSON key camelCase，但 evalMetrics 内字段 **snake_case**（grade_prompt 写 snake_case，bson_doc_to_json 原样透出不转 camelCase）。FIVE_GATE_KEYS 固定 5 值序（fact_risk_block/pressure_risk_block/human_like_score_rewrite/emotional_value_rewrite/product_accuracy_score_block）。MetadataSection 聚合 key 移出**仅 kind=prompt 时按白名单**——five_gate_hit_delta_per_gate 等 key 与 threshold 类共享，笼统按 key 名过滤会误伤。Δ 语义色方向：五闸命中率降=好（绿），自评解决率升=好（方向相反）。聚合 doc 只有 per-gate delta 无原始/新命中率（原始明细由样本点阵 ●○· 呈现，两表互补）。

55. **06-29 memory-summary-not-authoritative-fact**｜⑨真因修正（12K 极小改动，一删一改）｜**三段修复系列的终章**：06-28 两件套（dimension 感知救回+非原子检测重试）落地后 **server117 全量真测证明"件一件二未触及真因"**——真因=memory_summary（短期滚动上下文，gateway 逐轮 append 累积）被 memory_card_from_contact:215 当权威 core_fact 注入种子卡，8 段 summary 累积成 blob 致 8 岁/10 岁并存。修复：删该 push、内容归位 extra.recentEpisodeSummary（identity 单值回落/human_profile_note/tags 注入不动）。这是"计划→实施→真测证伪→再定位真因→极小修复"完整闭环的标本案例。

56. **06-29 content-assets-tiered-injection**｜内容资产分档注入+清理过期录入项｜修复"文本资产注入绑死 Full 档、三段式降档后失效"缺陷：ContentAsset 加 min_inject_tier（闭集 lean/relational/full；None/非法=full **逐字等价改造前行为**）；查询下推语义：Full 档 $or 纳入字段缺失（老数据），Lean/Relational 只取显式值。安全红线：**media_id/url 字段保留不删**（media_id 是文件发送链命脉：ensure_media_uploaded 缓存/防重发不变式），仅去掉表单录入项+moment_media 选项+create 端点入参。加字段后 4 个构造点必须同步补全防 E0063。

57. **06-29 campaign-domain-completion**｜campaign 域从"只能总控 AI 操作"补全为独立运营界面（GET /api/campaigns 列表端点+前端三视图列表/建活动/看板+CSV 导出+翻页）｜**dispatch 红线**：前端绝不做 dispatch 按钮（真发送只走总控 AI 恒确认门），create.test.tsx 必须断言"无 dispatch 控件"；预览区只提示"确认推送请在 AI 总控对话中 dispatch"。CampaignListItem 投影**不泄漏 workspace_id/segment_filter/intent_text**（照 ProductView 范式，createdAt 转 RFC3339 非 {$date}）。draft 复用（一次会话一个 draft，改条件只重 preview 不再 create）；bucketTone/Label 抽 buckets.ts 防 csv↔Board 循环依赖；CSV \uFEFF BOM+RFC 转义；列表列头 title 注明"已扇出≠真实送达"。Campaign 存储键 camelCase（workspaceId/createdAt——与 Contact snake_case 不同集合不同风格再现）。

58. **06-29 contract-alignment-batch2-operations**｜契约对齐批 2（运营/Agent 域 9 投影：behavior_signal/outcome/llm_call_log/memory_candidate/operating_memory/agent_run/decision_review/guide_preview/operation_health）｜完全复用批 1 机制+ALLOWLIST 移除使 lint 渐紧。**历史改名痕迹**：AgentOutcomeMetric 有历史 serde alias `human_handoff_success_rate`（禁词），现字段名 `ai_hold_cleared_rate`——构造测试**绝不写旧 alias**（no-human-takeover lint 硬门）。铁律"raw Document 嵌套桥接的 fixture 必须 bless 生成绝不手写"（嵌套 BSON DateTime/ObjectId 会成 {$date}/{$oid} 手写必错）；"键数以 bless 出的 fixture 为唯一真相源"（批次 1 document '16 键'笔误教训）。AgentRunLog 全字段构造（计划内表格 35/注释 38 不一致，最终指令"以编译器 E063 报错为准补齐不要猜"）；operating_memory 用 default skeleton 第三分支（memory_card 与 context_pack 都空时的确定形状）；operation_health 聚合投影吃 3 个 model 引用只下发 2 顶层键。

59. **06-29 contract-alignment-batch3-taxonomy + batch4-evolution**｜契约对齐批 3（字典/分类域 5 投影）+批 4（进化/实验域 8 投影），机制同批 1/2｜批 3 特殊点：taxonomy_entry 的 value 嵌套 6 子键（TaxonomyValue 的 priority_weight/is_terminal/is_reactivation_target 三字段投影不映射）；operation_domain 22 字段投影只下发 20 键（**principal_decider/high_risk_escalation_mode 不下发**）；methodology 等 5 个是 String 非 Document；计划物理排版顺序 4,5,1,2,3,6,7 与 task 编号不一致（编辑顺序痕迹，加了导航注记）。批 4 特殊点：**cohort_run_ids_json 不纳入契约机制**（返回裸数组非对象，无顶层键集概念，改归 helper 豁免区）；experiment_envelope 的 cohort Vec<ObjectId> 只下发 .len() 数字不泄漏；runtime_flag rolloutPercent=clamped 方法值+rolloutPercentRaw 原始值双发、id 不下发；proposal_detail 29 键最重；experiment_summary 聚合吃 2 参数顶层仅 3 键。8 个 task 改同一文件同一 mod tests（串行 dispatch 防陈旧行号，批次 2 同坑教训）。四批累计基线 1686→1695→1720→1728。

60. **06-30 content-vs-knowledge-nav-disambiguation**｜纯导航文案对齐（内容资产 vs 知识库 Wiki 概念混淆消除，7 个字符串字段+walkthrough.py 2 行）｜**spec 假设被证伪的实例**：spec §5"grep 确认无测试硬编码引用"被 plan 证伪——walkthrough.py:84/87 字面依赖"Wiki 管理"文本（本地手动走查工具不在 CI），plan 补上同步。knowledgeWiki subtitle 从"schema/信号/历史"运维向改为"录入/审核/问答"（探索证实该频道实际是全功能知识工作站，旧文案让想录知识的管理员扑空）。
61. **06-30 forbidden-expression-injection**｜禁用表达独立段注入（修**语义反转**缺陷：禁语素材混在"可引用内容资产"段=把禁语当可用素材喂给 LLM）｜split_context_assets 纯函数分流 (referable,forbidden) 两段；**禁语恒注入无视 min_inject_tier**（安全红线，查询 $or 分支不带 tier_cond）；模板新增"以下表达禁止使用（不得直接说也不得改写后变相说）"段；占位符对位铁律（新增 {} 在第 N 个则参数必须插第 N 位，Rust 编译器数量不匹配硬报错是保护）；limit 12→16；不加出站硬拦截门（本次只改注入语义）。

62. **06-30 evolution-ui-toggle**｜演化中心 UI 开关（运行时即时生效不重启）｜**EVOLUTION_ENABLED 语义反转**：默认 false→true，从"是否启用"降为"是否允许 UI 开启"（=生产硬上限，显式 false=运维硬锁定/紧急熔断）；mongo evolution_runtime_flags.enabled 升为总开关（**出厂仍关**——默认无文档=关）。"开=全量"：UI 开时 PUT rolloutPercent:100（高级折叠里可设灰度）。worker 无条件 spawn，env=false 进函数即 return；tick 内 mongo flag 门已存在。前端三态（运维硬锁/可点开关关态/开态）收敛单数据源 GET runtime-flag（含 envEvolutionEnabled）；enabled prop 保留不删（7 处测试依赖）重定义为 env 覆盖。最高危点=Tab 挂载 fetch 顺序变化（runtime-flag 先 experiments 后），既有 7 用例 mock 序列必须前插一个。

63. **06-30 guide-apply-partial-validation**｜guide apply 部分应用+prompt 注入合法值（治双向）｜治下游：apply_contact_changes 三个枚举校验点（customerStage/intentLevel/operationState）从"越界整请求 400 连坐合法字段"改为"越界记 SkippedField 跳过、合法字段照落"；**绕过共用 helper apply_admin_dim_validation**（它把 Reject 吞成 Err）就地 match 原始 DimValidation——不改 helper 因 contacts.rs 手动表单/management.rs AI 建档仍需硬拒（三路径语义分化：guide=LLM 产值宽容、手动/审批=硬拒）。治源头：preview prompt 注入状态机合法态+字典 canonical 值（"LLM 凭空猜必然越界"；空字典兜底"暂无受控取值，留空不要臆造"）。canonical 逐字警示：intent_level 是 `medium` 不是 mid。skippedFields/appliedFields 回流响应+审计事件；前端 toast 动态拼接不硬编码字段名；孤儿 operationStateReason YAGNI 界定不联动。biz-test 断言翻转（原把 apply 越界 400 当红线生效，修复后 400 反而是回归 critical）。

64. **06-30 h3-cross-tenant-idor**｜H3 跨租户 IDOR 修复（14 handler 认证后水平越权：调用方自带 workspaceId 解析后未校验 ∈ admin ACL）｜llm_providers×7+domain_schemas×5+domain_profiles×2 统一把 `X.unwrap_or_else(|| admin.current_workspace)` 替换为 resolve_authorized_workspace（每请求校验含回落值，单一闸）；is_workspace_authorized 纯函数（**空 ACL=单租户回落语义只允许 default workspace**，与 switch_workspace 同源）；越权→BadRequest("workspace_not_in_user_acl")——**AppError 无 Forbidden 变体**（错误码复用既有）。switch_workspace 内联判定改调同一纯函数（DRY）。集成测试只挑 activate_provider+list_providers 两个可达 handler（其余请求体 pub(super) 外部 test crate 不可命名）；"其它文件同模式命中属范围外——记录但不在本 PR 处理"。

65. **06-30 h10-relay-identity-provenance-fix**｜H10 relay 身份从"内容前缀"改"来源凭证"（终审 UPHELD High 安全漏洞：客户伪造 `__PRINCIPAL_RELAY__` 哨兵可劫持领导决策转述模式+绕过所有频控闸）｜核心设计：ConversationMessage 加 `is_synthetic_relay: bool` **`#[serde(default, skip_serializing, skip_deserializing)]`**——绝不落库、反序列化恒 false（客户 payload 显式塞 true 也无效=不可伪造根基），仅 synthetic_principal_relay 构造器内存置 true；~30 构造点机械补 false（E0063 编译器强制逐个定位）。is_principal_relay_trigger 改读标记（gateway relay-exempt 频控豁免与号码护栏因调用它自动收敛，不改 gateway）。LLM 层第二道防御：prompt_isolation 三纯函数——合法 relay 保留哨兵触发转述模式（byte-equivalence 护栏），一切客户内容（当前 inbound+history）剥哨兵。**关键不变量**：合法 relay 合成消息从不落库→history 永不含合法哨兵→history 无条件剥哨兵零误伤。可见性红线：relay 判定/网关 API 全 pub(crate)，集成测试不可见——**绝不为测试放宽生产可见性**，安全断言全放 lib 内。Task 6 可选一致性加固（knowledge_router/reaction/review/memory 非转述路径统一剥哨兵，"防未来把转述契约加进这些 prompt 时复活攻击面"）。

66. **06-30 content-assets-injection-hardening**｜content_assets 注入加固（修 06-30 forbidden-expression 落地后核实出的 4 问题）｜A **禁语恒注入被共享 limit(16) 击穿**：可引用与禁语一次查询共享 limit，可引用吃满 16 名额禁语被丢→拆两次独立查询（禁语无 tier 无 limit 恒全量"绝不与可引用争名额"）。B **读写 workspace 锚定不一致**：三个 Agent 加载器（load_context_assets/load_sendable_assets/load_referral_cards）用 state.config.default_workspace_id 而写入用 admin.current_workspace→签名收 workspace_id 传 contact.workspace_id。C filter 抽三纯函数+query-shape 单测（防"漏 workspace pin→跨租户泄漏；禁语误带 tier→恒注入破；漏 tier_cond→分档失效"）。D 前端禁语行显"恒注入"徽标、隐藏 tier 选择器（不再误导）。E 陈旧注释修正（"可引用内容资产仅 Full 注入"已过时）。Task 1/2 暂无生产调用方时不跑 -Dwarnings（dead-code 预期，Task 4 接上后再验）。

67. **06-30 wiki-audit-high-fixes**｜wiki 审查两个 High 缺陷修复｜**#1 domain_schemas serde 字段名错配（重大静默失效）**：路由层 11 处查询/`$set`/sort 用 camelCase（workspaceId/isActive/updatedAt）而模型无 rename_all 实写 snake_case→load_active_domain_schema **恒 None**→enforce_domain_attributes（required/enum 动态字段校验）**从不执行**——即 06-27 D9 交付的字段表功能后端从未真正生效过；修复=11 处字面量改 snake_case。**#2 prompt_templates create/publish 绕过红线闸**：create-draft→publish 链可绕过 update 单点拦截的三闸→create 补字面双闸、publish（draft→active 最终生效点）补字面双闸+LLM 三闸；**force 只跳 LLM 不跳字面闸**（禁词/锚完整性是确定性硬闸不可绕）。**测试覆盖弱点诚实声明**：handler pub(super) 集成测试不可直调，"handler 真的接了门"这一事实只能由终审逐行 review 保证（"这是 pub(super) 的客观限制，不是可通过加测试消除的"）。

68. **06-30 contract-alignment-batch5-config-playbook**｜契约对齐批 5（配置/playbook 域 5 投影：playbook/prompt_template/evaluation_scenario/suspected_deal/outbox_entry），**五批工程收官**——移除后 ALLOWLIST 只剩 6 项永久 helper 豁免｜**关键决策（执行前全面审查定论）**：evaluation_scenario 的 contactSeed/groundTruth Document 直发**投影不改**——bson_doc_to_json 不是消毒器（= serde_json::to_value(Bson::Document)，与 json! 直发同一 serde 路径字节级相同输出，改桥接是 no-op）；泄漏只取决于内层类型（Int32/String 等干净，仅 ObjectId/DateTime 泄漏 $oid/$date），而这两个 Document 来自 HTTP JSON 入口结构上不可能含之。投影漏发档案：playbook 漏 createdAt、prompt_template 漏 5 键（createdAt/currentVersion/previousVersion/seededBy/locale）、outbox_entry 漏 3 键（mediaAssetId/referralCardId/reclaimedInFlight）、suspected_deal 1:1 零漏。**decisionId None→null 与 id None→"" 的不一致**在投影中并存（Option.map(to_hex) 无 unwrap vs 有 unwrap_or_default）。

69. **07-01 wiki-three-p0-fixes**｜wiki 三条 P0（红线收紧+类型透传+派工闭环）｜**①-a 红线重大收紧：auto-verify 从"只拦 product_fact"改为对所有 chunk_type 都不自动 verified**——enforce_product_claim_human_audit 改名 enforce_verified_needs_human_audit 删 chunk_type 参数，"auto-verify 退化为预审分诊：过闸的挑出来等运营重点看，绝不自动放行"；旧测试 non_product_fact_verified_kept 断言被改写（"行为定义变更不违反增量叠加铁律"）。①-b 冷启动 peer_case 推送加 integrity=verified 过滤（"active/approved 是启用门，verified 是内容门，双门都过才可对客推送"）。②导入类型透传：请求体加 wiki_type/chunk_type（此前 LLM 吐的类型被 serde **静默丢弃**）；chunk_type 加入 DEFAULT_LOCKED_FIELDS（7→8 项，PBT 自动覆盖）；抽取 prompt 加 9 类 wikiType/4 类 chunkType 说明并**删除 2026-05-25 已删死字段的指令**（safeClaims/forbiddenClaims/evidenceItems/routingCard——"prompt 让 LLM 产它们纯浪费 token+误导"）。③抽 propose_chunk_repair_inner（不自建 budget scope，ambient RUN_BUDGET 读取）供派工 fix_chunk 复用产可审草稿（StepOutcome 加 details 字段，repairDraft 进 completed_steps 持久化+SSE 推前端；红线：worker 绝不 apply）。**禁词自查实例**：计划正文自己写了"人工核验"随即标注"含'人工'是禁词，落地时改'运营核验'"。

70. **07-01 h1-ingest-worker-not-due**｜H1：ingest worker not-due 误刷 last_fetched_at｜根因：not-due（没到 schedule_minutes 未发请求）与真 304 共用 SourceOutcome::NotModified→run_one_round 无条件 mark_success 刷 last_fetched_at→worker interval(60min)<schedule 时"每 tick 把节流基准无限前推，源首拉后永不更新，schedule 越大越必中招"。**潜伏原因**：现有 smoke 测试都从 last_fetched_at=None（is_due 恒 true）进入，从没走 not-due 分支。修复：拆 Skipped 变体（不写任何 DB）；真 304 仍走 mark_success（"304 是成功探测该刷"）。
71. **07-01 h7-m016-collection-names**｜H7：m016 workspace_id 回填迁移的集合名表四类失效｜7 处拼错真实 accessor 名（accounts→wechat_accounts 等，update_many 匹配 0 行**静默失效**）+漏收录 15 个带单值 ws 的集合+误收录 2 个无单值 ws 字段的集合（admin_users 用 workspaces:Vec、chunk_revisions 靠 chunk_id 反查租户——回填是注入垃圾字段）——"多租户过滤上线后这些集合 legacy 行数据黑掉"。修复+4 个 lib 一致性单测：**审计基准 const 按 snake/camel 拆分交叉锁死**（基准由 models.rs 逐 struct 亲验定稿带 file:line，"不是从表抄一份对自己那是 tautology"）；spec §7 教训记载"首轮抽样亲读漏了 chunk_revisions"→要求机械穷举复核不接受抽样。

72. **07-01 h8-boot-brick-stale-index**｜H8：启动砖——E5-T1 迁 4-tuple 多版本索引时**漏删两处旧 unique create_index**（operation_domain_configs 2-key/operation_state_policies 3-key），建完即被 ensure_ops_versioned_indexes drop 是残留死代码，却带 .await? 致命语义：多版本数据（admin publish 攒下同键多 version 行）下二次启动建旧 unique 撞 E11000→main.rs ? →**进程启动崩溃（prod-117 部署炸雷）**｜潜伏原因：现有测试全用空库跑 ensure_indexes。修复=删两处+注释改写；3 个回归测试用 raw doc seed 多版本行精准复现（raw() 正为集成测试直写 BSON 而设）。
73. **07-01 chunk-request-deadfield-cleanup**｜请求体死字段清理（正规层）：删 OperationKnowledgeChunkRequest 六个 100% 空转字段（routing_card/safe_claims/forbidden_claims/evidence_items/unsupported_claims/verified_claims——转换函数一个都不读、model 无这些字段）+连带删**恒不可达的 rejected 分支**｜重要否决记录："body/summary 非空→rejected"新判据**经审查证明会误伤正常草稿已否决**（绝不引入新判据）；distortion_risks 保留（preview→前端活 wire 字段）；chat 裸 $set 旁路（字符串 key 直写 Mongo 独立于 struct）不动另开专题。删字段后带旧 wire 键的请求 serde 静默丢弃不 400（无 deny_unknown_fields=向后兼容）。preview 路径红线强化：恒 0 verified（anchor 命中只作审计线索）。当时基线 1777/0。

74. **07-01 user-ops-cockpit-redesign**｜用户运营驾驶舱重设计（Spec A）：9 段纵向堆叠→"常驻判断条+观测/配置段控+下钻"三段式，**把后端已产出但前端未表达的能力顶到前台**——finalReviewStatus 10 态精确映射（取代 approved 二值"通过/拦截"）、conversation_mode 四态人格 chip、健康度 tone 三色、作息灯（inQuietHours/nextWakeAt）、请示灯、记忆溯源（MemoryFact 的 confidence/importance/deprecated 结构此前被当纯文本渲染"浪费了结构"）、决策复盘自治协议 9 字段（此前完全没展示）｜Task 1 唯一后端改动：operation_health_json 补 3 个 quiet_hours 只读字段（端点内加载 domain_config→runtime+profile 算好传参）。finalReviewTone 四组硬编码 10 态（"闭集穷举合理非魔法值"）。Self-Review 发现并修正笔误（ConfigureView 应在 Task 2 就独立成文件）。行为等价迁移纪律："verbatim 搬迁现有 JSX+重接 props，不新写逻辑，搬迁时逐段核对 legacy 源不凭记忆"。

75. **07-02 decision-review-autonomy-protocol**｜决策复盘补齐 AI 自治协议 9 字段（"AI 内心独白"三组展示：回复决策/理解/运营依据）｜纯读投影——9 字段已由 to_document(&final_decision) 落在 agent_run_logs.decision（camelCase），join 时多抽 9 字段投影为 autonomyProtocol 嵌套对象（9 键全空→null）。**优雅降级是硬要求**：覆盖"历史旧数据"+"管理 Agent 主动发送路径（send_contact_message_gateway 写复盘但不写 run log）"两类现实来源。契约快照 29→30 键 re-bless+CANONICAL_KEYS 同步（"如实标注为必要连带"）。
76. **07-02 h11-evolution-threshold-keys**｜H11+M9+L1：evolution threshold 重判三缺陷（**功能上线以来被架空**）｜H11：gate_key_to_score_field 只读 factRisk/productAccuracy，但 ReviewScores rename_all=camelCase 的**真实序列化键是 hallucinationScore/knowledgeGroundingScore（factRisk 仅反序列化 alias）**→生产文档 get_i32 miss→score 恒 0.0→fact_risk 恒不命中、product 恒误命中→**#152 安全回归门+5 闸涨幅门被架空**；修复=改用同模块已有的双键兼容 read_gate_score+删死函数。M9：original_5gate_hit 硬编码 Document::new()→significance 的 original_rate 恒 0→delta 虚高偏拒；修复=5 闸循环同时产出真实 original（用 current_value）/new（用 proposed）两向量。L1：死 match 臂+误导注释消除。3 个用真实序列化键 seed 的护栏测试（旧代码下必红）。
77. **07-02 m13-profile-attributes-preserve**｜M13：前端 saveOperationProfile 保存即清空 AI 积累画像｜根因："**后端把'没发'当'清空'**"——前端表单不管理 profile_attributes（由 AI 在 gateway 积累），PUT 不带该字段→payload #[serde(default)] 空 Document→handler 无条件 $set 清空。修复=镜像 gateway.rs:4034 非空守卫（空则跳过保留现值）；前端不改（不发是正确的）。handler+请求体改 pub 供直调集成测试（serde_json::from_value 构造私有字段请求体范式）。

78. **07-04 inbox-ui-jargon-cleanup**｜统一收件箱 UI 重做+前后端全量清黑话｜三层根因分层修复：A 后端嵌在拼接串里的英文枚举**前端字典翻不了**→后端拼串前映射中文（escalation 请示串/digest 兜底文案）；B 扩 active-view 下发 4 个已有中文 label 的字典 kind；C 前端 reviewLabels 扩 8 组字典（gap_signal 10 类 kind/escalation 3 category 5 verdict/风险维度含历史别名双键）+折叠式 InboxRow+CSS 补全。**追加 Task 8/9（全分支审查发现）**：8 个字典只有 1 个真正接进 UI，**其余 7 个是"加了没接"的死导出**；且计划外文件 ResolvedEscalations 有自己硬编码标签与新字典**措辞漂移**（approved:"批准"vs"同意"）——Task 8 统一共享字典+删 3 个无展示位死导出；Task 9 gap_signal kind/severity 端到端下发（用独立 signal_severity 字段而非覆盖 severity——后者是跨来源排序标度，标度不同不能覆盖）。resolved_via 后端只写 wechat/admin 两值（前端已有的 principal_chat/admin_direct 是**永不出现的死键**）。排除项（涉阈值红线单独议）：情绪价值阈值三处不一致（后端 6/前端 5/CLAUDE.md<5）；请示 severity 恒 high。

79. **07-06 escalated-run-budget**｜B-1 修复（来自 07-05 newuser-journey 四路审计冒烟）：progressive-tier 升档 run 分档 token 预算｜根因：Lean→Full 升档触发**两程 reply.task**，叠加超 base run_token_budget(30000)→blocked_by_budget **静默拦截，需知识的首触问题永不回复**。修复：RunBudget 加 escalation_bonus（默认 0），gateway 两处升档分支在第二程重生成前 grant_escalated_ceiling 抬**判定用 gating 上限**到 run_token_budget_escalated（默认 100000，运营域可配 BSON key runTokenBudgetEscalated）——**绝不改 tokens_used 真实累计**（成本如实反映）；未授予时行为逐字等价（既有单测断言不改）。锁顺序纪律：escalation_bonus 独立语句取值即释放再取其它锁（防与 record_tool_call 的 tokens→tool_calls 顺序成环）。不动 max_llm_calls/tool_call 两维（升档健康路径 4≤6 非绑定约束）。

80. **07-06 knowledge-workbench-dispatch-wiring**｜知识库工作台派工链路补通（此前"派工长任务"空转：execute_step 是占位桩+手打框无结构化入口）｜两条结构化入口（卡片驱动 DigestCanvas 多选批量派工+对话驱动 ChatWorkbench 承接 plannedSteps 确认小卡）汇入同一 chat_task_create；**cardId→targetChunkId 在派工落库时解析**（extract_chunk_ref 从卡片 target_refs 取第一个 kind=chunk 非空 id；step 已带则尊重）。6 action 分层真实现：fix_chunk/add_chunk/retag 真产 draft+needs_review 草稿（**复用 apply_create/update_chunk 硬编码红线**）、analyze_logs 只读 24h 拦截事件摘要、dismiss 真标记卡片（$addToSet dismissed_card_ids）、review_evolution 产人工评估指引（"不假装自动评估"）。freeform 卡不可派工（suggestedAction 闭集含 freeform 但不在 ALLOWED_TASK_ACTIONS）。extract_knowledge_tags_inner 抽出供 handler 与 worker 共用。删手打框（stepsText/stepAction 状态+dispatchTask+JSX 整块）。

81. **07-07 taxonomy-candidate-inbox-card**｜标签候选收件箱卡片小白化（走不通的 inline 二元按钮→rich 卡片带证据+命名表单）｜后端只改只读投影（taxonomy_candidate_to_inbox_item 具名纯函数，rich_params 带全 8 字段**无需新 get-by-id 端点**）；title 人话化不暴露裸维度键（"AI 新识别标签：焦虑"）；409 视为已存在提示不当错误；rawValue 自动并入别名。**降级条款模式**：Task 4 复用组件去重有明确降级判断标准（"若删内联表单后 taxonomyFlags.test 无法不改断言通过=耦合过深→git checkout 还原，改记后续清理"）。"AI 提议+人工确认"闭环不变。

82. **07-07 user-ops-roster-batch-enroll**｜全量通讯录+批量托管+头像（roster 功能线起点）｜avatar_url 全链路（Contact/WechatAccount/ApiContact）；MCP 好友工具**名称不确定性处理**：计划背景与代码注释自相矛盾（背景说"真实名 contact_list 非 contacts_fetch_cache"、代码注释说"contacts_fetch_cache 线上亲验 contact_list 不存在"——编写过程中信息更新的痕迹，最终用 contacts_fetch_cache）；解析器对头像 key 用优先级 fallback（bigHeadImg→smallHeadImg→headImgUrl→avatarUrl→headimgurl）+数组路径多候选（structuredContent.contacts/friends/list/顶层/content[0].text 内嵌 JSON）。批量托管：初始画像从同步改**异步 AgentTask（kind=initial_profile）**不阻塞；**两条路径共用抽出的 apply_generated_profile_to_contact 防漂移**（本计划唯一重构）；worker 找不到联系人/已被取消托管视为完成不重试。roster 端点左连接标注 agentStatus 三值（managed/normal/not_imported）；batch-enable 前置校验 account 在 wechat_accounts 注册（"否则 webhook 入站会被拒收"）。

83. **07-08 roster-fetch-cache-fix-m1-m2**｜roster 修复模块 1+2（**07-07 计划的 fallback 全没猜中真实形态**）｜线上 117 亲验：contacts_fetch_cache 就绪返回 `{result:{friends:[wxid字符串]}}`——**纯字符串数组非对象**，原候选路径无 /result/friends 且要求元素 as_object→恒解析空。修复：候选选择改"取第一个真正**是数组**的候选"（防高优先键存在但非数组短路后面真数组）+字符串元素分支+contact_like_array 按内容识别兜底。模块 2：**空 cache({}/Null 未就绪) 与真 0 好友(friends:[] 已就绪) 语义区分**——空 cache 同请求短重试(2s×3)仍空则 syncing=true，前端显"同步中"自动重拉（不误导"暂无好友"）。

84. **07-08 system-strategy-tab-layout**｜系统策略频道 7 平铺面板→4 职能 tab（消除页面无限长）｜只改顶层加 tab state+条件渲染，面板内部零改动；复用现有 .profileTab CSS 类不新建视觉；TDD"先红"（测试先依赖新 tab 结构，报"找不到 button 标签与状态"即证明测试真依赖）；5 个测试文件同步加 selectTab 前置。
85. **07-08 taxonomy-candidate-display-name**｜决策路径标签候选补 AI 中文建议名（收件箱命名卡预填中文而非 anxious 英文裸值）｜dimensionDisplayNames 四处透传：LLM 输出→RawAgentDecision→手写 Default→carry_through_fields——"**漏一处则字段被静默丢弃，同 namecard/assets 老坑**"。**键名规则硬要求**：外层 camelCase（rename_all）内层 **snake_case**（kind 空间来自 domain_signals 恒 snake_case，写错 get_str 落空）。**同源竞争路径必须同改**：decision_taxonomy fire-and-forget 与 gateway 主循环写同一幂等键 (scope,kind,raw) 且 upsert **先写者赢**——前者传 None 先落库会把 gateway 的中文名挡在门外。serde default 是 LLM 输出容错非兼容 shim（改必填会崩决策链）。不 bump PROMPT_PACK_VERSION（align 按内容 diff 生效——机制③）。**诚实标注真模型局限**："LLM 是否真按指令产合理中文名需真模型验证，本地只验证结构链路，不得声称已验证"。

86. **07-09 webhook-signature-verify-restore**｜Webhook 签名校验恢复方案 B（封死 :3003 公网无鉴权入口，WEBHOOK_VERIFY_SIGNATURE 从联调期 false 恢复 true）｜每账号明文 webhook_secret（gewe-agent slot 的 messageWebhookSecret）+x-webhook-timestamp 毫秒时效（±300s 防重放）+fail-closed（secret 未配置=拒绝）。签名内容 `"<ts>." + raw_body` HMAC-SHA256 hex，**golden 测试与 gewe-agent webhook-signing.ts 逐字节对齐（"方案 B 成败关键"）**。handler 重排：验签门下沉到"查到账号密钥后、任何副作用前"——testMsg 纯 ack 无副作用留门前，Offline/Online 写库移门后（fail-soft：状态落库失败不阻 ack 防 MCP 重推）。旧 x-mcp-signature+全局 MCP_API_KEY 方案整体退役（删 verify_hmac_sha256+7 测试）。**分阶段部署防断流**：合并→部署时仍 false→mongosh 写密钥→改 true 重启→验证矩阵（真实消息通/伪造 400/超窗 400），"错序会中断生产消息流"。手写 Debug impl 掩码 webhook_secret。spec §6 管理端 API+前端表单不在本计划（密钥走 mongosh）。

87. **07-09 roster-fetch-full**｜通讯录切 contacts_fetch_full（昵称/头像/性别富化）+4831 好友分页+懒加载｜就绪判据从"空 cache"改读权威 status 字段（**refreshing:true 是干扰项**——ready 时带全量数据也算就绪不参与判据）；**铁律"解析出任何好友一定就绪"**（否则前端无限重拉清空运营勾选）；旧形态无 status 回落空 cache 判据（向后兼容）。**sex 是客观事实字段（MCP 源）不进 profile_attributes（AI 推断空间）**——"忠于源"：int 0/1/2 原样存储，文字转换只在前端展示层；批量托管"带值才写"防覆盖已入库数据。Contact 加字段打挂约 30 处字面量构造点（无 derive Default）——E0063 全站补齐模式。头像键新增 bigHeadImgUrl/smallHeadImgUrl 放最前。

88. **07-09 roster-mcp-ratelimit-syncing**｜MCP 429/503 限流柔化为 syncing（不弹 internal_error 红条）｜新增 AppError::UpstreamBusy（503+upstream_busy）；classify_mcp_http_error 纯函数只柔化 429/503，**401/500 等真实错误照常上抛不掩盖**；fetch_roster 捕获 UpstreamBusy→last_result=Null→走既有 syncing 重试自愈。**SSH 部署纪律（踩坑记录）**：fail2ban 高频连触发须长退避约 10min、密码走 DEPLOY_PASS 环境变量不进 argv、别高频打 MCP 占 SSE 名额撞 429 影响线上发送。跑不动测试时退 cargo check+报告注明留 CI"不假绿"。
89. **07-09 webhook-gewe-addmsg-parse-fix**｜真实 GeWe AddMsg 嵌套 payload 解析修复（生产级 bug：发件人归错成账号自己→不触发回复；内容取到"吴界 : 你好"PushContent 通知串；非文本被误判 text）｜真实形态（线上亲验）：顶层大写驼峰+Data 嵌套+{string}/{low} 包裹——通用 find_string 递归被顶层 Wxid/PushContent 遮蔽。修复：gewe_data_string/gewe_data_msg_type_code 显式走 Data.*.string/low **优先 + find_string 回落**（扁平 payload 向后兼容）；命中空串返回 Some("") 刻意不回落 PushContent。**"回归留证"测试模式**：保留断言 find_string 依然取错值，防有人把提取改回纯 find_string。群消息（chatroom 发言人嵌 Content XML 前缀）明确 Phase 1 之外。含 obsidian 式 memory 引用（[[deploy_117_bundle_when_github_unreachable]]：117→GitHub 不可达用 git bundle 部署；[[project_webhook_sig_disabled_liaodiao]]：签名开关联调期临时关）。

90. **07-10 outbox-chat-search-idempotency**｜dispatcher timeout(150s) 兜底核对源升级：本地 mcp_call_logs→**优先 MCP chat_search（server 真实已发记录，同步落库 0.02s 可查、失败不写）**+本地日志 fallback——根治 timeout 取消 send future 致本地日志缺失误重发｜**核对精度铁律"宁重发不漏发"**：content_contains 服务端初筛+客户端 content **精确等于**（非子串，防历史相似内容）+createdAt>=entry.created_at（排除历史同内容）+ISO 解析失败保守不命中。核对带独立 15s 短超时（"核对本身绝不能卡死 dispatcher"）。MCP 新统一成功信封 {ok:true,submitted,newMsgId,...}：send_receipt_is_ok 纯函数 ok 优先/newMsgId 兼容旧信封——**仅可观测 warn 不改 .await? 成功语义**（保守不误拦已成功发送）。只做 text 主链路，media/referral 分支不动。

91. **07-10 passive-reply-daily-limit-and-ai-holding-reply**｜两修复：①daily_limit（每日触达上限）语义收窄为**仅约束 AI 主动触达（FollowUp）**，客户主动消息的被动回复（Inbound）豁免（与 quiet_hours 门 FollowUp-only 范式一致；防刷屏仍靠 min_reply_interval+账号级软上限）②过渡/占位回复从硬编码改 **AI 生成 generate_holding_reply**：三场景（GateHold 闸门占位/ChainTail 链尾失联/ExpiredAuthorization 授权过期）+独立小预算旁路（RUN_BUDGET.scope 新 RunBudget，1 次 LLM 上限，**主 run 预算耗尽也能生成**）+运行期出站守卫 holding_reply_text_is_safe（非空+复用 evolution lint 禁词表+授权场景复用 relay 数字护栏）+任何失败回落硬编码三文案（"**客户永不被晾死**"——硬编码是所有降级路径终点）。细节：**禁词字面量不能写在 src/agent/ 下（CI lint 扫新增行会自噬）**，须复用 evolution/lint.rs 词表。发送路径差异保留：A 类走 outbox、C 类裸 MCP 直发，只换文案来源。**注意**：本计划 Global Constraints 写"派子 agent 省略 model 参数（传 opus 会 400）"，与 CLAUDE.md"必须传 model:opus"直接矛盾（工具环境变化痕迹，见 §6）。

92. **07-10 roster-backend-snapshot-persist**｜通讯录 DB 持久化快照（根治"每次进通讯录长时间转圈"——4832 条大 body 传输慢且偶发 error decoding response body）｜roster_snapshots 集合每 (workspace,account) 恒一条覆盖写；快照优先秒回，>24h 后台 fire-and-forget 静默自刷（复用 decision_taxonomy 范式：失败仅 warn 不加锁不去重，重复 spawn 覆盖写无害）；**有快照时任何 MCP 失败都返回旧快照（syncing:false），绝不让页面转圈/空白**；后台任务独立更健壮重试循环（连 AppError::Http 解码失败也退避 3→6→12→24→48s 重试 5 次），区别于同步路径 3 次重试。

93. **07-10 roster-sex-parse-nonhuman**｜性别解析修复+非真人折叠+前端缓存｜sex 全显"未知"根因：**MCP 返回 sex 是 int64 序列化对象 {high,low,unsigned}**（07-09 的 as_i64() 对对象返回 None）——roster 线又一次"猜错形态→亲验修正"；修复兼容裸整数+对象取 .low。非真人判定：13 个微信保留系统账号白名单（fmessage/weixin/filehelper 等）+type=="system"；**公众号无可靠字段识别，不自动过滤**（宁保留当普通好友避免误伤真人，测试 public_account_not_misjudged 钉死）。前端 rosterCache 按 accountId 键控 store 化（进频道不重拉 force 才重拉；**syncing:true 结果不落缓存**防卡同步中态）；非真人默认折叠（分页只对真人）。

94. **07-10 roster-single-flight-refresh**｜roster 后台刷新 single-flight 去重｜根治自我限流循环："前端 8s force 轮询叠加 spawn 不去重→并发打爆 MCP SSE 并发上限→1.37MB 大 body 读取 TimedOut→快照永远写不进→前端无限卡同步中"。McpClient 加 roster_refreshing Arc<DashMap<account_id,()>>（与 sessions 同款模式），spawn 进入即抢锁（insert 返回 Some 则放弃），**RosterRefreshGuard RAII 保证 panic 也释放**（catch_unwind 测试钉死）；进程内锁不引入分布式锁（单副本足够）。端点无快照/force 分支不再同步阻塞拉全量，改立即 spawn+返回 syncing:true；前端自动轮询去 force、8s→10s，仅手动刷新 force。

95. **07-10 user-ops-pool-redesign**｜运营池计数与文案修正｜三 tab 计数改后端真实 count_documents（GET /contacts/counts，与 list_contacts 同源 filter，**不受 limit 截断**）；normal=all-managed 用 saturating_sub 防两次查询间隙并发下溢（AgentStatus 仅两态故无第三态遗漏）；**路由顺序陷阱：/contacts/counts 必须挂在 /contacts/:id 之前否则被通配吞掉**；refreshContacts 加 limit=500 防截断；tab 文案改"已互动/待启用"；删与通讯录重复的导入框+顺藤清理 importQuery 全链死代码。零业务逻辑改动。

96. **07-11 deep-logic-audit-batch-a/b/c/d/e（5 篇，深度逻辑审计线）**｜**审查工程非写功能**——deliverable 是带 file:line 证据的 findings 台账（`docs/superpowers/specs/2026-07-11-deep-logic-audit-findings.md`，全五批续写同一文件），"只入账不改 src"（发现≠修复）。批 A=自动回复命脉链 8 环节（webhook→去抖→gateway 闸→决策→review→outbox→MCP→回写，编号 A-NNN）；批 B=知识链 7 条业务链（grounding 召回最高优先——verified 口径三处协同核验：chat.rs 快照过滤+domain_profile product_fact+gates 硬门，编号 KB-NN）；批 C=成交活动链 4 环（圈人→触达→成交登记→成效聚合，触达环 dispatch 孤儿 send 最高优先，编号 KC-NN）；批 D=请示配置链 4 链（请示裁决+relay 出站守卫红线"客户永不知道有领导"最高优先，编号 KD-NN）；批 E=其余 5 块（evolution auto_release 红线"唯一 AI 自动写生产路径"双闸默认关+referral 受控例外，编号 KE-NN，收官全五批）。方法论：派 opus subagent 只读审+**主控逐条亲验 file:line 后才入账**（凭猜驳回）；验证状态三值 CONFIRMED（117 真跑）/PLAUSIBLE（读码）/BLOCKED（防假绿）；**严重度跨批一致性校准**（DB-fault/时序触发默认 Med，只有"推荐配置下确定性发生的核心交互/红线破坏"够 High，以批 D KD-04 为基准）；117 真跑硬约束（只对吴界+AI应用开发两联系人真发、绝不与套件并发、带方案 B 签名）。**这解释了 07-12 起 kb/kc/kd/ke 系列修复计划的编号来源**。批 D 特别提醒："改 prompt 不必 bump PROMPT_PACK_VERSION（生效闸是内容 diff）——审查时不要把没 bump 当 bug"。

97. **07-11 friend-picker-modal**｜好友选择器弹窗（换手填 wxid 痛点）｜纯前端 UI-only 受控组件，复用现有 Overlay（scrim/focus-trap/Esc/portal），统一 FriendPickerItem 形态由调用方 map（roster 全好友源/运营池源两处接入）；单选无 multi；决策链 DeciderChainEditor 不改（多选累加交互不同）。后端零改动。

98. **07-11 full-system-test-remediation**｜全量系统测试 P0+P1 修复（对应另一台账 `docs/superpowers/specs/2026-07-10-full-system-test-findings.md`，F-NNN 编号）｜F-005+F-020 账号错配家族：前端三处联系人选择器不带 accountId→后端回落 default→账号 102 下查无联系人；campaign 创建 account_id 硬编码 default 治本改前端传+后端加字段。F-013 知识库概览卡顿：completeness 端点**零缓存且内含 5×count+2×find+1 次阻塞 LLM 调用**→进程内 DashMap TTL 缓存+前端局部骨架不阻塞整屏。F-003 任务日志内部作业泄漏：list_tasks 加 kind 白名单。**架构论述"组合过滤即隔离"**：filter 同时插 workspace_id+account_id，不显式校验账号归属（不属于则组合查询自然返回空，**加校验是过度设计、偏离现有模式**）。AgentTask.kind 实际取值 6 个（follow_up/deferred_inbound_reply/principal_decision_relay/memory_consolidation/outcome_aggregation/initial_profile）；status 闭集 8 值。

99. **07-11 taxonomy-candidates-batch-filter**｜F-007 标签候选批量驳回+kind 服务端筛选｜**批量只做驳回不做批量采纳**（采纳需逐条人工填 canonicalValue——字典质量红线）；零后端改动（前端循环调单条 reject 端点）；批量驳回只在 pending 视图启用；useConfirm 弹窗 body 是只读 ReactNode 不能内嵌输入框→驳回原因用组件内 inline 输入框。

100. **07-11 user-ops-pool-display-fixes**｜运营池三显示问题修复（Playwright 复现：tab 挤畸形椭圆/消息预览吐原始 XML/系统号混入池）+媒体号手动移除｜**hidden_from_pool 走 doc-only 标记**（不进 Contact struct，Mongo filter+$set 层面处理——已亲验 contacts 无 replace_one 全 struct 写路径且 Contact 无 deny_unknown_fields 故安全，**避免 160 处 Contact{} 构造点编译改动**）；`$ne:true` 兼容缺字段旧文档不新增 migration。preview_label_for_type：非 text 类型绝不读 content（可能是 XML 垃圾）出静态标签 [图片]/[链接]；**产品红线"normal 联系人绝不调 LLM"**（预览是静态标签非智能摘要）。is_system_account 建档+list 读双拦。YAGNI：单向移除不做恢复端点、不自动判媒体号（无可靠信号）。

101. **07-11 user-ops-pool-real-contacts-redesign**｜运营池"真人漏斗"重设计（修"昵称全是 Demi、无头像、混入公众号/群"）｜**"Demi"根因：真实 GeWe payload 发件人只有 wxid，find_string 递归误命中 _mcp.nickName（账号自己的昵称）**——昵称/头像唯一正确来源改为 roster_snapshots 富化（禁再 find_string 取 nickName）。真人判据黑名单纯函数 is_operatable_person（gh_ 前缀公众号/@chatroom 群→false；117 亲验 roster 好友 gh_=0 群=0）——webhook 建档与 m029 migration 共用杜绝漂移；非真人消息仍落库但不建 contact 不触发流水线（Ok(None) 优雅短路而非 Err）。**migration 安全红线**：只碰 nickname/avatar_url/删非真人 normal 行；绝不动 agent_status/operation_state/agent_profile/memory；managed 一律保留只清昵称；**绝不带 APP_ENV=production 守卫（必须无条件对所有环境存量生效）**；不删 conversation_messages。$set 只在 roster 命中才写（不覆盖已有）。

102. **07-12 kb01-lean-tier-clear-used-knowledge-ids**｜KB-01 修复（审计台账首个修复）：非 Full 档清空 used_knowledge_ids——堵"LLM 自报真实 verified ObjectId 架空 blocked_unverified_product_claim 硬闸"缺口｜根因：gateway:1457 只有 if 无 else（Full 档覆盖写、非 Full 档不动**透传自报值**）；修复：resolve_used_knowledge_ids 纯函数无条件赋值（非 Full 返空 Vec）；端到端不变量测试：自报 verified id→resolve 清空→compute_verified_chunks 交集空→硬闸不被架空。严格限定范围（不改 carry_through/rewrite/revision 站点——均 Full 档合法）。

103. **07-12 kb08-audit-inbox-blackhole**｜KB-08 修复：auto_verify 分诊出的 needs_human_audit 切片从人审收件箱消失（黑洞——两收件箱查询只认 needs_review）｜knowledge_review_statuses() 纯函数返 [needs_review, needs_human_audit]，**列表查询与 summary 计数共用防漂移**；InboxItem 加 integrityStatus 供前端区分"AI 预审通过·待复核"（held 琥珀色 pill，**不新造颜色**只用现有 6 tones）；写端 verify.rs 分诊逻辑不动（每个 needs_human_audit 必有 quote+anchor 的红线未破）。

104. **07-12 kc-family1-dispatch-atomicity**｜KC-01/02/03 修复：campaign dispatch"占去重位→建 task→回填 taskId"三步中途 Err 补偿回滚（失败即删已建 send/task，全有或全无；**补偿删除用 let _ best-effort**——补偿再失败属极窄双重故障超出 finding 范围）+status 前置门纯函数（draft/previewed/dispatching 放行；**completed 拒防重推；dispatching 允许重入恢复**——配合去重跳过已完成 send；未知态 fail-safe 拒）。send 先占位写序不动（unique 去重闸）。零 schema 改动。

105. **07-12 kc-family2-segment-coverage**｜KC-05 修复：带 product_ids 圈人静默漏掉缺 verification/eventKind 字段的老成交客户（破坏"**粗筛 ⊇ 精筛**"不变量）｜两防线：A=粗筛 $elemMatch 改"缺字段=默认值"显式 $or 表达（即时生效不依赖迁移）；B=m030 用 $map+$mergeObjects 回填数组元素缺失字段默认值（治本、非破坏幂等、**不加 APP_ENV 守卫——语义保持型回填同 m018/m022/m025 惯例，误加会致 117 生产静默 SKIP**）。不动精筛（口径正确是对齐基准）。存储键混合形态亲验：Contact 顶层 snake_case（无 rename_all）+内层 OutcomeEvent camelCase。

106. **07-12 kd-relay-number-guard**｜KD-01+KD-03 修复：**删除守卫**（罕见方向）——relay 出站的字符级数字护栏 fail-closed 用法整体移除｜数字护栏威胁模型错误：**既漏中文数字（"八折"检测不到）又误杀无害的时间/序数/等价折扣数字，fail-closed 曾致裁决黑洞（KD-03：领导已裁决但转述被拦，客户永远收不到）**。转述忠实度交还生成侧 prompt（substance 唯一事实源）+独立 Review Agent 语义级把关；载荷泄漏守卫（确定性字符串检测，威胁模型正确）保留 fail-closed。logic.rs 三数字函数不删（holding_reply.rs 仍是合法调用者）。**YAGNI 拒绝清单**：sent 回传接口/KD-03 补偿话术/中文数字归一化/review 增补维度（spec 已论证全部不做）。

107. **07-12 kd-family2-escalation-channel**｜KD-05+KD-06 修复，**KD-02 经用户裁决不写任何代码**（字符级领导泄漏词表同数字护栏是威胁模型错误 backstop，忠实度交 prompt+Review）｜KD-05：骚扰门统计口径漂移——改派换 principal 时 created_at 不刷新会低估对 next 的打扰，加 last_pushed_at_ms（首推=created_at、每次 reassign 刷新）+m031 backfill（不加 production 守卫惯例第 5 例）；KD-06：改链孤儿 pending 永不改派——next_decider_on_timeout 在 current 不在链中时从静默退化链尾改**回落链首**。**加字段须 cargo check --tests**（--lib 不编译 tests/ 漏改测试构造点的 E0063 只有 --tests 或 CI 暴露）。

108. **07-12 kd04-principal-reply-decider-chain-fix**｜KD-04 修复（**批 D 唯一 High**）：领导用 decider_chain（推荐配置）时微信回复不被识别为裁决——lookup_principal_config 只 find_one 查旧 principal_decider 标量字段，链上决策人查不到｜抽 is_decider_for_config 纯谓词复用权威解析器 resolve_ask_human_policy（已内含旧字段回落——decider_chain 优先，None 时 principal_decider 转单元素链）；重写 DB 壳为遍历 current_version 配置调谓词；返回类型不变（唯一调用者只用 .is_some()）。

109. **07-12 ke-family1-auto-release-direction-consistency**｜KE-01+KE-02 修复（evolution auto_release）｜KE-01：放量方向一致性——decide_auto_release 加候选方向门（**升阈候选仅命中率仍过高时放行、降阈候选仅仍过低时放行**，防命中率跨 band 翻转时反向放量），旧逻辑的安全收窄；KE-02：threshold 重判 original 终态从"源 run 真实终态"改"基于 original_5gate_hit 的 5 闸重推"（与 prompt 路径对齐，避免非-5gate 终态虚假抬高 send_delta）。不动双闸/负反应门/#152 反向安全门。

110. **07-12 kb-family1-edit-audit-unification**｜KB-09/10/11 修复：知识编辑两条绕过路径（AI 会话 apply_update_chunk 直接 $set、admin PUT replace_one）没走 apply_chunk_revision"唯一编辑落库入口"，无 chunk_revisions 审计、locked_fields 不被强制｜组件1 底座：existing.locked_fields 并入 enforce_locked_fields 锁集——**只进静默覆盖不进 apply_field_patch 硬拒集（避免锁定字段连坐毙整条编辑）**；组件2：apply_update_chunk 改走 RevisionRequest{op:Patch,source:Ai}（**source=Ai 自动强制 draft+needs_review，"AI 永不自动 verify"不破**）；组件3：admin PUT 保留 replace_one 前补锁强制后补审计行，**复用同一 enforce_locked_fields 绝不各写一份（修复本身不能再造旁路/dual-path drift）**。亲验陷阱：source_anchors（复数）≠ DEFAULT 锁的 source_anchor（单数）。

111. **07-12 audit-events-failsoft-alignment**｜B-02+C-01+H-01 修复：回复入队前的纯审计/旁路写在 DB 瞬时故障吞掉本轮客户回复｜apply_agent_updates 内 5 处纯审计 write_event .await? 降级 `let _ =`（与**同函数孪生 fail-soft 铁证**对齐——dimension_dropped/stage_transition_rejected 已用 let _、注释明写"审计写失败不阻断主流程"）；webhooks 步骤 (e) 网关聚合回复从步骤 (d) reaction 的 else 分支解耦**无条件执行**（reaction 分析失败不该吞回复）。测试创新：**用 MongoDB collection validator 注入确定性审计写失败**，断言 outbox 仍入队（不变量编码进 e2e）。

112. **07-12 async-import-job-progress**｜异步 import job+进度轮询（长文本导入 ~9min 墙钟从同步阻塞改 job 队列）｜大小分流：content≤单次上限同步秒回（零回归无 jobId）/超则建 ImportJob 返 {jobId,async:true}；worker 仿 tasks.rs 全套（claim find_one_and_update+心跳 timeout/3 bump+孤儿 reclaim，**claim_recovery_count≥3 直接 failed**）；**进度粒度决策**：run_import_extraction 加可选进度回调参数（同步传 None/worker 传闭包），分块合并逻辑一字不动只在段完成点插回调；跨会话恢复用 GET running jobs 接管轮询（**不用 localStorage**）；status 闭集 4 值；IDOR workspace 收口。无 enable flag（常开对齐 tasks.rs）。含 obsidian 引用 [[config-field-add-test-helpers]]/[[project-prod-llm-provider-db-not-env]]（**生产 LLM provider 配置在 DB 不在 env**——重要事实）。

113. **07-13 ingest-source-serde-camelcase-fix**｜外部源列表 serde 大小写修复（前端读不到 sourceId/scheduleMinutes→删除/重激活按钮失效+间隔列"undefinedm"）｜方向决策：**禁止给存储结构体加 rename_all**（IngestSource 同时是 bson 存储类型——会连带改坏存储键/worker 查询/两条索引/全部存量文档），只在 API 输出边界抽 ingest_source_json 投影函数（仿 operation_knowledge_chunk_json）。**测试路线决定**：不引入契约 fixture（33 个 fixture 无 ingest_source、历来不在契约体系内，"对一个字段映射 bug 属过度基建"）用纯手写 assert_eq!。

114. **07-13 p3-family1-dead-code-cleanup**｜H-02+F-02+KB-05 低风险清理｜**H-02 重要事实：run_envelope R0 三函数（write_run_envelope_started/update_run_envelope_terminal/install_panic_hook_for_envelope）是"已设计+已测+未接线"的安全基建**——只改模块头 doc 从将来时改"未接线/推迟"标注，**删函数=毁掉规划的安全特性明确禁止**；F-02：dispatcher max_attempts <=0→3 兜底与 enqueue 对齐；KB-05：删 propose_pack_repair 死桩+摘路由（同区 chunk 级 record_repair_apply 前端真在用是活的绝不碰）。

115. **07-13 p3-family2-serde-key-tolerance**｜D-01+KB-03 一致性加固｜D-01：RawAgentDecision customer_stage/intent_level 加 snake_case alias（LLM 键形漂移吸收为 Some 而非静默 miss→None；**只加 alias 不改 rename_all；只给台账点名的两字段 YAGNI**；已亲验无误吸风险——顶层 alias 抓不到嵌套 Document 内同名键）；KB-03：is_verified 从 eq_ignore_ascii_case **收窄**精确小写 ==（写入侧恒写小写，等价收窄消除 latent 口径漂移，与写入/召回侧对齐）。

116. **07-13 p3-family3-referral-hardening**｜KE-03+KE-05 名片子系统加固（referral 红线敏感，绝不改主流程语义）｜KE-03：account 归属校验加进权威内存判据 validate_card_sendable（enabled/approved 同层，三处发送路径全经过→**单一事实源**；不改 DB filter——分层一致；global 卡 account_id=None=任何 account 可用与 $or 口径一致）；KE-05：toggle/delete 补 fail-soft 审计（写失败仅 warn 绝不影响主操作；**保留硬删不做软删——软删涉全链查询加过滤超范围**；delete 先查后删留痕）。

117. **07-13 p3-family4-campaign-scale**｜KC-04/07+KC-06 campaign 规模就绪债｜受众规模硬上限（CAMPAIGN_MAX_AUDIENCE 默认 500）：**.limit(max+1) 探测法**在 Mongo 层截断扫描量防 OOM+循环内粗筛计数守卫超限 400 防静默截断——**上限语义=粗筛扫描量非精筛后受众数**；KC-06：last_dispatch_target_count dispatch 完成回刷消除三义计数（targetCount preview 存/dispatchedCount/实际命中）误导。**config 加字段 E0063 铁律：恰 4 处全字段构造点，tests/common/mod.rs test_config 是 --lib 不编译的盲区最易漏**。

118. **07-13 reply-prompt-slimming-batch1**｜reply prompt 瘦身批次 1（零信息损失零行为改变削 token）｜删 4 死字段 schema 声明（intentAnalysis 6 行块/productFitScore/forbiddenClaimRisk/recommendedResourceIds——全库 grep 无任何 guard/阈值/发送逻辑消费，仅 carry_through 透传 None）——**struct 字段保留不动（删字段要动 carry_through/AgentDecision，风险大于收益；删模板声明=LM 不再输出=零行为改变）**；删槽5 runtime 硬参数注入+槽7 context_pack 二次注入（已完整嵌在槽6 memoryCard 内）；槽11 整对象序列化换 format_knowledge_route_for_prompt 裁剪（tool_trace/evidence_excerpts/selected_chunk_rankings 纯调试落库元数据裁掉，10 语义字段保留，**必须保持 camelCase 不偷改 LLM 看到的字段名**）。位置参数 format! 删槽三处配对铁律（标签行+{}+实参）。已亲验无整串 user prompt 黄金快照测试（等价护栏约束 tier/profile 差异非"prompt 永不变"）。

119. **07-13 revision-fallback-to-approved-draft**｜改写失败回退原稿：single-shot revision 三失败分支（LLM 错误/30s 超时/改写后二轮 review 未过）统一回退发送**改写前那份已 Approved 的原稿**而非丢弃补兜底占位（原稿本已过审——revision 针对 rewrite-worthy 弱项是改良非阻断）｜apply_revision_fallback 纯函数统一状态赋值（approved=true+final_review_status=revision_applied_approved+finalize=Approved）；改写调用前克隆原稿快照，三分支共用恢复；纯函数不碰 final_decision（gateway 局部变量由调用方恢复）。不触硬闸/不改 derive_revision_failure/不新增终态枚举。

120. **07-13 wiki-channel-full-playwright-verification**｜wiki 频道 21 视图 Playwright headed 真实验证（生产 117）｜验证工程非改码（发现 bug 只记录另开修复流程）；**副作用递增分层**：T1 纯只读→危险操作只点到确认弹窗断言出现**绝不点确认**（删文档/Schema 激活/治理 rollout/回滚）→T2 一次性测试数据写（[E2E验证] 前缀+自动清理+查库确认回 95 chunks/1 doc 纯净态）；生产 LLM 端点仅 2 线程涉 LLM 操作串行。导航真值与业务边界全部 file:line 亲验（对话工作台 chat.rs:989 隔离不发客户）。

121. **07-14 agent-capabilities-audit + auth-routes-security-audit + worker-fleet-audit（3 篇，审查工程第一/二/三批）**｜延续 07-11 五批方法论但**编排进化**：不适用 SDD 双裁决，改主控直接编排（第二/三批更进一步：TaskS 根因层先审并等回→结论作基准喂并行簇）。各产出独立台账（`2026-07-14-agent-capabilities-audit-findings.md`/`auth-routes-security-audit-findings.md`/`worker-fleet-audit-findings.md`）。第一批=agent 旁挂能力 4 簇 ~10.6k 行（memory/consolidation_window 等）；第二批=auth+routes 安全隔离面（元家族："middleware 保证认证不保证授权，授权靠每个 handler 自觉锁 workspace——找自觉漏掉处"）；第三批=后台 worker 群（supervisor/tasks/import/cold_contact/account_scheduler/silence_signal/behavior_signals，claim/幂等/崩溃恢复）。**关键校准口径**："单租户默认部署下不可达的隔离缺陷=多租户就绪债不夸大 High"；"单进程默认部署（117 单机 systemd）下不可达的多副本竞态=水平扩展就绪债"。**证实 model 参数矛盾的解释**：'model:"opus" 报 400 INVALID_MODEL_ID，省略即继承 opus'——harness 行为变化，CLAUDE.md 的"必须传 opus"已不可执行（见 §6）。

122. **07-14 f01-reclaim-chat-search**｜F-01 修复：reclaim 崩溃恢复分支防重发核对**不对称**（timeout 分支已有 chat_search 权威核对+本地回落，reclaim 分支只查本地 mcp_call_logs）｜抽共用 verify_already_sent（referral Ok(false)/media/text 三分派，text=chat_search 带 15s 超时→回落本地），reclaim/timeout 两块各替换为一行调用，**timeout 路逐字保留行为不变**；依赖 AppState 无法 lib 测→集成哨兵测（说明写进 commit message 防 reviewer 误判缺测）；顺带翻正 6 条台账状态。

123. **07-14 knowledge-agent-autonomy-redline**｜知识 Agent 反接管红线治本批次 1（知识层也须"无人工接管"：知识 Agent 检索到转接 SOP 类知识后其自然语言 reason 回流 Reply/Review prompt，引导 AI 说转接话术）｜两层纵深：Layer 2 先做（纯函数 TDD）——format_knowledge_route_for_prompt 额外剔除 reason 字段，**Review 侧从裸 serde_json::to_string 改复用同一净化函数（DRY）**；Layer 1——知识 Agent 内联 SYSTEM_PROMPT 追加反接管角色约束（AI 自治合规表述）；Layer 3 影子模拟泛化验证。**反过拟合最高红线：绝不加关键词黑名单/绝不对 reason 做关键词 replace**（点对点补丁），只沉淀可复现抽象方法论。**基线数字证据：cargo test --lib 此时已 1974 passed**（从早期 350 基线大幅增长，check-baseline 的 LIB_BASELINE 应同步演进）。YAGNI 留后续：不改知识数据/不动 /api/knowledge/ask。

124. **07-14 principal-authorization-exemption**｜领导授权豁免（让领导裁决 approved 的产品说法真正发得出去）｜A 类客户级：Contact.domain_attributes 挂 PRINCIPAL_PRODUCT_EXEMPTION_ATTR，作 R5.4 产品门**第三条并联旁路**（仿 priced_from_catalog），长期常驻+撤销端点；B 类沉淀全体可复用 verified 知识：**新 ProvenanceSource::PrincipalAuthorized 归"人类权威"家族视同 Human 待遇**（不被 source=Ai 强制降级影响——**红线定性：验证者是领导真人非 AI，"AI 永不自动验证"本质未破**）；两步法=建 draft chunk（substance 自锚过 D2 硬门）→apply_chunk_revision(op=Verify) 落 verified。exemption_type 闭集三值（none/customer_only/knowledge）。PrincipalDecision 故意 snake_case 持久化。**CI lint 家族扩展证据：check-no-model-hint（禁硬编码模型名）+check-skip-ledger 已存在**。

125. **07-14 manual-ops-enrollment-source-of-truth**｜修"AI 给非真人号自动回复"：手动加入运营为唯一真相｜**放弃 wxid_ 营销号自动判据**（无可靠信号，交管理员手动决定）；补判据硬盲区：@openim（企业微信/开放 IM）+is_self_account 自反身（账号自己给自己回复）；**判据双侧同源铁律**：is_operatable_person（Rust webhooks 侧）与 non_human_exclusion_filter（DB mcp 侧）任一处补拦截另一处必须同步否则 count/list 口径漂移；移出池在**写入侧联动** agent_status=normal（不在回复门 reload 加过滤——单一真相源）；四个升/降 managed 入口统一自反身硬拦+审计（MCP 工具分支无 admin 用固定 actor "management_tool"）；存量清理走 mongosh 一次性脚本**不入 migration**。

126. **07-14 p3-family5/6/7/8/9（5 篇，P3 修复家族后半）**｜⑤KB-07 在线 gap dedup 改全量精确命中（**绝不抽共用函数合并在线/离线——两路径合并语义不同，强行合并破坏差异**）+KB-06/12 纯 doc 标注（structural_proposals"生产未接线"）；亲验 **check-no-human-takeover 只扫 src/agent|routes|evolution|frontend/src，src/knowledge_wiki/ 不在范围**（但注释仍用中性词保持定位）。⑥KD-10 activate_provider 先 swap 后写 DB（swap 无 DB 副作用亲验，消 DB↔运行时假失败）+KD-09 openai 形态 base_url 缺 /v1 软 warning（**不阻断——hard block 会误伤 Azure/代理合法非 /v1 端点**；anthropic 形态不校验；warning=None 不出现键防 null 误导）+KE-06 sync_accounts mcp_base_url 移 $setOnInsert（与 mcp_api_key 对称保护手配值）。⑦KD-07 超时改派跳过链中误配成本请示客户 wxid 的成员（防内部请示卡直推客户；**首推守卫不动、不加写入口校验——用户裁决**；保留 KD-06 语义一字不变）。⑧A-06 统计 update 降 best-effort（防 Mongo 瞬时错吞回复）+A-05 无 appId 回落收敛（verify=false 且多账号→400 防张冠李戴，count 放 verify 分支内不白付代价）+**A-03/A-04 标 WontFix**（生产不触发/已幂等缓解）。⑨F-04 reclaim_count>5 转 failed_terminal（独立计数不复用 max_attempts——reclaim≠发送 attempt；两个 update 因单 update 无法基于 inc 后值分流）+B-03 second_safety_gate 加 is_managed fresh 复核（决策期翻 normal 拦截，"not_managed_at_send" 是 AI 内部状态名）+**B-01 抢占尾窗双回复标注已知产品取舍不改逻辑**。⑨还揭示**设计文档也会有错**：设计写 pure 函数在 dispatcher.rs 实际在 outbox.rs（主控亲验修正）。全家族统一模式：设计文档已获批 commit 号+行号亲验于具体分支+LNK1318 Windows PDB 已知环境错。

127. **07-15 pr-a-memory-consolidation**｜A-01/02/03 记忆层三 Medium 修复｜A-02：confirmed_tags 从 replace（丢未重判旧标签）改 merge 语义（reconfirmed 保留+旧未弃用保留+LLM 显式 discardedTags 移除）；A-01：core_facts 证据门（importance 天花板判定）；A-03：跨集合写非原子→fail-soft。**三线隔离铁律：consolidation 落库只碰 confirmed_tags/personality_profile，绝不碰 manual_tags（运营权威层）/bayesian_signals**。**DEFAULT 销售域字节等价约束**（改动仅非默认/异常场景生效）。PR#217 教训：提交前必跑 RUSTFLAGS="-D warnings" cargo check --tests 兜 must_use。

128. **07-15 pr-c-generalization + pr-e-post-release-mapping（2 篇）**｜PR-C：C-01 stagnation 计时写侧写死 customer_stage→按 active profile 的 stagnation_dimension 动态检测该维度自身变化写 {dim}_updated_at；C-02 初始画像半接线（非销售域维度不采集）→比照 live reply 追加两维度指引；**DEFAULT 销售域字节等价**（DEFAULT="customer_stage"、guidance 返空串）。PR-E：[2-01] post_release 观测面板 FIVE_GATE_KEYS 把 fact_risk/pressure_risk 的 status **映射对调**（与 significance/threshold 两权威源相反——三份映射对照表亲验；生产真相：fact_risk 硬闸→held_by_ai_policy，blocked_by_safety_guard 来自产品声明 fail-closed+relay）；pressure_risk 是软闸不产 block 终态→命中率改 revision_failed 口径；纯观测不反哺自动决策；一致性单测钉死与权威源对齐防再漂。**新 lint 证据：check-evolution-isolation（evolution 不得引 outbox/mcp）**。

129. **07-15 sediment-title-from-substance + import-failed-segment-visibility（2 篇）**｜前者：领导授权沉淀知识的 title 误用 entry.reason（卡点原因/reviewer 质检黑话）而非 decision.substance（真正知识内容）——derive_sediment_title_fallback 确定性纯函数（首句+限长）+LLM 提炼叠加（escalation.sediment.title 走唯一 JSON 入口），**"沉淀永不因 title 提炼失败而失败"**（已亲验 generate_agent_json 纯 JSON 无 tool call 不抛 BudgetExceeded）；生产已污染存量走一次性 mongosh（备份→修正→回读，非 migration）。后者：M1 导入失败段前端警示（后端数据链已就绪零后端改动）+M4 chunk_haystack 补 product_tags 参与召回打分（品牌别名不在 title/body 只在 tags 时修复前召不回）。**基线门三 lint 齐名确认：check-no-human-takeover/check-no-model-hint/check-evolution-isolation**。

130. **07-15 knowledge-wiki-audit + evolution-audit + db-migrations-indexes-audit（3 篇，审查工程第四/五/六批）**｜第四批=knowledge_wiki 11 文件 5272 行（写入强约束/反馈闭环/编辑历史/信号消解；元家族：union 不丢 tag/锁定字段守门/AI 永不自动 verify/双写一致性）；第五批=evolution 14 文件 6095 行（演化安全不变量：绝不自动回滚只 admin 手工/release 需 admin 二次确认/EvolutionBudget 硬上限/隔离红线禁引 gateway·outbox·mcp/shadow eval 不碰真实发送）；第六批=db/migrations+indexes ~5980 行（**最后一个未审领域**，全六批领域覆盖收官）。方法论演进注意点：subagent 用 **general-purpose 非 Explore（"Explore 读摘录漏 read window 不适合审计"）**；**第六批因 subagent 派发连续 API mid-response 失败改主控亲审**。第六批核心判据：**APP_ENV 守卫家族命门**——m011/m012/m014 等 drop 类迁移若生产未设 APP_ENV=production 非 prod 分支会删数据，但已入账迁移永不重跑，须区分"已入账（历史安全）vs 首次可达"不凭空判 High（与 m030/m031 语义保持型回填"不加守卫"是两类迁移语义）。含 obsidian 引用 [[prod-app-env-guard-migrations-risk]]/[[project-config-seed-in-prompts-not-migrations]]（种子源在 prompts.rs 非 migrations——防漂移检查项）。

131. **08-05 run-log-stage-table-overflow + wiki-admin-governance-css-baseline（2 篇，注意 07-15→08-05 三周空档）**｜前者：运行日志阶段表格溢出根因是**类名误用非 CSS 写错**（.tHead flex 为事件时间线设计被复用到阶段区块，flex 项默认 min-width:auto 不肯收缩）——新建 .stageBlock/.stageTable 不动 .tHead+全局守卫 .main{min-width:0}；**jsdom 无布局引擎：可自动化的只有结构断言，CSS 属性值靠文件断言+部署后目视**。前端基线此时 618 tests/125 files。后者：治理工坊五缺陷含**"发布给全部"白底白字功能缺陷（不可逆高危按钮不可见）**；**重要惯例：Knowledge.css 必须保持 plain .css——改 .module.css 副作用导入会被 Rollup tree-shake 删光整频道白板**（文件头注释警告）。
132. **08-06 ask-human-inbox-layout + decider-chain-roster-picker（2 篇，plans 收官）**｜前者：统一收件箱白卡结构对齐（.page+白卡、chip 单排、共享 EmptyState）；AskHuman.css 同为 plain .css 全局作用域，**新增 class 必须带 askHuman 前缀**（styles.css 已有全局 .panel 被 user-ops 消费，同名污染）；lint 严到测试名/注释不得出现"人工"二字（用"目视确认"）。后者：决策人链改通讯录选择器（复用 FriendPickerModal），not_imported 好友选中先调 POST /contacts/import 落库（agent_status=normal 不托管）再入链——**因 put_ask_human_policy fail-closed 要求决策人已在 contacts 表**；**维护陷阱提示：roster isNonHuman 判据比后端 import 守卫宽松不等价**，前端只补三条结构性规则（gh_/@chatroom/@openim）**不要复制 13 条白名单防两份清单漂移**；**lint 门禁陷阱：必须从仓库根执行，从 frontend/ 跑会因 pathspec 相对 cwd 解析假通过**（exit 0）。两篇均记录"工作树有他人 21 项未提交改动禁 git add -A"——多会话并行工作常态化证据。

<!-- END-S1 -->

## 2. plans 中的高价值实现决策档案（spec 未载、只在 plan 里的关键决策）

> 收录标准：该决策影响代码结构/数据模型/守卫行为/运维语义，且对应 spec（或 docs/）未记载、仅在 plan 正文中出现。每条附出处（plan 文件名+小节）。

- **D-001 召回排序无物化索引**：`list_catalog` 的 `rank_key = relevance × trust × recency` 为查询时实时计算，"重建原子索引已删除，再召回=对更新集合重跑"；`superseded_by` 非空 → trust×0.1 降权（旧版不物理删除只降权）。出处：06-02 closed-loop plan「已核实的签名与事实」+ Task 5。
- **D-002 verify 硬门的字段前置条件**：verify 前必须 `source_quote` 非空 ∧ `source_anchors` 非空，否则 BadRequest（计划引 knowledge.rs:589，当时行号）。出处：06-02 plan 签名段。
- **D-003 前端 6 色语义 token 的具体色值与"唯一呼吸"约束**：running #30D158 / scheduled #0A84FF / held #FF9F0A / blocked #FF453A / brand #5E5CE6（不表达状态）/ inactive #8E8E93；呼吸动画（breathe-running）仅允许"进行中"语义使用。出处：06-04 plan Task 0.1。
- **D-004 Zustand 引入策略**：固定精确版本 5.0.14（--save-exact，"审计过的依赖"政策：MIT、零依赖）；不上 React Router、不引 UI 组件库，保留 state 导航。出处：06-04 plan Tech Stack + Task 0.3。
- **D-005 前端 localStorage key**：账号选择持久化 key 为 `wechatagent.accountId`。出处：06-04 plan Task 2.3。

- **D-006 请示卡不走 outbox**：推给领导的请示卡直接 `mcp::logged_call_for_account("message_send_text")`，绕过 outbox——理由写明"这是给领导看的内部通知，不需要为客户设计的冷却/幂等/拟人评分"。出处：06-05 plan Task 16 Step 1。
- **D-007 trigger 失败降级不阻断**：推卡/落台账失败只记 warn 日志、不让 run 失败（占位已发给客户，若 run 标失败会造成"客户收到占位但系统记失败"的不一致）；补偿靠"同类 pending 不存在则下轮再试"。出处：06-05 plan Task 16 Step 3 执行注意。
- **D-008 短码方案**：`E` 前缀 + 4 位 base32（字符集剔除 0/O/1/I/L 易混字符），种子=毫秒时间+尝试次数×2654435761，短码唯一索引兜底、碰撞重试至多 5 次。出处：06-05 plan Task 6/11。
- **D-009 verdict 闭集与保守回落**：领导裁决 verdict 闭集 5 值 {approved, rejected, conditional, deferred, delegated_back}；LLM 解读失败或 verdict 越界一律回落 `deferred`（"宁可当领导还没定也不乱转述"）；deferred 保持 pending 不起 relay。出处：06-05 plan Task 14/19。
- **D-010 等待态承载方式**：「等待领导决策」不是 hold category，而是 contact `domain_attributes` 上的布尔标记 `awaiting_principal_decision`（key 常量 + 锁值单测防 set/unset 漂移）。出处：06-05 plan Task 4/18。
- **D-011 relay 合成消息不落库**：`synthetic_principal_relay` 构造的哨兵消息仅内存中作为 trigger 传给网关，**不写 conversation_messages**（防污染客户会话历史）。出处：06-05 plan Task 16 Step 2 执行注意。
- **D-012 请示沉淀知识提案的归属**：知识缺口提案 `account_id=None`（workspace 共享域，与 chat 补库修复 commit 0c78c13 口径一致），仅 verdict∈{approved,conditional} 且 `is_generalizable` 且未发过时发一次（`knowledge_proposal_emitted` 防重）。出处：06-05 plan Task 12/16。
- **D-013 禁词红线测试的落点纪律**：断言"文案不含禁词"的测试字面量必须含禁词，故只能放 `tests/`（lint 排除目录），绝不能放 `src/` 内联 mod tests——"放在 src/ 会被 lint 误杀"。出处：06-05 plan Task 8 lint 陷阱。
- **D-014 relay task 载荷约定**：`principal_decision_relay` task 的 `content` 字段=台账短码（relay 端据短码取台账）；task 由 tasks.rs else 分支流到 handle_follow_up_task 再按 kind 分流。出处：06-05 plan Task 17/19。

- **D-015 D2 人工后门收口语义**：create/PUT 提交 `integrity_status="verified"` 但缺 sourceQuote/source_anchors → 不报错而是**静默降级** needs_review，并往 `distortion_risks` push 一条含"D2"的审计文案；非 verified 提交不动。出处：06-06 plan Task B1。
- **D-016 grounding 兜底硬闸的词表分级**：reviewer 漏判（requiresProductKnowledge=false）时，回复含效果词且无 verified 背书 → 强制 block（事件 `product_claim_blocked_by_probe_fallback`/blocked，final_review_status=blocked_unverified_product_claim，hallucination_score 抬到 ≥6）；仅语气词 → 只发 `grounding_probe_reviewer_missed`/observe 事件不拦。两类同现取更危险的 ProductEffect。出处：06-06 plan Task A1/A2。
- **D-017 prompt 种子"存在即跳过"运维语义**：reviewer prompt 增补反向锚点后，"该 prompt seed 机制为存在即跳过，老库不变，CI 全新库自动生效（与 commit 5a78e94 第③件套同机制）"——即改 prompts.rs 种子文本对已初始化的库不生效，需 reset-system-pack 或新库。出处：06-06 plan Task A3 Step 2 注。
- **D-018 routes 拆分的 facade 模式与 orphan 守卫**：knowledge 路由拆分采用"私有 mod + mod.rs pub use 汇总"的 facade；`routes/mod.rs` 存在一个 orphan 检测单测 `no_orphan_pub_async_route_handlers`，靠 `include_str!` 逐文件扫源码找未挂载的 pub async handler，拆分后必须把 9 个子文件路径补进 `route_files` 数组；`KNOWN_NON_ROUTE_HANDLERS` 白名单当时含 3 项（ingest_chunked_text/import_pdf_bytes/build_operation_knowledge_completeness）。出处：06-07 routes-split plan Task 11。
- **D-019 拆分期共享 helper 的归属决策**：`chunk_verify_gate_reason`、`truncate_for_prompt`、`fuzzy_locate_quote`、`normalize_for_anchor`、`budget_document`、`load_operation_knowledge_chunks_for_query` 等 26 个跨域 helper 留 mod.rs（约 700-900 行"共享内核"）；6 个 Query/Request struct 留 mod.rs 作共享类型。兄弟模块跨调用推荐 `super::catalog::fn`（mod 内兄弟可见）而非上提。出处：06-07 plan 共享 helper 表 + Task 10 注。

- **D-020 answeringMode 三档闭集与前端降级默认**：`relationship_only | product_safe | fully_supported`；前端 parse 未知值一律降级 relationship_only（最保守档）。5 维认知矩阵固定序：capability/pricing/caseEvidence/effectClaims/deliveryBoundary（中文：能力/定价/案例/效果数据/交付边界）。出处：06-07 trust-cockpit plan Task 1。
- **D-021 auto-verify 参数契约**：`confidence_threshold` 默认 7（0-10）、`human_audit_sample_rate` 默认 0.1、`limit` 默认 50 上限 500；前端三档松紧映射 宽松=5/适中=7/严格=9。出处：06-07 trust-cockpit plan Task 10。
- **D-022 单一生效键 = apply→verify 串调**：前端"让 AI 可以用这条"按钮顺序调 chat apply → chunk verify，verify 4xx 视为 `gate_blocked`（D2 闸拒），有 sessionId 才先 apply；canGoLive 是 D2 闸的前端镜像"不是替代后端校验"。出处：06-07 plan Task 7/8。
- **D-023 escalation 模块三层拆分与可见性**：logic（纯函数+全部测试）/ledger（async CRUD）/mod（4 个编排函数+pub use 通配 re-export 保 13 处外部调用零改动）；`is_duplicate_key_error`、`enqueue_relay_task` 因跨文件升 pub(crate)。出处：06-08 escalation-split plan。
- **D-024 前端知识域跨组件通信 = CustomEvent**：knowledge 前端组件间通信靠 window CustomEvent（wikiFocusChunk 聚焦 chunk、wikiOpenCockpit 打开治理屏），拆分/重构时这是唯一隐性耦合点；明确"不把 CustomEvent 改成 Context/状态库"（YAGNI）。出处：06-08 knowledge-frontend-split plan。
- **D-025 memoryCard 默认 8 业务槽位 cap 表**（DEFAULT profile 逐字复刻值）：preferences=8/doNotDo=10/commitments=8/objections=8/openLoops=8/openQuestions=8/confirmedFacts=12/conflicts=6；typed 三数组 coreFacts/recentFacts/deprecatedFacts 固定 cap=**6/10/20**（不维度化）。出处：06-15 h17 plan。**注**：既有 06 号记录发现"deprecatedFacts 两处 cap 不一致 6 vs 20"——本 plan 佐证设计意图是 20，cap=6 的那处疑为缺陷（列入第 6 节）。

- **D-026 LLM 裁判的失败语义分级**：JudgeGate 两档——QualityGate（judge 为唯一质量门时：reply 为空/采样全失败/端点配错 4xx 一律 panic，"不静默绿"）vs ObserveOnly（跳过返 None）；REAL_LLM_JUDGE=1 是真模型评判总开关，未设则零成本跳过。出处：06-19 eval-overhaul plan Task 3。
- **D-027 状态机 policy 派生规则**：`derive_state_policy_lists(forbids_proactive)`——true→(allowed=["silent","follow_up"], forbidden=["reply"])；false→(allowed=["reply","silent","follow_up"], forbidden=[])；派生行以 seeded_by 前缀区分机器派生（statemachine_publish:/statemachine_edit:/legacy_migration）与运营手工行，手工行 publish 时跳过保留。出处：06-19 residuals plan Task 8 + 06-20 audit plan Task 5。
- **D-028 enforce_state_action_policy fail-open**：主动触达门对缺失 policy 行放行（`enforce_state_action_policy(None)=Ok`，guards.rs:263 当时行号）——这是 forbidsProactive 静默失效缺陷的结构性根因（派生表缺行=不拦）。出处：06-19 residuals plan Task 8 根因段。
- **D-029 五闸阈值 clamp 语义**：apply_profile_threshold_overrides 是五闸阈值覆盖唯一收口点，G13 修复后每字段 `.clamp(1,10)`；否则 admin 配 fact_risk_block_at=100 → `hallucination_score < 100` 恒真 → 幻觉硬闸整个禁用。evolution 侧 FIVE_GATE_HARD_MIN/MAX 是 f64 且模块私有，两处独立写路径各自设防。出处：06-20 audit plan Task 2。
- **D-030 grounding bypass 双闸对齐义务**：`grounding_gate_bypass_without_claim` 守卫必须同时存在于 classify_dual_gate 与 review_passed 两处（G01 前只有前者→bypass=true+无产品声明+低 grounding 时 classify=AllPass 但 approved=false，落 held_by_ai_policy 误拦纯情感回复）。出处：06-20 audit plan Task 3。
- **D-031 出站正文运行期禁词 fail-closed（G24）**：gateway Approved 分支在 outbox 入队前对 final reply_text 过 `evolution::lint::passes_forbidden_words`，命中→review.approved=false、final_review_status="blocked_by_safety_guard"、risks 加 "outbound_redline_forbidden_word"、写 `outbound_redline_blocked` 事件；理由=CI lint 只扫静态 diff，profile override 文本经 LLM 流入正文不在其覆盖面。出处：06-20 audit plan Task 11。
- **D-032 RISKY_FIELD 二次确认机制**：DomainProfile 危险字段清单（G31 后 13 项，含 reviewer_orientation/mode_gate_policy_override）改动需走旁路稿二次确认，非危险字段即时生效；reviewer prompt 的取向替换靠锚文本（DEFAULT_REVIEWER_REVIEW_FOCUS 等）定位，锚漂移则 override 静默失效——需护栏测试断言真 prompt 含锚。出处：06-20 audit plan Task 6。
- **D-033 多租户已知薄弱点（G21 型缺陷模式）**：`state.config.default_workspace_id` 在函数体内替代真实 workspace 是复发性缺陷模式（build_initial_operation_profile 4 处）；修复靠签名加参编译强制。出处：06-20 audit plan Task 1。
- **D-034 CI 真模型 key 闸对齐**：real-llm job 必须对每个用到的 key 加 Require 步骤（缺 ROLEPLAYER_API_KEY→roleplayer turn-0 静默 skip→数字分身轴假绿）；ROLEPLAYER_API_KEY 映射 secrets.NVIDIA_KEY。出处：06-20 audit plan Task 13。

- **D-035 既成事实纪律（发送后写库 fail-soft）**：MCP 发送成功后任何旁路记录（send_ledger/ConversationMessage）落库失败**绝不返 Err**只 tracing::error——返 Err 会让 dispatcher retry 重发，客户收重复文件/名片。出处：06-21 send-ledger plan Global Constraints。
- **D-036 转化率统计分母**：响应率/阶段推进率以"已评估条目"（outcome_evaluated_at 非空）为分母而非总发送数——避免新发未过窗口的条目拉低率。回扫默认响应窗口 24h，每 tick 限量 200 条，窗口未过跳过下轮再看；"阶段推进"= 当前 stage 在状态机 states 数组序中严格靠后于发送时快照，任一缺失/不在表保守判 false。出处：06-21 send-ledger plan Task 5/6。
- **D-037 防重发是 prompt 软约束不是硬门**：ledger 只喂 decision prompt"近期已发素材"提示段（近 10 条），"不得在发送路径加发过就拦的硬阈值/词表门"（agent-first 红线）。出处：06-21 send-ledger plan Global Constraints + Task 7。

- **D-038 prompt 种子版本门控**：改 prompts.rs 的 `user.reply.task` 等字面量后必须 bump `PROMPT_PACK_VERSION`（prompts.rs:15 当时）或走 reset-system-pack，否则 `ensure_prompt_pack_v2` 版本门控不重种、改动不生效——与 D-017"存在即跳过"互为表里。出处：06-21 referral plan Global Constraints + Task 10。
- **D-039 三处接线红线（LLM 输出字段防丢）**：AgentDecision 新增字段必须同时改 AgentDecision + RawAgentDecision + validate_and_promote carry-through 三处，"漏 carry-through 则 LLM 输出被静默丢弃"。出处：06-21 referral plan Global Constraints。
- **D-040 outbox 幂等键的非文本扩展**：名片条目空 content 时幂等键追加 `:card:{card_id}` 后缀（compute_synthetic_key_with_card），否则多张不同名片空 content hash 撞键误去重；content_required_for 对非文本条目放宽空 content 校验。出处：06-21 referral plan Task 5。
- **D-041 辅助模式三级开关**：客户级 override（domain_attributes.assist_mode_override="force_on"/"force_off"）> 账号级 OperationDomainConfig.assist_mode_enabled > 默认关；脏值视为无覆盖。名片可发前提 enabled ∧ review_status=="approved"（创建默认 draft+disabled，AI 不自我核验）。「已引荐」态= domain_attributes.referred_specialist_at/referred_card_id（dotted-key $set 防覆盖其它 attributes + 同步刷 domain_attributes_updated_at）。出处：06-21 referral plan Task 1/3/6。

- **D-042 素材文件存储布局与白名单**：本地存储路径=`{workspace}/{sha256 前 2 位}/{sha256}.{ext}` 分片；路径段仅允许 ASCII 字母数字+-_（防穿越）；扩展名白名单 14 种（pdf/png/jpg/jpeg/gif/webp/doc/docx/xls/xlsx/ppt/pptx/mp4/mov），exe/sh 拒绝；上传大小默认上限 50MB。出处：06-21 media plan Task 2/3。
- **D-043 媒体类型→MCP 工具映射集中制**：image→message_send_image / file→message_send_file / video→message_send_video 集中在 `mcp_tool_for_media_type` 单表，"链接卡片/小程序确认工具名后在此加一行，不改调用方"；不认识的类型选材阶段即被排除。出处：06-21 media plan Task 5。
- **D-044 素材的领导核准分流**：`requires_principal_approval=Some(true)` 的素材在 gateway 转 outbox 时**不入队**，改走 escalation 请示通道，拿结论后再决定（名片则明确不套用此分支）。出处：06-21 media plan Task 8 + referral plan 实现期对齐。

- **D-045 ask_human 升级范围四布尔与回落映射**：`should_escalate_held` 由 ResolvedAskHumanPolicy 驱动——safety_guard/unverified_product 默认升级、ai_policy_hold 默认不升级（旧 high_risk_escalation_mode="all" 映射为 escalate_ai_policy_hold=true）、ai_waiting_for_more_context 等其它终态永不升级；ask_human_policy=None 时旧字段（principal_decider→decider_chain 单人链）兜底，字节等价。出处：06-21 phase1 plan Task 2/3。
- **D-046 决策人链与超时改派**：decider_chain 有序（主→备选…），推送恒取 chain[0]；`next_decider_on_timeout` 仅在 timeout_hours 配置且超时且当前人非链尾时返回下一位；task worker 周期扫描 pending 改派+重推卡（"AI 绝不替决策人拍板——只把请示转给链上下一位真人"）。出处：06-21 phase1 plan Task 4/10。
- **D-047 ask-human 收件箱聚合器形态**：8 类待审源扇出查询归一为 InboxItem{source,severity,action_kind(inline/rich),rich_component}，每 source 独立降级（错误进 errors 数组不整体崩）；lessons_learned 无 typed struct 走 raw Document collection；知识审核项**只读列出**（红线③：配置层不提供"知识免审"开关）。出处：06-21 phase1 plan Block C。

- **D-048 前端 CSS Modules 导入形态红线**：module.css 必须**绑定导入**（`import styles from "./X.module.css"` + `className={styles.x}`）；裸副作用导入 `import "./x.module.css"` 会被 Rollup tree-shake 把整份样式丢掉（生产 build 无样式）。出处：06-21 config-page plan Global Constraints（"用户硬要求"级）。
- **D-049 前端测试位置铁律**：vitest.config.ts include 锁死 `src/__tests__/**/*.test.{ts,tsx}`——测试文件放错位置会被 CI **静默跳过=假绿**；每次新增测试须 `npx vitest list | grep <name>` 确认被收录。出处：06-21 config-page plan Global Constraints + Task 3 Step 4。

- **D-050 relay 转述数字白名单**：`relay_introduces_unauthorized_number` 提取双方文本的数字 token（阿拉伯数字串+小数点，归一化去前导零），转述含授权 substance 之外的任何数字→fail-closed 不发（领导授权"9折"转述成"8折"或加"95%成功率"都拦）；"只兜客观数量，不判断措辞语义"。出处：06-22 audit-wave Task A1。
- **D-051 授权过期/链尾失联的客户反馈义务**：授权过期早退必须清 awaiting 标记+发中性收尾话术（否则 awaiting 永挂"永久压制对该议题的自主回复"+客户零反馈）；决策链尾失联时发 AI 延期安抚（last_holding_reply_ms 去重，holding_reply_min_interval_hours 默认 6h），安抚发给客户**不过 push_allowed**（"quiet_hours 是约束打扰领导的"）。出处：06-22 audit-wave Task A2/A3。
- **D-052 唤醒 jitter 算法**：per-contact 确定性 jitter=FNV-1a(wxid) % (max_ms+1)（"避免 DefaultHasher 跨版本不稳定"），wake_jitter_max_seconds 默认 900（15 分钟），防静默期结束整点齐发。出处：06-22 audit-wave Task D1。
- **D-053 账号离线防护语义**：webhook Offline 事件落库 online=false（此前直接 ack 丢弃、online 长期陈旧）；dispatcher 发送前查 online，掉线 **defer**（推后 next_retry_at 不消耗 attempt，区别于 retry）；账号日发送软上限默认 500 仅告警（agent.account_daily_send_soft_cap_exceeded）不拦。出处：06-22 audit-wave Task E1/E2。
- **D-054 非文本入站兜底**：入站解析 msgType 归一（未知归 "unknown" 不当 text）+落 media_ref；媒体理解未接通时发类型化过渡话术请客户文字补充（"不硬答空串/原始 XML、不崩"）。出处：06-22 audit-wave Task F1/F2。

- **D-055 标签四层信任模型的写权限矩阵**：manual_tags 仅 admin 端点写（AI 代码路径零引用）；confirmed_tags 仅压缩重判 replace 写（逐轮判断只进 memory_candidates source="tag_observation" 暂定层）；bayesian_signals/personality_profile 纯观测永不进 planner filter/状态机/选材/触达门（契约测试守护）；裸 Contact.tags 已删除。出处：06-23 tag-trust 五部曲。
- **D-056 证据锚定与强弱判定**：Evidence={窗口序位 turn, 消息 ObjectId hex}（引用不拷原文，对齐 D2 source_anchors 哲学）；resolve_evidence 越界序位丢弃 fail-closed；Strong=锚定 Inbound 消息 ∧ LLM 标 explicit_intent（代码客观判，不信 LLM 自称置信）；customer_stage 弱证据不实时写只沉淀暂定层。压缩窗口与逐轮窗口的 turn 序位互不通用，msg_id 是唯一跨窗口稳定锚。出处：tag-trust-2/3。
- **D-057 压缩窗口预算参数**：consolidation_window_char_budget 默认 6000（clamp 1000-16000）、consolidation_window_max_messages 默认 60（clamp 10-200），字符+条数双上限谁先到为准；贝叶斯占槽阈值 bayesian_slot_min_hits=3（clamp 1-20）/min_strong=2（clamp 0-20）。出处：tag-trust-3 Task2 / tag-trust-4 Task2。

- **D-058 鉴权单角色事实**：全系统只有 `AuthenticatedAdmin` 一种鉴权注入（require_session 对 cookie/JWT 都注入它），不存在"运营态非 admin"角色；任何"按角色区分权限"的设想在当前代码没有落点。出处：06-25 taxonomy-label-wiring 可行性修正 M2（实测 auth/middleware.rs:51）。
- **D-059 taxonomy 候选 approve 的 display_name 链路**：TaxonomyCandidate.suggested_display_name（serde default）承载 AI 建议中文名，approve 时 `display_name = suggested_display_name.unwrap_or(raw_value)`；运行时落候选处传 None 行为不变。AI 生成取值只进候选层复用人审 approve 通路（"绝不直接进 system_taxonomies"）。出处：06-25 plan M1 + Task 8。
- **D-060 三档提示词与 grounding 的耦合口径**：used_knowledge_ids 只在决策实际经知识档（forced_full 或 Escalate）时记路由 id；PROGRESSIVE_TIER_ENABLED=false 退回单程 Full。出处：06-24 progressive-tier-hardening Task 2/3（安全相关，与 D-016/D-030 同族）。

- **D-061 prompt 种子机制三阶段演变（时间线）**：①06-06 前后："存在即跳过，老库不变，CI 全新库自动生效"（D-017）→ ②06-21 前后：改 prompt 字面量必须 bump PROMPT_PACK_VERSION 否则 ensure 不重种（D-038；v10→v11 等版本号快速迭代）→ ③**06-26 起：空库分流+内容对齐**，版本号降级为溯源 stamp，改 spec 重启必生效；GC+align 每次启动跑；LRU 缓存靠"align 是否写入"布尔驱动失效。**读代码时以 ③ 为现行机制**，①② 已被推翻。出处：06-26 prompt-pack-alignment-completion。
- **D-062 前后端对齐系列的结构性缺陷模式**（batch1-3 累计）：前端调不存在端点（A1 双死端点）、后端能力前端零入口（C2-C5/D1-D3 等 30+ 条）、闭集枚举前端只认子集（B1 2/5 verdict）、富数据被投影压扁（B2/E10）、写死销售语义（batch2 全部）、serde rename 错配（"批次1/2 踩 5 次"）——审查方法论：按"76 缺口路线图"编号分级分批修。出处：alignment-batch1/2/3。

- **D-063 提示词编辑三闸架构**（管理面改 prompt 的完整防线，spec §4.4 落地细节只在 plan）：闸1 禁词（evolution::lint::passes_forbidden_words）→ 闸2 锚完整性（业务锚+红线锚 normalize 后逐字 contains，fail-closed）→ 闸3 LLM 语义审查（只审 diff 增量省 token，"判定靠 LLM 语义不靠词表"守 agent-first）；单点拦截在 update_prompt_template（所有路径共用）；编辑行落库置 seeded_by="manual" 使启动对齐永久跳过。出处：06-26 management-agent-thickening Task 6.5/6.6。
- **D-064 生产服务器事实档案**（多篇 plan 反复引用的部署记忆）：117.72.54.28:22 root、/opt/wechatagent、APP_PORT=3003（8080 空闲）、独立 Mongo 库名 wechatagent、systemd wechatagent.service（Restart=always、RUST_MIN_STACK=8388608）、首构建必配 USTC sparse 镜像、启动期 run_active_domain_state_machine_sanity_check 空状态机 active 会 bail、服务器上另有 agime-* 服务不可动。出处：06-26 management-agent-thickening Task 9（引用 [[deploy_server_117]]/[[project_deploy_117_first_deploy]] 记忆）。

- **D-065 prompt 加载链路的架构事实**（只在 plan 里核实成文）：reply 链 prompt 在 decide_reply_with_promote 内部加载（非入参），load_prompt_for_contact 全 src 仅 3 处调用（user.reply.system/policy/task），"纯按 key 读 DB 无注入口子"；review 链在 review_decision 内部按 review_mode 选 user.review.system 或 user.review.light.system；decision.rs 主模式="DB 读基线→内存 format! 追加 override→喂 LLM"（5+ 处同构追加）。出处：06-27 prompt-evolution-human-gated 阶段二核实节。
- **D-066 evolution 隔离 lint 的 8 禁符号**：check-evolution-isolation.sh 全量扫 src/evolution/**/*.rs，非注释行禁 `crate::agent::gateway`/`crate::agent::outbox`/`crate::mcp::`/`agent_send_outbox.insert`/`mcp_client.send`/`run_user_operation_gateway`/`handle_managed_message`/`handle_follow_up_task`；prompt_shadow 放在 src/agent/ 内（不受此 lint 扫描）从而可合法复用 loader 链——"在 agent/ 但只跑决策+评审不发送"。出处：同上 Task 12/13。

- **D-067 五批契约对齐工程全景**（06-28→06-30 收官）：批 1 知识域 5 投影+机制地基；批 2 运营/Agent 域 9；批 3 字典/分类域 5；批 4 进化/实验域 8；批 5 配置/playbook 域 5——共 32 个投影函数全部纳入"后端快照+前端键集对账"双门，防腐烂 lint ALLOWLIST 从 30+ 项渐缩至 6 项永久 helper 豁免（bson_from_json/bson_doc_to_json/parse_warning_to_json/vision_generate_json/lesson_doc_to_json/cohort_run_ids_json）。fixture 是前后端唯一真相源（bless 生成绝不手写）。此工程与 76 缺口路线图（alignment-batch1-4）互补：前者防未来漂移，后者修存量缺口。出处：contract-alignment-batch1~5 五篇 plan。

<!-- END-S2 -->

## 3. system-review 逐文件记录（性质/发现/处置/遗留）

### 3.0 体系总览（读者必读）

`docs/system-review/` 是一套独立于 `project-understanding/` 的"全系统 100% 代码审查"台账体系（README.md 自称），冻结对象为 **PR #223 head commit `12d99b3b`（2026-07-17 冻结，基线 1243 个跟踪文件）**，不是浮动分支（baseline.json）。方法论分 13 个阶段（阶段 0 冻结台账→1 规则/启动→2 数据层→3 外部边界→4 Agent 主链→5 管理面→6 知识→7 演化/Worker→8 前端→9 测试/脚本/CI→10 交叉验证→**11 真实业务闭环复审→12 业务简化/反过度工程复审**），结论标记四态 FACT/CONTRACT/RUNTIME_UNVERIFIED/UNKNOWN（review-plan.md）。产出：findings.md 记 SR-001~SR-183 共 183 条发现；two-pass-review-ledger.md 记每条 SR 的两轮复审结论+修复实施证据；human-confirmation-checklist.md 把 183 条 SR 合并为 **36 个人类决策项（HC-001~HC-036）**，每项含推荐/最小处理/不过度工程边界/人类决定/实施状态。审查与修复在时间上交叠：findings 基于 07-17 冻结树，修复证据从 07-18 持续追记到 08-05（HC 清单与 two-pass 台账 8 月 5 日仍在更新）。

### 3.1 逐文件记录

1. **README.md**（性质：体系状态页）——声明"逐文件亲读、结论必须回指当前快照、不以历史报告代替代码事实"；两轮收尾复审（真实业务闭环+反过度工程）均采用"候选→正反向证据→反证→验证→去重"循环。**关键自陈：全系统审查尚未完成**（写作时阶段 8 前端逐频道审查进行中）；SR-001~183 的两轮复审"仅针对现有 findings，不把 file-ledger 中 pending 的历史材料宣称为已全文阅读"。
2. **review-plan.md**（性质：审查协议）——"100% 阅读"完成定义：每文件恰好一行 `read_complete`（亲读到 EOF）或 `verified_non_text`（二进制/锁文件核验用途）；"只看符号、搜索结果、文件头、局部片段、测试名称或旧审计报告均不算完成"。最终完成门 10 条（含哈希漂移重读、UNKNOWN 显式列出）。
3. **baseline.json**（性质：冻结元数据）——PR #223 `fix/management-raw-mcp-fail-closed`，head `12d99b3b`，base `9d28b73f`（main），冻结于 2026-07-17T13:09:04+08:00，1243 tracked files。
4. **file-ledger.csv**（1246 行；性质：逐文件台账）——列：baseline_commit/path/bytes/sha256/content_class/subsystem/review_status/batch_id/evidence/notes。subsystem 分布（抽样统计）：frontend 363、documentation 339、automation_scripts 65、e2e_scripts 57、agent 46、database 36、backend_core 22、evolution 14、auth 5、ci 3 等。
5. **reading-notes.md**（2101 行；性质：批次阅读笔记 B01~B08L）——每批次记 FACT 级事实与 file:line。B01 要点：main.rs 在 32MiB 专用线程建 Tokio runtime（规避 Windows 栈溢出）；启动顺序读配置→连 Mongo→迁移→索引→bootstrap→sanity→缓存预热；最多 12 类 supervised worker；启动必需 env 仅 MCP_API_KEY+OPENAI_API_KEY；**CORS 允许任意 origin/method/header**；路由 tripwire 静态扫描（挂载遗漏则测试失败，但"不等价于运行时路由完整性证明"）。
6. **architecture.md**（87KB；性质：审查产出的现状架构文档）——单进程拓扑图（SPA+webhook+API+最多 12 worker 共进程）；"进程内缓存/锁/广播不跨副本同步；是否安全多副本部署需逐 worker 复核"。
7. **data-model.md**（142 行；性质：集合/索引/迁移台账）——存储约定："默认顶层 snake_case，带 rename_all 的模型 camelCase；**llm_provider_configs/campaigns/campaign_sends 用 workspaceId（camel）**；索引与 doc! 查询必须按实际 BSON 键书写"。集合分组表内嵌各 SR 修复状态（与 two-pass 台账同步更新）。
8. **findings.md**（1643 行；性质：核心发现台账，SR-001~SR-183）——见 §3.2 主题归纳与 §4 遗留表。
9. **two-pass-review-ledger.md**（480 行；性质：两轮复审逐项账本+修复实施证据）——判定规则：第一轮只回答真实业务（直接/条件/治理/契约/合并五类）；第二轮只接受最小闭环（"优先补作用域、CAS、状态校验、字段投影或失败语义；默认不引入新服务、消息总线、分布式事务或平行状态机"）；"外部事实"标记表示冻结仓库无法证明的部署事实。每条 SR 恰好归属一个 HC。头部追记大量"当前工作树实施证据"（HC-003/014/015/016/018/019/020/021/022/023/027/028/029/032 等的部署级证据，含服务器 ELF SHA-256、PID、证据目录哈希）。
10. **human-confirmation-checklist.md**（642 行；性质：36 项人类决策面）——每项含来源 SR/两轮结论/推荐/最小处理/不过度工程边界/不处理代价/人类决定/实施状态/负责人。人类已明确决定的关键项：HC-002 Evolution **默认关闭**；HC-004 **正式支持多租户并修复**（"不能用当前单实例豁免隔离"）；HC-010 **采用 Mongo durable inbox，不引入 Kafka**；HC-017 Evolution **当前全部人工发布**（保留未来"安全收紧类阈值"自动白名单能力）；HC-005 登录限流分阶段（公网上线前硬门）。
11. **automation-script-review.md**（56 行；性质：65 个自动化脚本批次审查）——**biz-test 套件语义缺陷**：run_all.py 不把 preflight 失败传播为非零退出码、忽略各 domain 脚本返回码（"进程成功退出不是套件裁决"）；`_lib.assert_llm_success` 接受 cache_hit 为成功证明（不能证明本次真调了外部模型）；多脚本缺完整 workspace/account 身份、部分 fixture 制造 default-scope 数据绕过真实部署错配。运维安全：43 脚本有写、20 有删、8 远程执行；SSH helper 用 AutoAddPolicy 接受首次 host key、`_push_bundle.py` 远程路径无 shell 安全引号；`llm_providers_e2e.sh` 会激活合成 provider 且可能留活。**本批零执行零联网**（纯冻结源码审查）。
12. **automation-read-evidence.json**（33KB；性质：上条的机器证据）——65 行逐脚本 path/blob/行数/hash/首尾行/副作用标签，集合对账精确（65=65）。
13. **production-release-2026-07-25.md**（141 行；性质：首次正式生产发布证据）——用户明确授权"切换"；停服→备份（39.6MB 归档+SHA-256）→原子替换→12/12 健康观察；关闭 SR-008 部署门。含 4 个部署后专项（Provider 生命周期 5/5、SR-168、SR-170/171、SR-172）。**诚实记录事故**：编译时复用正式 Cargo target 导致磁盘二进制被测试构建覆盖，已从发布清单副本原子恢复（运行中进程未受影响）。
14. **production-release-2026-07-27-hc014.md 等 10 个小文件**（07-27/07-28 密集发布记录：hc014/hc015/hc016/hc019-relationship/hc020/hc021/hc022/hc023/sr056/sr129130）——统一模式：正式 release 源码逐字比对→服务器真实 Cookie Router+rs0 副本集随机库红线→测试库计数前后一致（126→126）→正式 PID/哈希/健康零漂移→证据目录 SHA-256 清单。
15. **production-release-2026-07-28-wave1.md**（39 行；性质：Wave1 批量发布）——结算 SR-097/132/138/139/141 部署边界；发布脚本任一门失败自动回滚（本次未触发）；**真实模型边界诚实记录**：四个 provider 分别 Cloudflare 530/1016、120s 超时、无成功日志、DashScope Arrearage（欠费）——"真实路径已触达且失败时安全降级，成功响应受外部 provider 状态阻塞"，不记模型门通过。
16. **production-release-2026-07-28-hc026-m039.md / hc029.md / 2026-07-30-hc004-sr025.md**（性质：单项发布记录）——hc029：SR-135 首次 32 并发红线**发现测试自身缺陷**（跨集合独立读取跨过 winner commit 把原子提交误判 1/2，改同一 snapshot 只读事务后 7/7）；sr025：旧实现 expected-red 先证明 bug 真实存在再验证修复。
17. **security-incident-hc001-2026-07-30.md**（43 行；性质：安全事件记录）——见 §4 HC-001 条目。核心事实：70 字符 LLM 凭证进入公开 Git 历史两个可达提交树、**2026-07-30 合成探测确认凭证仍有效**、69 个 CI run 日志合计出现原值 1,795 次（根因：Workflow"Secret 缺失时回退明文字面量"表达式）、服务器 10 个普通文件+29 个 Git blob（249 次）+17 个压缩载体（38 次）含旧值。已完成无中断控制（删除受跟踪文件、check-secrets.py 硬门、轮换工具链就绪）；**8 项未完成硬门**（生产轮换/撤销/日志删除/历史改写决策等）。
18. **hc001-credential-rotation-runbook.md**（139 行；性质：轮换运行手册）——5 条完成标准（预检→原子切换→GitHub Secret 同步+最小真实模型任务验证→撤销旧值双向证明→载体收口）；"任一动态验证未执行只能记未完成，不得以静态检查、mock 或 Secret 名称存在代替"；凭证只经 owner-only 文件+stdin 传递。
19. **build-ledger.ps1 / check-ledger.ps1 / update-ledger.ps1**（性质：台账维护脚本）——生成/校验/更新 file-ledger.csv 的 PowerShell 工具（哈希对账、集合相等性检查）。

### 3.2 findings.md 主题归纳（183 条 SR 的系统性模式）

- **多租户 workspace/account 隔离系统性缺失（最大主题，≥30 条）**：SR-011（MCP 凭证解析固定 default workspace，P0）、SR-012（debounce reload 丢 workspace，P0）、SR-013（LLM Registry 进程单例跨租户串配置）、SR-018（LLM 日志固定 default）、SR-021（Reply/Review/Reaction/Memory prompt+Soul 固定 default workspace 加载）、SR-024（公共事件写入器固定 default）、SR-025/026/027（发送计数/幂等键/stop 取消缺 workspace）、SR-036/040/041（outcome 任务/领导回复匹配/timeout scanner 缺租户或 domain 维度）、SR-050/116/137/152（台账/成交归因/BehaviorSignal/复盘缺 account）、SR-085/134（Evolution/Planner/Cold/Silence worker 只扫默认租户——"功能黑洞"）、SR-110（revision 聚合把全库编辑者暴露给任一租户）、SR-119/124（Digest 调度/卡片 dismiss 缺租户）。
- **前端切号身份漂移家族（HC-030，11 条）**：SR-142~161 中 11 条——切账号后旧对象（联系人/密钥/Playbook/标签/运营池勾选/任务/Outbox/素材/成交选择）保留并可通过 B 页面发出合法请求写 A 对象；SR-169（ReviewQueue 无 React key，A 草稿可提交给 B）。
- **多步写无事务/CAS/唯一约束家族（≥25 条）**：SR-008/043/053/055/056/071/074（current/active/default 指针多步写可留零或多指针）、SR-057/059/097（先终态后校验/无 claim 重复晋升）、SR-064/076/082/103/112（Management/Campaign/纳管/Split/导入半提交）、SR-133（threshold 并发 release 双生效）。
- **worker 无 fencing 家族（≥10 条）**：SR-028/034/039/115/117/122/136（Reaction/Task/escalation scanner/Catalog/Ingest/KnowledgeTask/ImportJob 缺 owner/generation/lease）、SR-177（webhook ACK 后仅进程内 PENDING，崩溃永久不回复，P1）。
- **fail-open / 伪装成功家族（≥15 条）**：SR-022（产品门信任 Reviewer 自报）、SR-023（revision 失败恢复危险原稿，P0）、SR-175（Reviewer 危险评分缺失归零被当安全）、SR-068（DB 错误伪装零待办）、SR-107/123（auto-verify/KnowledgeTask 把失败包装成功）、SR-121（Digest 失败覆盖成功报告）、SR-138（**Prompt pack 探测读失败触发四集合破坏性重置，P0**）。
- **红线旁路家族**：SR-037（领导原话 LLM 解释直升 verified 知识）、SR-065（Management LLM 可无确认写 staff_confirmed 成交——"突破 AI 永不自证成交"）、SR-101/102/113（通用 PUT/Split/Patch 可绕人审直写 active+verified、跨 workspace 注入知识）、SR-062（裸 message_send_text 绕过发送网关）。
- **测试自证/假绿家族（HC-032）**：SR-126（召回评测把金标注入 mock——"80% 命中由测试脚本预先保证"）、SR-128/178（真模型套件允许目标行为零发生仍绿+skip 不记账）、SR-174（测试独立 Mongo 库共享进程级缓存互相污染）、SR-176（隔离测试重写生产 filter 自证——"生产 Handler 漏掉租户条件仍会绿"）、SR-179（Kiro 任务表把未接线/已删除工作标 [x]——**与本 project-understanding 既有记录高度相关**）、SR-183（47 域"事实底座"允许空证伪和静默漏域）。
- **契约漂移家族**：SR-069/105/118/129/130/131/145（Playbook/Chunk 操作/Catalog 包络/AutoVerify 参数/ReviewChat/文档编辑/扫码登录前后端字段漂移——多条"正式 UI 确定性不可用"）。
- **安全个案**：SR-001/002/005（凭证泄露，P0）、SR-109（摄取 URL 持久 SSRF）、SR-016（登录无频控）、SR-158（CSV 公式注入）、SR-089（UTF-8 字节切片 panic）。

<!-- END-S3 -->

## 4. system-review 遗留问题总表（声称未修的全部条目，附出处）

<!-- END-S4 -->

## 5. smoke 文档要点

<!-- END-S5 -->

## 6. 与既有 19 份深读记录的矛盾点（待裁决清单）

1. **evolution 审计伪造缺陷的演变链**（对应 12 号记录缺陷）：06-26 main-health-audit-batch1 的 EVO-2 只修了 release/rollback 三端点的 actor（改传 admin.username），**明确保留** `DEFAULT_RELEASE_ADMIN` 常量供 put_evolution_runtime_flag（当时 :581）作 updated_by 回落——12 号记录后来发现 evolution.rs:742-746 的 updated_by 取请求体可伪造审计身份，正是这条未修面的延续。**裁决**：12 号缺陷属实且有计划级铁证（batch1 有意只修三端点）；runtime flag 端点的 updated_by 可伪造是 EVO-2 的已知残留而非新引入。
2. **裸 POST chunks 不强制 draft（红线端点级缺口）**：06-26 alignment-batch3 E7 明文记载"后端裸 POST chunks 默认落 active，不强制 draft"，红线（人工新建也先进待审池）靠**前端提交时显式带 draft+needs_review** 自律维持。与 08 号记录"红线 15 处落点正面核证"的结论并存时需注意：08 号核证的是 ingest/import 等 AI 入库路径，**admin 手工 create 端点是独立面**——若有直接调 API 的客户端（脚本/第三方）可绕过红线。待裁决：需对码确认 create_operation_knowledge_chunk 现状是否仍默认 active。
3. **deprecatedFacts cap 设计意图=20**（对应 06 号记录疑点"两处 cap 不一致 6 vs 20"）：06-15 h17 plan 明文"coreFacts/recentFacts/deprecatedFacts 仍走 typed 三数组的固定 cap 6/10/20"——设计意图 deprecatedFacts=20，cap=6 的那处代码与设计矛盾，佐证 06 号疑点为真实缺陷（cap 用错了 coreFacts 的值）。
4. **user.reply.task 的时间线佐证**（对应 17/18 号记录）：06-05 principal plan Task 13、06-21 referral plan Task 10 等均直接修改 `user.reply.task` prompt——该 key 在 6 月下旬仍是活跃主 prompt；18 号记录称其退役窗口为 07-15→08-05，plans 侧与此吻合（07 月中旬后的 plans 改称 reply prompt 分层）。无矛盾，但 plans 全程无一篇记载"user.reply.task 退役"决策本身，佐证 18 号"最大文档-代码漂移点"结论。

5. **15 号记录"零断言空壳测试"的计划级出处**：15 号记录发现 `tests/memory_card_write_occ.rs` 与 `tests/revision_recheck_action_gate.rs` 是"永远绿的空壳"（无断言）。06-26 main-health-audit-batch2 Task 4/5 的 Step 1 **明文预告了这一点**："集成测试搭建成本高……此测试作为 #[ignore] 骨架 + 详细注释说明验证点，实际并发断言留 CI/后续。核心交付是 Step 3 的 OCC 写正确性，由代码审查 + 镜像现成模板保证"（Task 4）；"revision 路径依赖真实 LLM，难在本地确定性复现。测试作 #[ignore] 骨架……核心交付是 Step 3 的复检逻辑由代码审查保证"（Task 5，并附"若能用 mock 注入点优先做确定性测试"的未兑现期望）。**裁决**：空壳非事故而是计划内决策，但"实际并发断言留 CI/后续"的后续从未发生——15 号的"测试债"定性成立，且可溯源至此计划。

6. **D9 字段表功能"交付即坏"的时间线**：06-27 alignment-batch4 D9 交付 domain_schemas 前端写表单（复用"现成后端 CRUD"）；**06-30 wiki-audit 发现该后端 CRUD 的 11 处查询键 camelCase 错配**（模型实写 snake_case）→load_active_domain_schema 恒 None→动态字段校验从未执行——即 D9 交付时后端就是静默坏的，前端功能看似可用实际 enforce 从不生效。**裁决**：与 08/11 号记录读到的现行代码（已修复后 snake_case）无矛盾，但揭示"复用现成端点"类计划的风险——现成端点未必真的工作过。
7. **prompt 编辑三闸的单点拦截神话被打破**：06-26 management-agent-thickening 设计"update_prompt_template 单点拦截（所有路径共用）"；06-30 wiki-audit #2 发现 **create/publish 两条路径绕过该单点**（create-draft→publish 链不经过 update）→补 create 双闸+publish 三闸。若后续记录仍描述"三闸单点在 update"，应以"create/update/publish 三点全有闸，publish 是最终生效点"为准。

<!-- END-S6 -->

## 7. 覆盖自证（读过的文件数+清单）

<!-- END-S7 -->
