// 后端 threshold_override_json 投影下发的 canonical 顶层键集(抄自 fixture,非手猜)。
export const CANONICAL_KEYS = [
  "gateKey",
  "id",
  "releasedAt",
  "releasedBy",
  "rolledBackAt",
  "rolledBackBy",
  "sourceProposalId",
  "value",
] as const;
