# HC-001 凭证暴露事件记录（2026-07-30）

本记录只保存不可逆、不可用于恢复凭证的事实。禁止在本文件、issue、日志或提交信息中写入凭证值、前后缀或摘要。

## 已确认事实

- GitHub 仓库为公开仓库，默认分支为 `main`，当前未启用 GitHub Secret Scanning，`main` 未配置分支保护。
- 当前 GitHub API 可复现两个仍可达的历史提交树包含同一个 70 字符非占位 LLM 凭证：`81f820f7afdf87cfc25bc58f7fb63b414a3d69b4` 直接修改 `.env.e2e`，`3c66561d59eab3205936863445c446484fd6f6f9` 继承含该值的树。先前记录的第三个短哈希当前无法解析，已从可复现计数中排除；当前受跟踪树对该完整值精确扫描为零。
- 2026-07-30 使用不含项目代码、业务数据或用户数据的合成 `ping` 请求进行最小鉴权探测，上游接受该凭证。因此它必须按仍有效且已公开泄露处理。
- 早期服务器扫描在 23,722 个普通文件中发现 8 个精确副本。2026-07-30 使用仓库化只读预检器重新扫描当前三个部署/备份根目录的 102,259 个普通文件后，当前基线为 10 个文件、各命中 1 次：正式 `.env`、四个 `.env` 备份、`.env.e2e`、一个 release 回滚环境，以及三份备份目录中的 `app.env`。新增两项来自早期扫描后的后续备份，不再沿用过时的 8 文件计数。
- 正式 `.env` 及备份为 root `0600`；`.env.e2e` 为 root `0644`。权限限制不能抵消公开 Git 历史暴露。
- 服务器 `/opt` 下共识别 101 个 Git 仓库，只有 `/opt/wechatagent` 的规范化 `origin` 精确匹配 `jsjm1986/wechatagent`，未发现第二个同源服务器克隆。对该正式克隆的 14,448 个 Git 对象（其中 5,117 个 blob、234,081,004 字节）逐一读取后，29 个 blob 精确命中旧值，共 249 次；普通文件扫描不能覆盖这些压缩 Git 对象。
- 对本项目部署、回滚和备份目录中的 25 个压缩载体逐一流式解压扫描，25/25 成功，其中 17 个载体精确命中，共 38 次。16 个为数据库归档类 gzip，每个命中 2 次；另一个 `source-config.tar.gz` 的 6 个环境文件成员各命中 1 次。压缩载体未被解包落盘、删除或改写。
- 当前 GitHub Actions 使用 repository secrets 注入模型凭证。2026-07-30 对 GitHub API 当前可枚举的 807 个 run 日志逐一下载、解压并精确扫描：807/807 均成功扫描，其中 69 个 `CI` run 的日志在 2026-06-02 至 2026-06-08 间合计出现旧凭证原值 1,795 次，常见 Base64 表示零命中。69 个 run 包含 51 次 push、18 次 pull_request，结论分布为 cancelled 45、failure 4、success 20；最早日志暴露早于当前本地可复现的 `.env.e2e` 历史提交。
- 同期对 313 个当前可下载 artifact 逐一下载、解压并精确扫描，313/313 零命中；另有 1,933 个 artifact 已过期、正文不可下载，因此该零命中不能外推到已过期工件。仓库当前 fork 数为 0。
- 仓库未发现 Dockerfile、Buildx、GHCR 或镜像推送步骤，服务器也未安装容器运行时，因此未发现本项目实际的容器镜像构建/发布路径。当前 GitHub 令牌缺少 `read:packages`，Packages 元数据无法枚举；该平台侧空缺不能被记为“确认无 package”。

## 已完成的无中断控制

- 当前受跟踪树不再包含已知旧 LLM 凭证。
- 删除受跟踪 `.env.e2e` 和明文探测 Workflow，示例配置只保留占位符。
- 新增高置信受跟踪文件扫描器 `scripts/check-secrets.py` 并接入 baseline CI。扫描器只报告路径、行号和规则，不打印值或可逆摘要。
- 合成测试凭证使用显式 `synthetic`/`test` 形态；固定 JWT 私钥仅允许在明确的公开测试夹具路径中存在。
- 已新增生产轮换器、GitHub Actions Secret 标准输入同步器及运行手册。定向测试、Linux 权限/锁测试和 GitHub 只读目标预检通过；截至本记录更新时未执行生产切换、远端 Secret 写入、旧凭证撤销或服务器副本删除。
- 已新增只接受冻结扫描结果的 Actions 日志清理器。20 项 HC-001 工具联合测试通过（Windows 上 1 项 POSIX 权限测试按设计跳过），真实只读预检验证 69/69 个命中 run 身份；尚未调用删除接口，69 个日志仍可下载。
- Actions 日志根因已定位并关闭：历史 Workflow 使用“GitHub Secret 缺失时回退到明文字面量”的表达式，再把同一值赋给三个凭证别名，runner 因无法把字面量识别为 Secret 而在步骤环境清单中反复输出。当前 Workflow 的凭证型环境变量只允许直接引用单个 `${{ secrets.* }}` 或显式空值；`scripts/check-secrets.py` 已增加 `workflow-secret-must-be-direct` 硬门及历史形态回归测试，当前受跟踪树零发现，CI policy 仍强制执行该扫描器。
- 两轮服务器载体扫描均为只读，临时 `/run` 扫描器按路径、普通文件类型和冻结 SHA-256 精确清理。扫描前后正式服务 PID `2517121`、`NRestarts=0`、健康 `ok=true`，未修改正式配置、数据库、Git 对象或备份。
- 已新增无 mutation 模式的 `scripts/deploy/audit_hc001_server_carriers.py`。本地联合门 28 项中 26 通过、2 项仅在 Windows 跳过 POSIX 权限语义；授权 Linux 服务器专项 8/8 通过。正式只读预检完整扫描 102,259 个普通文件、14,448 个 Git 对象和 25 个压缩载体，返回预期的 `ordinary_file_copies_present`、`git_object_copies_present`、`archive_copies_present` 三项 blocker；扫描无错误，临时文件已精确清理，生产状态零漂移。

## 未完成硬门

1. 在上游生成替代凭证，并以合成请求验证新凭证可用。
2. 备份正式环境文件的权限/元数据，原子替换正式凭证，重启服务并验证健康、模型调用和运行中/磁盘 ELF 一致。
3. 同步 GitHub Actions repository secret `RSXERMU_KEY`，并由最小真实模型 Actions 任务证明新值可用；Secret 元数据只能证明名称存在。
4. 生产与 Actions 验证成功后撤销旧凭证，再证明旧凭证被拒绝、新凭证仍可用。
5. 轮换验证后按三类边界处理服务器载体：10 个普通文件逐路径替换或删除；17 个压缩备份依据回滚保留策略重建为不含旧值的备份或加密封存；正式克隆的 29 个命中 Git blob 只能随单独批准的历史改写/重新克隆处理。三类不得用递归通配一并删除。
6. 经明确删除授权清理 69 个已确认泄露的 Actions 日志，并复验日志下载端点不可用；当前可枚举 run、当前可下载 artifact、fork、同源服务器克隆及本项目容器发布链盘点已完成，但 1,933 个已过期 artifact 无法回溯扫描，GitHub Packages 元数据因令牌缺少 `read:packages` 仍不可证实。
7. 启用 Secret Scanning、push protection 与 main 分支保护；日志输出根因已修复并由 CI 硬门防回归。
8. 评估是否改写公开 Git 历史。历史改写会改变提交 ID并要求所有克隆重新同步，必须另行授权。

## 轮换回滚边界

轮换属于生产认证变更。只有在新凭证预检成功后才允许修改正式 `.env`；若服务重启或模型烟测失败，应恢复切换前环境文件并重启旧配置。旧凭证撤销必须晚于新配置的生产验证；撤销完成后不得以恢复已泄露旧凭证作为回滚方案。
