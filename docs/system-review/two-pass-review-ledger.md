# 两轮业务复审逐项账本

本账本覆盖 `findings.md` 的 SR-001～SR-183。

## 判定规则

- 第一轮只回答真实业务：`直接`=正常生产流程可达；`条件`=实现缺陷成立但依赖部署/并发/开关；`治理`=不会直接改变客户业务事实，但会使上线、审计或测试结论失真；`契约`=代码与产品意图冲突，必须由人决定；`合并`=问题成立，但与同一根因共用一个决策项。
- 第二轮只接受最小闭环：优先补作用域、CAS、状态校验、字段投影或失败语义；除非现有数据模型无法恢复，默认不引入新服务、消息总线、分布式事务或平行状态机。
- `外部事实` 表示冻结仓库无法证明的部署事实，不能擅自按“已启用”或“未启用”裁决。
- 每个 SR 恰好归属一个 HC；合并只合并人类取舍，不删除原证据。

## 当前工作树实施证据（不改写冻结审查结论）

### HC-003 Ops 三表单-current版本协议后续实施证据

- 覆盖边界：本批只结算 SR-008，不自动结算 Prompt、Soul、Playbook、DomainProfile 或 Operation Domain reset 的独立版本问题。
- 状态：`production-deployed / deterministic-verified / replica-set-integration-verified / post-deploy-observed`（2026-07-25）。已确认产品契约为：`operation_domain_configs`、`operation_state_policies`、`system_taxonomies` 保留 append-only 多版本历史，但每个逻辑 scope 恰好一个 `current_version=true`；不再把多个 current 解释为隐式灰度/A-B。
- 存量与约束：m048 在任何写入前完整解析三张表，拒绝非 canonical identity、非正版本和 scope 内重复版本；合法唯一 current 原样保留，零/多 current 才确定性选举最大 `(version, _id)`。迁移后以新名称先建立 logical-scope partial unique current 索引，成功后才退役旧非唯一辅助索引；首次升级和重复启动不会主动撤掉已生效约束。
- 运行与写侧：Domain/Policy 读取在 scope 已存在但 current 为零或多个时 fail-closed；Taxonomy 启动预热、TTL 刷新、Decision、Planner、Shadow、Guide 与运营视图均传播字典指针错误。三表 publish/rollout/rollback 共享副本集事务：事务内校验预期 current、分配版本、降级旧指针并插入/提升目标；冲突返回 409，历史行不删除。Taxonomy 缓存只在事务提交成功后失效。
- 验证：既有格式、编译、m048 单测 2/2、授权服务器 `rs0` 隔离迁移/索引/fail-closed 与 publish/rollback/rollout 动态用例均通过。2026-07-25 正式切换到后端 SHA-256 `539effe4f0cc1f6962c495f7454cd51375a35c854ca246901a9de1b5375e8acf` 后，12/12 次内外网健康通过，PID `1686295` 无重启，MongoDB 保持可写主节点。切换点备份、回滚材料与完整哈希见 [生产发布证据](production-release-2026-07-25.md)。

### HC-014 DomainSchema 精确版本与唯一 active 后续实施证据

- 覆盖边界：本批只结算 SR-056，不自动结算 SR-043、SR-044、SR-072、SR-073、SR-074、SR-089、SR-090、SR-094、SR-168，也不改变 DomainProfile、OperationDomainConfig 或自由 runtime BSON 的协议。
- 状态（2026-07-27）：SR-056 为 `production-deployed / deterministic-verified / replica-set-verified / post-deploy-observed`。DomainSchema POST 只创建全新 lineage；PUT 必须携带所选 `expectedVersion`，精确读取来源后追加 inactive 新版本，不再原地改写历史或按最高版本猜目标。activate/delete 同样绑定 `schemaId + expectedVersion`；删除只允许命中该 inactive 版本，零命中按冲突失败。
- 生效不变量：激活在 Mongo 事务内读取至多一个 workspace active、CAS demote 旧行并 promote 精确目标；重复 active、目标漂移或事务冲突均 fail-closed。运行时加载 active 时最多读取两条，发现多 active 返回错误而非任取一条。m044 在索引前只读审计 canonical workspace/schema identity、正版本、lineage 内版本唯一及 workspace active 至多一条，不猜业务赢家、不改数据；随后唯一版本索引与 workspace partial unique active 索引锁住不变量。
- 前端身份：列表同一 `schemaId` 的多张版本卡分别把所点版本送入编辑 body、激活 URL 与删除 URL；loading key 也使用 `schemaId@version`，避免同 lineage 卡片互相误锁。POST 对既有 lineage 返回 409，后续版本只能从带来源版本令牌的 PUT 创建。
- 验证：既有格式、编译、m044 identity/重复版本/多 active 定向单测 2/2、前端 10/10 与生产构建证据继续有效。当前正式 release 的路由、m044、索引和副本集测试源码与本地逐字一致；正式库 m044 为 `applied`，`(workspace_id,schema_id,version)` unique 与 workspace `is_active=true` partial unique 索引形状正确。服务器真实 `rs0` 精确用例 1/1：v1 active + v2 inactive 事务切换到 v2，active 始终恰好一条，两条不可变历史均保留；不存在 v99 返回 `domain_schema_version_changed` 且 active 指针不变。随机测试库 126→126，正式服务 PID、哈希与健康前后不变。证据见 [SR-056 部署后验证记录](production-release-2026-07-27-sr056.md)。

### HC-014 SR-094 typed runtime 写边界后续实施证据

- 覆盖边界：本批只结算 SR-094，不自动结算 SR-043、SR-044、SR-072、SR-073、SR-074、SR-089 或 SR-090。
- 状态（2026-07-30）：`production-deployed-artifacts-observed / deterministic-verified / cookie-router-replica-set-verified / post-verify-observed`。手工 Domain 编辑与 Guide 共用 typed runtime validator；未知键、错误类型、越界值与预算倒置均拒绝。Guide 再叠加 cadence/context/quiet-hours 白名单，高风险安全阈值、模型预算、lease 与协议开关不可由自然语言 Guide 修改。被验证的生产实现此前已进入正式 release；本批只新增专项测试和动态证据，没有修改或切换生产业务代码。
- 动态验证：授权服务器以真实 `wa_session` Cookie Router、`rs0` 随机隔离库执行 SR-094 专项 1/1。手工 PUT 将合法 cadence 值写入当前 `user_operations.runtime_parameters`，并把 legacy `factRiskBlockAt` 规范化为 canonical `hallucinationBlockAt`；未知键请求返回 400，目标 Domain 完整 BSON 零写。Guide 的高风险 `runTokenBudget` 建议返回 400 且不创建 Preview capability；合法 `maxDailyTouches=2` Preview 由服务端冻结为 `workspace_user_operations`，缺强确认时 Preview/Domain 零写，确认后在事务内提交 runtime、审计与稳定 receipt，同 candidate 重放不重复应用。首轮已走到成功 Apply，但测试只接受 BSON Int32、把合法 Int64 误判失败；仅将 oracle 改为 Int32/Int64 数值等价后完整重跑通过，生产代码未改。最终编译 18m24s、动态 16.33s，`SCRIPT_RC/CLEANUP_RC/FINAL_RC=0/0/0`；随机库差集、挂载和临时构建根均为 0，共享 target 5,584 项哈希通过。正式服务 PID `2410064`、`NRestarts=0`、ELF SHA-256 `d0b7ffc63ce93a0e4f3ec09e1d8b64c06c578fd08879908c25d04caa531e027b` 与健康前后不变。证据目录 `/tmp/hc014-sr094-evidence-20260730T073000Z`，`SHA256SUMS` SHA-256 `4aac2a22e50a08613c35140a6b3bd7bbc9d584a383eab82034a2891c9afd550d`。

### HC-014 DomainProfile 发布/激活协议后续实施证据

- 覆盖边界：本批核对并推进 SR-043、SR-044、SR-072、SR-073、SR-074、SR-089、SR-090；不改写已单独记录的 SR-056、SR-094、SR-168 结论，也不把工作树证据外推为生产部署结果。
- 状态（2026-07-27 基线，2026-07-30 更新）：SR-043/044/073/074/089/090 为 `production-deployed / deterministic-verified / targeted-server-verified / post-deploy-observed`；SR-072 为 `production-deployed / replica-set-verified / production-clone-rehearsed / post-deploy-audited`。DomainProfile 草稿与发布版本不可变，编辑只追加 draft；publish/rollout/rollback 只移动 lineage 的 published-current，activate 才在副本集事务内切换 workspace 唯一 runtime-active。m051 在索引创建前审计 canonical identity、正版本、重复版本、重复 current 和重复 active；随后版本、lineage-current、workspace-active 三个 unique/partial-unique 索引锁住约束。运行时 active loader 对多 active fail-closed。本批不自动结算 SR-056 或 SR-094。
- 身份与输入边界：publish 和 activate 都绑定精确 Mongo `_id`；activate 还要求目标仍为 `release_status=published + current_version=true`，更新不会原地替换已确认行，因此确认对象由不可变行身份绑定，不需要另建 content-hash capability。动态画像 kind 只允许 canonical ASCII snake-case 单路径组件，拒绝重复、`_updated_at` 后缀和系统保留 namespace，校验覆盖 draft append、publish/rollout 与 activate/runtime load。Guide key 归一化使用 `Vec<char>` 索引，非 ASCII 前缀不再发生字节下标 panic。
- 可达性与维护：DomainProfile 主列表返回完整版本流水；统一待审箱及 summary 共用 `draft OR (published current AND inactive)` 过滤器，并通过精确 profile id 打开既有 `ProfilePublishCard`。Operation Domain reset 不再删除历史，而是复用版本事务追加内置默认、CAS 切换 current，再返回 Policy 对账报告。
- SR-072 真实边界：核心 active 指针事务先从同一快照读取目标并完成全部永久性校验；`generated_state_machine=Some` 必须包含非空 states 且通过结构校验，非法内容返回 `BadRequest` 并在 active demote/promote 前 abort。新增真实 Handler 副本集红线比较 `domain_profiles`、`operation_domain_configs`、`operation_state_policies`、`contacts` 四集合完整 BSON，锁定永久非法状态机零写。核心提交后的状态机发布、Policy 对账和联系人迁移仍按显式步骤返回 `completed/partial + retryable + errors[]`，前端保留重试入口。
- SR-072 补强（2026-07-30，已正式部署）：保留四集合写侧现有 `completed/partial + retryable` 协议，不扩成跨集合大事务；运行时 Policy loader 收紧为：仅完全没有 current `user_operations` 状态机的旧部署兼容缺 Policy，一旦 current 状态机存在，缺失、零 current、多 current或非 active current Policy 均 fail-closed。普通 Reply、Revision、Shadow 复用该 loader；管理发送在 Outbox 前补接同一状态动作门，决策未携状态时按 Contact 当前态、再按状态机 initial 态回落。Prompt pack bootstrap/reset/既有库启动对齐在状态机写入后复用同源 Policy reconcile，结果不完整即失败，避免全新空库被永久锁死；人工 Policy 仍由既有 seeded-by 白名单保护，不被系统刷新覆盖。
- SR-072 验证与部署（2026-07-30）：授权服务器 `rs0` 随机隔离库真实执行 1/1，先证明全新 Prompt pack 已生成 active current Policy，再删除 `need_discovery` Policy 模拟 reconcile 丢行；mock-LLM 普通 Reply与管理发送均返回 `missing_current_operation_state_policy`，且 Outbox/MCP 均为 0。生产克隆演练确认候选启动唯一写集为 `operation_state_policies`，9 态/0 Policy 精确收敛为 9 态/9 Policy；正式 ELF 从 `d0b7ffc6…e027b` 原子切换为 `155bb77c…2ce67`，PID `2517121`、`NRestarts=0`。39,585,322 字节全库压缩备份、内外健康、69 项静态资源、静态配置不变、零活跃队列与零测试库残留均通过；独立部署后证据清单 SHA-256 `096a4ab292d5fb6dcd5f95f7cf7d605f73da19ed444491a662bac5b883697868`。匿名 Management 确认端点严格返回 401，六类发送相关计数不变，证据清单 SHA-256 `490e2aa2c065ce35826780bed20de59cba33691e3214461182191d54baecbae9`。正式状态为 `production-deployed / replica-set-verified / production-clone-rehearsed / post-deploy-audited`。
- 验证边界：服务器纯函数红线 1/1、`rs0` 四集合完整 BSON 零写 1/1、部署后 DomainProfile 测试函数 28/28 返回成功；其中 26 条真实 Mongo/Handler 路径实际执行，2 条真实模型生成用例明确 self-skip，不能记真实模型通过。补充真实 Cookie Router+`rs0` reset 红线 1/1、动态 kind 生产 validator 1/1、Unicode key 真实生成 Handler 1/1。候选在随机克隆库完成 72 集合、55 条迁移、m050–m054 applied、69 个静态文件逐字校验和外链入口/队列清零。正式后端切换到 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096`，PID `2021387`、重启数 0，健康与六类队列持续正常。状态机与 Policy 并非跨四集合原子提交，瞬时 Policy 写失败的短暂 fail-open 风险仍开放。

### HC-015 Taxonomy 身份与单 run 画像信号部署后证据

- 状态（2026-07-27）：SR-045/046/047/061 均为 `production-deployed / deterministic-verified / replica-set-verified / post-deploy-observed`。本批没有再次切换二进制；核对确认当前正式 release 源码已逐字包含四条实现，生产库 m050 为 `applied`，active/current partial unique multikey 索引形状与代码一致。
- SR-045/047：真实 mock‑LLM Gateway+Mongo 红线 1/1。单次 Agent run 同时产出未知 Taxonomy 值和同维重复 Bayesian 观测；候选只由 Gateway 最终决策写一次，`occurrences=1`。同 run 同值观测归并为一个 history 点、保留最高 confidence，且 `sourceRunId` 等于实际 AgentRun id。
- SR-046：真实 m050+索引红线 1/1。迁移为 current 与 historical 全版本回填 canonical+alias claims；两个 active owner 共享 alias 时全表审计在首写前失败，sentinel 保持无 claims；清除歧义后恢复生产索引，第二 active owner 由 Mongo E11000 拒绝。
- SR-061：真实 Cookie Router+`rs0` 事务红线 2/2。字典插入故障使候选 claim 整体回滚，修正后重试成功且 actor 取认证会话；合并既有 canonical 时从 current v3、历史最大 v9 追加 v10/previous v3，原始值与手工 alias 持久化，运行字段保持，候选终态与 current 指针同事务提交。
- 证据卫生：四条精确测试 4/4；本轮新增两个随机库均删除，测试库 126→126；正式 PID `2021387`、重启数 0、磁盘与运行中后端 SHA-256 均为 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096`，健康正常且 Evolution 关闭。服务器证据目录 `audit/hc015-taxonomy-20260727T103000Z` 的清单 SHA-256 为 `bdb514b609c188429c4d038c6057bd467395504b2771bec9f377e1c6a6117df7`；详见 [HC‑015 部署后验证记录](production-release-2026-07-27-hc015.md)。

### HC-019 SR-058/059/060 关系审核部署后证据

- 覆盖边界：本批只结算关系建议路径的可信 actor、事务一致性与审核周期；不自动结算 SR-057、SR-067、SR-097 或 SR-169。
- 状态（2026-07-27）：`production-deployed / replica-set-router-verified / post-deploy-observed`。正式 m045 为 applied，pending partial unique 索引形状正确；已部署源码与本地可信 actor、关系事务及索引实现逐字一致。
- 动态验证：真实 Cookie Router+`rs0` 1/1。认证管理员覆盖伪造 `reviewedBy`；成功审批同行提交建议终态与联系人画像；终态历史允许下一 pending 周期，同周期第二条 pending 被 E11000 拒绝；联系人写故障使建议 CAS、审核字段和画像变更整体回滚。
- 证据卫生：随机库 126→127→126，正式服务零漂移。原始 systemd `rc=1` 仅来自业务测试成功后的证据哈希相对路径错误；未重跑业务，裁定文件保留原始失败阶段。服务器证据目录 `audit/hc019-relationship-20260727T124500Z` 的最终清单 SHA-256 为 `03716865cbfa27e089483bb4dca50d64e965d9ea27ba4cdfcaae0a413c0060a6`。

### HC-019 SR-057 成交审批部署后证据

- 状态（2026-07-27）：`production-deployed / replica-set-verified / post-deploy-observed`。成功审批、重复审批、非法输入和审计故障三类路径均直接调用已部署同源 Handler。
- 动态验证：服务器真实 `rs0` 3/3。成功路径同行提交 signal、`staff_confirmed` outcome 与审计，重复审批零增量且认证 actor 覆盖请求伪造值；负金额首写前失败并保留 pending；审计 validator 故障使三处整体回滚。
- 证据卫生：三个随机库 126→129→126，正式服务零漂移。服务器证据目录 `audit/sr057-suspected-deal-20260727T130000Z` 的最终清单 SHA-256 为 `e98047eeb2c78d62b8fe868aa08681cec6b1db50d9ab65ba7697671e909fe1b4`。

### HC-019 SR-067 疑似成交统一收件箱部署后证据

- 状态（2026-07-27）：`production-deployed-artifacts-observed / deterministic-verified / real-mongo-handler-verified`。当前正式后端进程含 `suspected_deal`/`suspectedDealReview` 投影，正式前端静态制品含“疑似成交”筛选与专用审核卡；release 中聚合器、动态用例及两份前端源码与本地逐字一致。
- 闭环：统一收件箱第九源按 `workspace_id + status=pending` 投影疑似成交，summary 使用同一过滤口径；专用卡展示证据、联系人、置信度和出现次数，确认/驳回复用 SR-057/058 的事务 Handler 与认证 actor，不复制底层队列。
- 验证：前端收件箱/卡片/API/Store 专项 16/16、TypeScript 与生产构建通过；服务器真实 Handler+Mongo 1/1，证明当前 workspace pending 集合、`?source=suspected_deal` 与 summary 计数一致，终态和外 workspace 行均排除。服务器无可复用认证会话，因此本轮未声称生产 Cookie HTTP 交互通过。
- 证据卫生：测试库 126→126、差集为空；正式 PID `2021387`、重启数 0、运行中/磁盘 SHA-256 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096` 与健康状态不变，Evolution 关闭。证据目录 `audit/sr067-unified-inbox-20260727T150000Z` 的清单 SHA-256 为 `90965551a934c737231937149bc231d0eef14f79b5b60908535cba0819b062cb`。

### HC-019 SR-097 Lesson 晋升事务与幂等身份后续实施证据

- 覆盖边界：本批只结算 SR-097 的 Lesson→peer-case 晋升协议；不自动结算 SR-169，也不把部署前隔离证据冒充正式部署。2026-07-28 Wave1 已单独完成正式切换与部署后复验。
- 状态（2026-07-28）：`production-deployed / deterministic-verified / replica-set-router-verified / post-deploy-router-verified`。晋升以 Lesson `_id` 作为确定性 Chunk `_id`，并写入 `provenance.source=lesson_promotion + source_doc_id=lesson_id`；Chunk insert、Lesson expected-state CAS 与审计事件在同一 Mongo 事务提交。并发或重复请求最多重试后读取同一已提交身份，不再产生第二 Chunk；已 promoted 但来源锚不匹配时 fail-closed。
- 存量与约束：m055 在首写前审计完整 Lesson→Chunk 图，兼容缺失/null `review_status` 为旧 `pending_review`，拒绝重复 Lesson identity、缺失/共享/跨 workspace Chunk、错误 context、冲突 provenance 与孤儿 promotion-shaped Chunk；只对精确旧配对回填来源锚。启动随后建立 `(workspace_id,lesson_id)` unique 与仅 `provenance.source=lesson_promotion` 生效的 Chunk partial unique，不引入通用工作流。
- 验证：稳定部署基线上的 `cargo check --release --test sr097_lesson_promotion` 通过；m055 纯单测 3/3、索引契约 1/1、真实 Cookie Router+`rs0` 3/3。动态用例覆盖并发晋升收敛为一 Chunk/一审计、重复请求返回稳定 Chunk、审计 validator 故障使 Lesson 与 Chunk 整体回滚且修正后可重试，以及迁移精确回填与歧义首写前失败。
- 证据卫生：部署前证据目录 `audit/sr097-lesson-promotion-20260727T140000Z` 的清单 SHA-256 为 `169fd6c608e550d1df54f6cd0ce8e6b4fc4cf1a195b2553ba31f64d3959196af`。Wave1 正式切换后 m055 marker 恰好一条、Lesson identity unique 与 promotion provenance partial unique 索引形状正确；正式 Router 中两个并发晋升与后续重放收敛为同一 Chunk，持久事实恰好一 Chunk/一审计、Lesson 为 promoted，外 workspace 不可见。正式 PID `2086752`、零重启、后端 SHA-256 `5df573cf5aef14e5919e13157c5213d58e24219cc424098610fc5ee7f29a558b`，临时数据零残留。详见 [Wave1 生产发布记录](production-release-2026-07-28-wave1.md)。

### HC-019 SR-169 ReviewQueue 对象身份部署制品观测

- 状态（2026-07-27）：`production-deployed-artifacts-observed / deterministic-verified / authenticated-browser-interaction-pending`。当前正式 release 的 `ReviewQueue` 与三份专项测试源码和本地逐字一致；正式静态制品含陈旧列表拒绝提示及对应 generation 门实现。
- 闭环：共享队列以 `getId(item)` 作为直属 React key；每次接纳新列表提升 generation。动作执行前同时校验 generation、对象仍在当前集合且当前不在刷新，旧卡片闭包只提示重试并保持副作用函数零调用，覆盖 Taxonomy、领导裁决、Lesson、Chunk、Profile 等复用队列。
- 验证：`ReviewQueue + TaxonomyCandidateReviewCard` 的 `[A,B]→[B]` 回归锁定 B 的 URL 与 canonical body，旧 generation 回归锁定副作用零调用且无 React key warning；三份专项 17/17，TypeScript、生产构建与 scoped diff 门通过。
- 证据边界：服务器无可复用认证浏览器会话，因此本轮不声称完成部署后真实点击交互。正式 PID `2021387`、重启数 0、运行中/磁盘后端 SHA-256 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096` 与健康状态不变，Evolution 关闭。证据目录 `audit/sr169-review-queue-20260727T153000Z` 的清单 SHA-256 为 `5885a0e3e0f3cb17a663f5987cb3980f2823bd71d2c343c38df8de8c0277dbd0`。

### HC-016 Shadow/Simulation 零业务副作用部署后证据

- 状态（2026-07-27）：SR-048 为 `production-deployed / deterministic-verified / real-mongo-full-snapshot-verified / post-deploy-observed`。当前正式 release 源码与本地 `simulation.rs`、`shadow_finalize.rs` 逐字一致；Shadow 使用只读 memory、knowledge route 与 taxonomy 路径，复用 Reply、Review、独立 ClaimGate、finalize 和状态动作策略，不写联系人、记忆、知识整改、候选、Outbox 或 outbound message。
- 真实红线：服务器 `rs0` 随机库运行完整 mock‑LLM Shadow 1/1。测试在执行前后枚举除 `llm_call_logs` 外的全部集合并逐文档排序比较，结果业务集合差异为 0；唯一新增事实为 Reply、Review、ClaimGate 各一条、共 3 条 `run_mode=shadow` 成本日志。预置 operator preference 的 `last_used_at` 保持不变，终态为 `would_send`。
- 证据边界：该证据证明已部署确定性 Shadow 路径的数据库副作用边界，不声称真实模型或真实 MCP 外部链路已执行。测试库 126→126；正式 PID `2021387`、重启数 0、磁盘与运行中后端 SHA-256 均为 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096`，健康正常且 Evolution 关闭。服务器证据目录 `audit/hc016-shadow-20260727T120000Z` 的清单 SHA-256 为 `5d44a24a20dd77573ce542004dacb417337294e511a5cdd4eee9e83340b37d07`；详见 [HC‑016 部署后验证记录](production-release-2026-07-27-hc016.md)。

### HC-023 Guide 冻结预览与稳定 receipt 后续实施证据

- 状态（2026-07-27）：`production-deployed-artifacts-observed / deterministic-verified / cookie-router-replica-set-verified`。Protocol v3 冻结 Contact/Memory/Playbook/Domain 直接基线与验证后的写计划；candidate hash 绑定 workspace/account/contact/plan。Apply 用身份、hash、lease token 与各对象 CAS；事务 guard 让无业务变更的依赖对象也参与并发冲突。
- 服务端从计划生成 authoritative diff、影响 scope 与强确认要求；前端只展示 authoritative diff。标签写权威 manual 层，未知建议字段进入 skipped。共享 Playbook/workspace runtime 缺二次确认、scope 不一致、旧 Preview 或 hash 不一致均在 claim 前失败。
- 业务写、审计、Preview 终态与 receipt 同事务提交；成功只返回稳定 receipt，详情另行刷新。已提交重放重新验证 plan/hash/scope/receipt 后返回同一 receipt，不重复应用。
- 部署后验证：六份生产/前端文件与正式 release 逐字一致；release 测试文件虽因后续其它用例与本地整文件哈希不同，但 Guide 红线函数块 418 行逐字一致，SHA-256 同为 `a3cac908ded91e51849079e943e7616e64d7919b204b25bc05ff92a637e4cd85`。服务器真实 `wa_session` Cookie Router+`rs0` 精确红线 1/1，证明错账号、错 hash、缺强确认均在 claim 前完整零写；审计写故障使 Contact/Memory/Playbook/Preview 整体回滚，修正后成功提交，同 hash 重放返回逐字相同稳定 receipt 且不重复升版。前端 3 文件 37/37。
- 证据卫生：测试库 126→127→126，本轮唯一随机库已按差集删除；正式 PID `2021387`、重启数 0、运行中/磁盘 SHA-256 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096` 与健康状态三次快照一致，Evolution 关闭。证据目录 `audit/hc023-guide-20260727T173000Z`，最终清单 SHA-256 `ce1fc888297e828012a9f362ab4da8cfe34e698f7a6e88ee454239503d9d9d7f`。真实管理员浏览器跨账号、强确认与迟到响应交互仍待复验；本用例不结算 SR-094 的专用 runtime 参数动态写入。

### HC-018 Prompt 不可变版本后续实施证据

- 覆盖边界：本批只结算 SR-055，不自动结算 SR-069、SR-070、SR-071、SR-138、SR-139、SR-151，也不把显式 system-pack reset 的销毁语义纳入本条。
- 状态：`production-deployed / deterministic-verified / targeted-upgrade-verified / broader-dynamic-regression-pending`（2026-07-25）。Prompt 内容改为 append-only 版本流；POST/PUT 只追加 draft，PUT 返回新版本 id，手工发布在 Mongo 事务内归档旧 current 并提升目标，历史 system/manual/evolution 行均保留。运行时、版本审计和 contact 读取只认唯一 `current_version=true + status=active`，已有历史却缺 current、多个 current、current 非 active 或残留非 current active 均 fail-closed，不再按 contact/locale 对多个 active Prompt 隐式分桶。
- 生产接线：共享 `prompt_template_versions` 负责并发安全的 `max(version)+1` 追加、首版发布、事务指针切换和严格 current 读取；启动 spec 对齐复用同一追加/发布协议，当前人工或 Evolution 版本不被覆盖，旧 Evolution 历史不会永久阻断后续系统对齐。Evolution 发布/回滚同步维护 `status` 与 `current_version`；m043 在索引前全表验证 scope/version/status/current，歧义数据首写前失败，合法旧多-active 流只归档非 current 行且不删历史。partial unique 索引继续锁住每 `(workspace_id,prompt_key)` 唯一 current。
- 前端接力：保存成功后 `editingPromptId` 必须切到 PUT 返回的新 draft id；待人工确认/语义拒绝保留来源 id 以便 force 重试，普通异常或成功响应缺 id 时清空旧发布目标，避免“保存后发布”误发旧版本。
- 验证：既有格式、编译、Prompt 定向单测和前端验证保持有效；历史完整 Mongo publish/rollback 动态矩阵仍未运行，不据此宣称通过。2026-07-25 新增 m049，以独立 migration id 复用 m043 全表 validation-before-write，覆盖旧 m043 marker 已存在而 planning-only draft 仍带 current 的升级库。副本集专用回归 1/1、真实克隆库联合冒烟和生产切换均通过；生产 `group.policy`、`moment.policy` 保持 draft 且 `current_version=false`，详见 [生产发布证据](production-release-2026-07-25.md)。

### HC-018 SR-138/SR-139 Prompt reset 与语义审查后续实施证据

- 覆盖边界：本批只结算 SR-138 的自动探测与误触安全边界、SR-139 的语义审查可见性；不回写 SR-055 的既有部署结论，不把显式 reset 改造成跨集合事务或快照系统，也不关闭整个 HC-018。
- 状态：`production-deployed / deterministic-verified / post-deploy-reset-rejection-verified / real-model-success-blocked`（2026-07-28）。`ensure_prompt_pack_v2` 只有明确 `Ok(None)` 才授权空库 bootstrap；任何 Mongo 探测错误原样返回并在首写前停止。显式 reset 请求使用 `deny_unknown_fields` DTO 且必须精确携带 `RESET PROMPT PACK`，前端危险确认框列明会替换 Prompt、Playbook、Domain Config 并重新绑定纳管联系人，取消零请求、未输入短语不能提交。
- SR-139 硬门：语义审查不再使用 new-only 行集合，而是对除 CRLF 等价外的每次变化提交完整规范化 `BEFORE/AFTER`，因此纯删除、行内改写、重排与重复行删减都不能得到空 diff。`user.reply.system`、`user.reply.task` 增加最小运行契约锚；`management.prompt_redline_review.system` 与 Evolution critic 同级禁止自然语言 create/update/publish，自身不能用同一个 judge 削弱自己。内置 judge 契约同步要求同时审查删除与新增。
- 验证：Prompt Guard 23/23、Prompt probe/内置契约 3/3、前端 reset/store 专项 24/24、前端生产构建、Rust 格式与 scoped diff 门通过。Wave1 正式 `wa_session` Cookie Router 对缺 body、错短语、未知字段三类 reset 请求全部拒绝，Prompt、Soul、Playbook、Domain Config 治理快照逐字零变化；正式运行制品、内外健康与 69 项静态资源通过复验。
- 模型边界：SR-139 的正式/隔离 Router 模型路径已串行触达并在失败时安全降级，但四个现有配置分别遇到 Cloudflare 530/1016、120 秒超时、无成功审查日志与 DashScope `Arrearage`。因此本批不记成功真实模型判定，也没有切换正式 active provider。
- 未完成边界：显式 reset 仍会顺序替换 Prompt/Playbook/Domain Config，尚无跨集合事务、恢复快照或 durable saga；生产未执行破坏性成功 reset。恢复外部模型可用性后仍需补一条成功的完整 `BEFORE/AFTER` 语义审查。详见 [Wave1 生产发布记录](production-release-2026-07-28-wave1.md)，冻结清单 SHA-256 `d16d0ce7f529c279fee16374107eeba990921b6f867135a74a5370a8f62b677a`。

### HC-007 运行与用量审计后续实施证据

- 覆盖边界：本批补齐 HC-007 的 Rust 链接级验证，并修正 LLM usage 缺失时的事实表达；不把库单测外推为随机 Mongo 集成、真实模型、部署后回归或整个 HC-007 已关闭。
- 状态：`working-tree-wired / rust-lib-verified / frontend-verified / mongo-integration-pending / deployment-pending`（2026-07-21）。`tokens_used` 只累计上游明确报告值，`unknown_usage_calls` 独立审计；未知用量不伪装成 0、不构造估算，也不把已生成回复误判为硬预算超额。知识循环与批量校验等可安全停止的追加调用在用量未知后停止，并以 `usage_unknown` 留痕。
- 验证：本地 `cargo fmt --all -- --check`、`cargo check --lib`、三个相关集成目标 `--no-run`、前端定向 28/28 与生产构建通过。Windows 在工作区和两个全新 target 中均被系统策略以 build-script `Access denied (os error 5)` 阻断；授权 Linux 服务器随后在独立 `/tmp` 源码、独立 target、无 `.env`/业务数据条件下运行完整 `cargo test --lib`，最终 `2116 passed; 0 failed`。
- 服务器隔离证明：测试前后 `/opt/wechatagent` 无 tracked 修改，服务始终为原 PID `1144911` 且 `/api/health` 正常；未重启服务、未修改生产仓库、未连接业务数据库。本批 3.5 GB 临时验证目录与三个归档仍保留在 `/tmp`，等待明确清理授权。
- 未完成边界：随机 Mongo 集成、真实外部模型/链路、正式测试服务器部署与部署后回归仍未执行；这些证据不得由 `2116/2116` 库单测替代。

### HC-008 LLM 精确缓存 provider 代际后续实施证据

- 覆盖边界：本批只结算 SR-020，不改变 provider 发布流程、第二 Reviewer 或非白名单调用，也不引入分布式缓存。
- 状态：`working-tree-wired / rust-typed / isolated-linux-verified / deployment-pending`（2026-07-21）。精确缓存键现同时包含 workspace、provider id、model、Registry 单调 generation、Prompt pack version 及 system/user 内容哈希；任一身份变化都不能命中旧条目。
- 并发语义：每个请求在缓存查找前从 `LlmRegistry` 取得不可变快照，缓存身份与 cache miss 的真实调用使用同一 client/meta/generation；热切换前已取得快照的请求继续使用旧 client，之后的请求使用新 client。每次 swap（包括同配置重新加载）都递增 generation，避免 API key、base URL、format、timeout 等未直接写进 key 的配置变化复用旧结果。
- 验证：`cargo check --lib`、`cargo check --tests`、`cargo fmt --all -- --check` 与定向差异检查通过。授权 Linux 隔离目录低内存运行缓存身份 5/5、Registry 快照/代际 1/1；测试后生产服务仍为原 PID `1144911`、健康正常，`/opt/wechatagent` 无 tracked 修改。
- 未完成边界：当前工作树尚未部署，未执行部署后的真实 provider 热切回归；这些不由单元测试替代。

### HC-009 实施证据

- 覆盖 SR-022、SR-023、SR-031、SR-042、SR-051、SR-175；六条仍各自保留原始业务结论和最小方案，仅共享同一发送前 fail-closed 决策面。
- 状态：`production-wired / deterministic-verified / real-model-capacity-blocked`（2026-07-25）。生产入口已接线；SR-175 的实时 Reviewer 严格 wire、必审不变量与 fail-closed 收口已进入工作树，部署与真实模型复验仍待执行。
- 核心证据：任何 `should_reply=true` 的正文都必须执行独立 Reviewer；Reply Agent 自报 `needs_review=false`、低风险或高 confidence 只能影响 light/full 深度，不能跳过审核。默认销售域的低风险正文走 light Reviewer，高敏域强制 full Reviewer；预算耗尽或意外进入本地兜底时，正文分别收敛为 `blocked_by_budget` 或可审计 `blocked_by_safety_guard`，只有 `should_reply=false` 的主动沉默可本地完成。实时 Reviewer 要求 `approved`、六个发送闸评分和 `requiresProductKnowledge` 完整且类型正确；评分必须是 0..10 整数，每组 alias/canonical 恰好出现一个。缺失、畸形、越界或歧义均按 `valid|missing|invalid` 留审计并 safety hold；主 Reviewer 与已启用的第二 Reviewer 共用该门，实时明确的 pressure/boundary 零分不再借历史缺字段兼容放行。历史 `DecisionReviewResult` 宽松读取保持兼容。独立语义 Claim Gate 失败即 hold；安全类 revision 不恢复危险原稿；Knowledge 引用验证 opened/verified/relationRole/quote/anchor；holding reply 经独立语义审查、全场景数字授权校验并入 Outbox。SR-051 的每条目录声明必须携带最终回复中的完整 clause `sourceQuote`；服务端逐字段核验 productId/name/amountMinor/currency/SKU，并反向要求最终回复中每个 active 产品名/SKU clause 都有对应声明。
- SR-175 本地验证：严格 wire 6/6、必审契约 5/5、完整 `agent::review` 107/107、`cargo check --tests`、rustfmt 与 scoped diff 均通过。覆盖默认销售域与高敏域不可自我豁免、默认 low-risk 选择 light / 高敏域选择 full、本地兜底不批准正文、主动沉默兼容、主/第二 Reviewer schema fail-closed、历史零值兼容和实时零值拦截。更新后的预算 PBT 源码通过 `cargo check --test autonomy_protocol_pbt`；动态 PBT 在 Windows 测试二进制链接阶段超过 15 分钟，断言未启动，遗留 Cargo/rustc/link 进程已按本次 PID 链定向清理，故不记为测试失败或通过。
- 未完成验证：2026-07-21 授权服务器只读盘点证实生产实际文本端点已改为 NVIDIA HTTPS；`/models` 鉴权返回 200（118 个模型，约 40 秒）。隔离的六轮知识编造弧运行 30 分钟未完成；缩小后的单轮 T6 在 9 次重试后收到 `503 ResourceExhausted` 并按测试协议 transient-skip，业务断言未执行。随机测试库、隔离进程和临时运行状态均已清理，生产服务原 PID/二进制/仓库状态不变且健康 200。该结果只证明端点鉴权可达，不证明 HC-009 真模型行为；容量恢复后须重跑 T6 并机械禁止 skip，方可改为 fully-verified。

### HC-010 / HC-032 发送子集实施证据

- 覆盖边界：本批只结算 SR-052（名片不确定送达不得自动重放）与 SR-066（in-flight 取消的最后可取消点和真实送达语义），并为 SR-004/HC-032 增加一个独立严格门。SR-028、SR-034、SR-050、SR-126、SR-128、SR-172、SR-174、SR-176、SR-177、SR-178 均未因本批自动结算。
- 状态：`production-wired / deterministic-verified / isolated-server-mongo-redline-verified / real-model-run-pending / deployment-pending`（2026-07-26）。Outbox claim 持久化不可复用 `claim_token` 与 `claim_generation`；MCP 前以 CAS 写 `send_started_at` 并拒绝已登记取消的 claim；跨过远端边界后，成功回执收敛为 `sent`，无法核验则进入 `delivery_unknown` 且禁止自动重放；名片无权威查询时，崩溃回收直接进入 `delivery_unknown`。共享发送回执改为三态：`ok:true`/非空 `newMsgId` 才是成功，纯 `ok:false` 才是明确失败，空、畸形或互相冲突的成功信封均为不确定送达。
- 确定性证据设计：`tests/outbox_integration.rs` 的五条 `delivery_redline_*` 经过 `enqueue -> atomic_claim_pending -> process_entry/reclaim_expired_leases`。两条异步 HTTP barrier 在 MCP 已完整收到 `message_send_text/message_send_namecard`、尚未回包时分别注入取消和 worker 崩溃；其余覆盖发送前取消零调用、HTTP 歧义结果不重放，以及名片收到缺少 `ok/newMsgId` 的成功 HTTP 信封后进入 `delivery_unknown`。共享回执三态纯单测 1/1 通过；最新 97 MB 集成测试二进制链接成功并枚举 46 项，五条红线均被发现；格式与 scoped diff 通过。
- 真实业务门：`.github/workflows/ci.yml` 的手动 `smoke_t4` 严格 job 无 `continue-on-error`，机械断言 `delivery_redline_*` 恰好五条并全部运行，再单独运行真实模型 T4；T4 要求两轮非空、承接上下文的单问题回复，Reviewer 分数过运行阈值且无残留风险，明确停止被 Reaction 高置信识别，第二轮待发在 MCP 前取消，并把 run/review/reaction/outbox/MCP JSON-RPC 完整轨迹先写 `t4_business_transcript.json` 再执行质量断言。
- 服务器动态补证（2026-07-26）：授权服务器 `rs0`、随机 `wechatagent_test_*` 数据库和仅回环 MCP mock 中，机械枚举的五条 `delivery_redline_*` 全部真实执行，结果 `5 passed; 0 failed`。首轮 1/5 暴露旧夹具没有注册 `(workspace, account)`；只补种测试账号，不放宽生产 MCP fail-closed 门后完整重跑 5/5。诊断库和最终状态只读投影、源码/二进制哈希、systemd 日志及生产零漂移基线固化于 `audit/server-backfill-batch2-20260726T1547Z`，清单 SHA-256 `aa6f91f0521dd5ffbbcef636122fcdd3994f7d4b5c1fa04e776e77cca71e08c7`。真实模型 T4、GitHub `smoke_t4`、工作树部署及部署后回归仍未执行，不得由本批外推。

### HC-004 / HC-010 Task 与 Outcome 后续实施证据

- 覆盖边界：本批只结算 SR-034（Task 所有权/取消 fencing）与 SR-036（Outcome task 跨租户去重）；不自动结算 HC-004、HC-010 下的 SR-028、SR-050、SR-177 等其它问题，也不把隔离测试解释为已部署。
- 状态：`working-tree-wired / deterministic-verified / isolated-real-mongo-verified / deployment-pending`（2026-07-19）。Task claim 原子写入不可复用 token 与单调 generation，终态、Outbox 绑定/授权、prepared commit 与 stale reclaim 均以当前 owner CAS 收敛；管理员取消在同一 Task 文档上竞争，旧 worker 不能取得发送授权。Memory consolidation 以 contact-scoped single-flight key、`rerun_requested` 交接和可重放 commit marker 收敛候选到达/终结/OCC 竞争。
- SR-036：Outcome partial unique key 已改为 `(workspace_id, kind, account_id, content)`；m017 历史清理按 `(workspace_id, account_id, content)` 分组，避免升级时误删另一 workspace 的合法任务；m033 只在旧索引存在时删除旧规格，统一索引入口再创建租户作用域新索引。
- 本地确定性验证：索引契约 5/5、Memory OCC/rerun fencing 11/11、Task claim/owner 10/10、m017 workspace 分组 1/1、迁移顺序/唯一性 2/2、`cargo check --lib`、`cargo fmt --check` 与 `git diff --check` 均通过。`tests/sr034_task_send_fencing.rs` 与 `tests/outcome_task_workspace_dedupe.rs` 均已在 Windows 以 `--jobs 1 --no-run` 成功编译。
- 真实 Mongo 验证：在授权测试服务器的独立 `/tmp` 源码与 target 中，以 `TEST_MONGODB_URI=mongodb://127.0.0.1:27017` 和随机 `wechatagent_test_*` 库单线程运行 SR-034 四条生产 helper 红线，4/4 通过：building decision 零远端发送、stale claim 取消且零远端发送、同一 owner 恰好一次发送、pending/in-flight 取消均在远端边界前终止。SR-036 另以完整迁移和索引初始化后的随机库运行 1/1 通过：不同 workspace 的相同 account/content 可各保留一行，同 workspace 重复写命中 Mongo duplicate-key，最终恰有两行。两批测试前后该前缀数据库清单均为空；临时源码、归档与独立 target 均已删除。
- 服务器非部署证明：运行服务 PID、release 二进制 SHA-256 在测试前后完全一致，`/api/health` 返回 200；本批没有修改 `/opt/wechatagent`、重启服务或连接业务数据库。正式测试服务器部署与部署后回归仍待后续上线批次完成。
- 部署后结算（2026-07-29）：上述段落保留 2026-07-19 当时的非部署语境；SR-036 现已进入正式 ELF `efe5e1e1…baf4`，并在部署后以同源测试 ELF `acc2511e…824f` 对授权服务器 `rs0` 随机库重跑生产索引协议 1/1。跨 workspace 的相同 account/content 各保留一行，同 workspace 重复写仍由 Mongo unique key 拒绝；所有 RC 为 0，UUID 测试库与测试进程零残留，正式 PID `2264975`、`NRestarts=0`、磁盘/进程 ELF 与健康响应前后不变。与 SR-124 的联合证据归档 SHA-256 `784e141c…2735`。

### HC-010 Reaction fencing 后续实施证据

- 覆盖边界：本批只结算 SR-028，不自动结算 SR-050、SR-172、SR-177 或 HC-010 其它发送问题。
- 状态：`working-tree-wired / deterministic-compiled / isolated-real-mongo-verified / deployment-pending`（2026-07-19）。Reaction claim 使用不可复用 token 与单调 generation；并发入口不能偷取活动 claim，超时重领后旧 owner 的结果 CAS 失败，轨迹、负例与 Outbox 取消等副作用只在当前 owner 成功提交后执行。
- 验证：`tests/reaction_claim_lock.rs` 在 Windows 以 `--jobs 1 --no-run` 编译通过；授权测试服务器以独立 `/tmp` 源码/target 和随机 `wechatagent_test_*` 库运行 2/2 通过，覆盖活动 claim 仅一次 LLM 调用，以及 ABA 重领后旧 stop 结果不能覆盖新结果、追加轨迹或取消 pending Outbox。测试库零残留，临时源码/归档/target 已删除；运行服务 PID 与 release SHA-256 前后不变，`/api/health` 为 200，未部署工作树版本。

### HC-010 发送台账完整业务键后续实施证据

- 覆盖边界：本批只结算 SR-050，不自动结算 SR-052、SR-066、SR-172、SR-177 或 HC-010 其它发送问题。
- 状态：`working-tree-wired / deterministic-verified / isolated-real-mongo-verified / deployment-pending`（2026-07-19）。新台账行保存不可变 `outbox_id`，以 `$setOnInsert` upsert；partial unique 索引只约束带 ObjectId 锚的新行，历史无锚行保持可读。近期发送、响应回扫、阶段回扫、只读 API、管理工具和前端统计均固定 `(workspace_id, account_id)`，缺少 `accountId` 的 API 请求 fail-closed。
- 本地验证：台账/索引/路由/模型契约 18/18、发送历史前端测试 2/2、前端生产构建、`cargo fmt --check` 与 `git diff --check` 均通过。Windows 已生成并成功列出 `send_ledger_integration` 测试二进制；随后 Cargo 为应用主二进制重复链接时因本机 E: 盘仅余约 9 MB 报 `no space on device`，该环境失败不解释为代码失败。
- 真实 Mongo 验证：授权测试服务器在独立 `/tmp` 源码/target 中离线单作业构建成功（RC 0），以 `TEST_MONGODB_URI=mongodb://127.0.0.1:27017` 和随机 `wechatagent_test_*` 库运行三条 `sr050_*` 生产 helper 红线，3/3 通过：同一 Outbox 重放仍恰有一条台账且不能改归另一账号；同 workspace、同 wxid 的近期发送按账号隔离；另一账号的入站回复和阶段推进不能归因到当前账号台账。测试库、源码、归档、target 与启动日志均零残留；运行服务 PID `727743`、release SHA-256 `ad45f055063def7c5f7adbfdf64ab68a53cdbb949c9d6aadc918fd65f5434615` 前后不变，`/api/health` 为 200，未部署工作树版本。

### HC-010 Outbox 运营对象投影后续实施证据

- 覆盖边界：本批只结算 SR-172，不自动结算 SR-052、SR-066、SR-159、SR-177 或 HC-010 其它发送问题。
- 状态：`working-tree-wired / deterministic-verified / local-real-router-mongo-verified / deployment-pending`（2026-07-19）。管理列表投影互斥 typed payload（`text | media | referralCard`），保留不可变素材/名片 id，并按页最多两次批量回表补可读名称；目标不存在或 id 非 ObjectId 时保留 id 回退。双目标异常行显式标 `invalid`，不静默选一类。元数据查询固定 workspace，账号私有目标只有与 Outbox `account_id` 一致时才下发名称，workspace 共享目标仍可解析。
- 前端与取消语义：发件箱按类型显示正文、素材名/文件名或顾问名/目标 wxid及不可变 id；同联系人多行不再混淆。取消前确认框展示业务号、客户、精确发送对象和恢复风险；pending 才提示可停止，in-flight 只提交取消请求，已跨发送边界或曾回收的条目明确可能已经送达且取消不能撤回。`delivery_unknown` 与 `cancel_requested` 继续使用既有状态机，不为显示层另建状态。
- 验证：后端纯函数覆盖 text/media/referralCard/invalid、缺失元数据 id 回退和跨账号私有名称隔离；共享后端 fixture 与前端 canonical key/typed payload 对账。前端组件与契约测试 9/9、生产 `tsc && vite build`、`cargo fmt --check` 与定向 `git diff --check` 均通过。新增 `tests/sr172_outbox_projection.rs` 经真实 `wa_session` Cookie 调生产 `api_router`，在本机 `127.0.0.1:27017` 随机 `wechatagent_test_*` 库运行 1/1 通过（36.18 秒）：同一客户的文本、同账号素材、名片均返回正确 typed payload和 id，恢复计数保留；账号 A 引用账号 B 私有素材时只保留 id、不泄漏名称，`accountId` 过滤排除账号 B 行。测试前后随机库清单均为空。
- 未完成边界：本批未部署工作树版本，也未修改或重启测试服务器服务；远端上传因当前进程无法恢复 `DEPLOY_PASS` 而未发生。本地真实 Router + 随机 Mongo 证据不等同于部署后回归，正式部署仍待后续上线批次。

### HC-010 Durable inbound handoff 后续实施证据

- 覆盖边界：本批只结算 SR-177，不自动结算 SR-052、SR-066、SR-159 或 HC-010 其它发送问题。
- 状态：`working-tree-wired / deterministic-verified / isolated-real-mongo-verified / deployment-pending`。Webhook 在 ACK 前把入站事实与 `handoff_status=pending` 同文档持久化；普通路径随后物化每 `(workspace_id, account_id, contact_wxid)` 唯一的可复用 `inbound_reply` task，worker tick 会恢复扫描未完成 handoff。即时唤醒和周期 worker 共用 `run_due_task_by_id`，claim 时重新核对状态与到期时间，避免游标旧快照提前执行被新消息延后的任务。
- 所有权与发送边界：后到消息按 `(created_at, _id)` 单调刷新同一 task，清除旧 claim/outbox binding；ObjectId 只作同毫秒 tie-breaker。执行器按 task 中的消息 ObjectId 和完整租户三元组恢复真实 Inbound，100ms 监视器用于尽早中止，并在画像/记忆首写前直接复核 `_id + running + claim_token + claim_generation`。主回复和非文本确定性过渡均走 decision bind→enqueue→authorize；Dispatcher 在 MCP 前复核同一 token。若崩溃发生在 enqueue 与 authorize 之间，新 owner 只能接管同一 task 旧 generation 留下的 `attempt=0`、无授权 marker、未跨 `send_started_at`、非人工取消 Outbox；接管后仍须以当前 token 完成 Task CAS。安全拦截后的兜底占位在 task 已终态时 fail-closed，不创建 `decision_id=None` 的旁路 Outbox。
- 验证：`cargo check --test sr177_durable_inbound_handoff`、全仓 `cargo fmt --check` 与定向 `git diff --check` 通过。`tests/sr177_durable_inbound_handoff.rs` 在本机独立回环 MongoDB 8.0、测试夹具 UUID 随机库中以单线程运行 3/3 通过：一是模拟消息已落库但 task 尚未物化的崩溃窗，恢复扫描幂等地产生恰好一条 durable task；二是故意构造时间更晚但 ObjectId 更小的消息，证明其仍刷新同一 task、旧 claim 不能 authorize，旧 Outbox 经生产 Dispatcher 收敛为 `canceled/stale_task_claim`；三是模拟 Outbox 已写但 Task 未授权即崩溃，证明新 claim 严格接管后经生产 Dispatcher 恰好一次调用 WireMock MCP，而人工取消及已写 `send_started_at` 的行不被复活。失败诊断遗留的随机测试库已删除，最终 `wechatagent_test_*` 为零；临时 mongod PID 已关闭，Windows MongoDB 服务保持测试前后的 `Stopped`。工作区 `target` 临时 dbpath 因执行策略阻止递归删除而保留，不含业务库。
- 未完成边界：工作树版本尚未部署到测试服务器，也未做部署后真实 Webhook/LLM/MCP 业务回归；本地隔离 Mongo 通过不等同于上线完成。

### HC-020 Management 副作用协议后续实施证据

- 覆盖边界：本批结算 SR-062、SR-063、SR-064、SR-065、SR-143；不自动结算 Campaign、知识修复、Provider 生命周期等被 Management 调用的下游业务自身缺陷。
- 状态（2026-07-27）：`production-deployed-artifacts-observed / deterministic-verified / post-deploy-isolated-router-mongo-mcp-verified`。动态 MCP 目录递归剔除全部 `message_send_*`，执行兜底也拒绝原始发送工具；合法主动发送只走 `wechatagent.send_contact_message`→生产 Gateway/Review/Outbox。代码层把所有真实副作用默认置于确认门后，仅只读工具、`media_get` 与 provider 连通测试可直接执行，未知工具 fail-closed。
- 冻结与恢复协议：命令持久化 `workspace_id + account_id + plan_hash`，确认/否决请求必须回传同一 account/hash；前端切号清除旧计划并拒绝迟到响应。每个工具调用先以 `(workspace_id, account_id, intent_key)` partial unique 落 `prepared` intent，再 CAS 为 `executing`；终态可重读，崩溃遗留的 `executing` 只收敛为 `execution_unknown` 并停止后续工具，绝不自动重放未知副作用。命令 lease/token 与 finalize CAS 防止并发 owner 覆盖。`write_deal_events` 只有确认端点传入真实 `AuthenticatedAdmin` 才可执行，`staff_confirmed.marked_by` 固定为认证用户名，LLM 只起草冻结参数。
- 部署后验证（2026-07-27）：当前正式 release 的后端、测试与 Command Center 两份前端源码均与本地逐字一致，运行中二进制含 `execution_unknown` 保守恢复协议，正式静态制品含 `planHash` 与结果未知提示。服务器以真实 `wa_session` Cookie middleware、生产 `api_router`、随机 MongoDB、生产计划入口及回环 MCP 运行 2/2：low/无需确认写计划仍停在 `pending_confirmation`，错账号/错 hash 均 409 且零写，正确确认只写一条 `staff_confirmed` 且 actor 为认证管理员；stale `running + executing` 收敛为 tool/command `execution_unknown`，联系人零写且 `tools/call=0`。前端 Command Center 专项 14/14。
- 证据与边界：测试库 126→126、差集为空；正式 PID `2021387`、重启数 0、运行中/磁盘 SHA-256 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096` 与健康状态均不变。成功证据目录 `audit/hc020-management-20260727T160500Z`，清单 SHA-256 `25b0ead03b764c73cdfa4be1b2454a6812b52fe104353d68782f281e500e4c20`。此前两次包装器尝试分别在 rustc 探测和 `/run` noexec 阶段退出，均未进入业务测试或创建随机库。尚未以真实外部 MCP 目录和远端副作用执行本协议，也未完成真实管理员浏览器切号或杀进程级崩溃恢复演练；本批不关闭 Campaign、知识修复、Provider 等下游业务自身缺陷。

### HC-021 Campaign 冻结规格与可恢复扇出部署后证据

- 状态（2026-07-27）：`production-deployed-artifacts-observed / deterministic-verified / real-handler-mongo-verified`。正式 release 的 Campaign Handler、集成目标、Store 与创建页四份源码和本地逐字一致，运行制品含 specVersion/specHash、dispatching 与 prepared intent 协议。
- 动态验证：服务器真实 Handler/Mongo 完整目标 11/11。覆盖终态 preview 完整 BSON 零写、草稿 PATCH 后按新规格预览、正常派发与重复拒绝、prepared send intent 在 task insert 故障后由 reconciler 恢复、受众上下限、lastDispatchTargetCount 与 workspace/account 隔离。前端 Campaign 定向 33/33。
- 证据卫生：测试库 126→136→126，10 个包装器清理库全部删除，另 1 条用例自清理；正式 PID `2021387`、重启数 0、运行中/磁盘 SHA-256 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096` 与健康状态不变，Evolution 关闭。证据目录 `audit/hc021-campaign-20260727T163000Z` 的清单 SHA-256 为 `bb99b1c1a991a3b93230782ae04a897b2440d2500e718859436a4a1bd796669a`。
- 未完成边界：真实 worker 并发、杀进程级崩溃恢复、Management 确认链和浏览器导出/打开仍待业务复验；同进程 validator 故障与 reconciler 恢复不外推为进程崩溃已验证。

### HC-022 联系人导入、纳管与画像代际部署后证据

- 状态（2026-07-27）：`production-deployed-artifacts-observed / deterministic-verified / real-handler-mongo-verified`。当前正式 release 的联系人 Handler、共享导入 helper、完整集成目标及五份前端源码与本地逐字一致；运行中后端含 `initial_profile_enrollment` reconciler、enrollment token 与 task claim generation fencing。
- 动态验证：服务器当前完整 `contacts_batch_enable` 目标机械计数与实际输出均为 13/13。覆盖稀疏导入保值、非真人与错账号候选完整零写、任务插入故障不留下 managed 半提交、并发单飞、代际旋转后同请求重启、幂等纳管，以及联系人缺失/已取消纳管时旧画像任务安全终结。前端 Roster、联系人视图与 Contact Store 专项 22/22。
- 证据卫生：测试库 126→139→126，13 个本轮随机库全部按差集删除，无新增或误删；正式 PID `2021387`、重启数 0、运行中/磁盘 SHA-256 `dabddf043a717c0c172d3bd9722b1f4b4975f544d201ef4f852228b926089096` 与健康状态不变，Evolution 关闭。证据目录 `audit/hc022-contacts-20260727T170000Z` 的最终清单 SHA-256 为 `d895a5bd26020001f71fd28143ad2e8148fb6a6fd4f058d6171b959492e45c1a`。
- 未完成边界：真实生产 worker 杀进程级恢复与认证管理员浏览器业务复验仍待执行；同进程 validator 故障、reconciler 与 Handler 动态证据不外推为进程崩溃已验证。

### HC-027 SSE 连续失败预算后续实施证据

- 覆盖边界：本批只结算 SR-173，不自动结算 SR-129、SR-130、SR-132、SR-141 或 HC-027 其它 Knowledge Cockpit 问题。
- 状态：`production-deployed / deterministic-verified / production-identical-browser-fault-verified`（2026-07-28）。共享 SSE 重连器把原生 `open` 与业务事件都视为连接恢复，只对连续未成功建连计数；已替换 EventSource 的迟到事件被隔离，terminal `close` 仍立即停止且不重连。当前正式 69 项静态制品逐项与 Wave1 冻结清单一致，且已包含该实现，无需重复部署。
- 任务事实收敛：TaskRail 消费 reconnect/gave-up 状态；每个 `turn` 都回读 `/api/knowledge/chat/tasks/:id` 更新权威进度，任务 id 与 snapshot generation 阻止迟到响应覆盖新任务。gave-up 或浏览器无 EventSource 时立即进入 5 秒一次、最多 12 次的有限轮询；completed/failed/cancelled、切任务或卸载都会停止 timer，耗尽后明确提示手工“拉取”。未更换 HTTP+SSE 协议，也未建立永久轮询。
- 验证：共享重连器与 TaskRail 专项 13/13 通过，覆盖多轮 `open -> error` 不耗尽预算、连续未 open 才 gave-up、旧连接迟到事件隔离、terminal close 零重连、turn 回读主卡、gave-up 轮询收敛终态、无 EventSource 时严格止于 12 次并停止请求；Knowledge 相关回归 18/18、前端生产 `tsc && vite build` 与定向 `git diff --check` 均通过。真实 Chrome 150 在生产同构制品上连续制造 7 次 EventSource 建连失败，TaskRail 仅作 2 次权威详情读取即收敛到 `completed / 1/1`，终态后额外轮询为 0。
- 完成边界：浏览器探针以硬网络隔离和契约形状的内存 API运行，`productionRequests=0`；它证明已部署前端的传输故障状态机，不冒充生产数据库写入。最终裁定与制品清单见 `audit/hc027-final/final-adjudication.json`。

### HC-027 SR-129/SR-130 DTO 与冻结对象后续实施证据

- 覆盖边界：本批只结算 SR-129、SR-130，不自动结算 SR-132、SR-141、SR-173，也不关闭 HC-027。
- 状态：`production-deployed / deterministic-verified / replica-set-occ-verified / post-deploy-smoke-verified`（2026-07-27）。Auto Verify 请求统一为 camelCase，后端 DTO 拒绝未知字段；Review Chat 冻结账号、Chunk、`updated_at` 与 `operation=update`，只消费服务端顶层权威候选包络。
- 并发边界：Apply 在同一 Mongo 事务中以冻结 `updated_at` 对目标 Chunk 做 OCC；陈旧快照返回 `chat_chunk_snapshot_stale`，Chunk 完整 BSON、revision、`knowledge_chat_applied` 审计和 assistant turn receipt 均零写。授权服务器真实副本集精确枚举 1 条并执行 1/1，测试库集合与正式运行态前后一致；证据清单 SHA-256 `3990137e9c7687982d344cbf045f32b44cf333a736c005a50b942eff83702001`。
- 发布边界：候选从已部署源码基线重放且机械限制为 3 个后端、2 个前端生产文件；后端 release 与前端 production build 通过。候选清单 SHA-256 `c7fcd3b444c716d3475446018a23716f23d47bef8fc5c11a721dedfecea6a109`，严格候选冒烟清单 `3eeb3e7c0f8f0c70e16a88ca90af3b62970eb043882bf9fc16b536f3795bb07b`。
- 部署结果：正式后端从 `f4863fa4401ead96a2c8cecbcadfe6328474d5020d22b115269abf67196119e8` 切换为 `c98f24a34404cd39bc5427cdfe4c25af84a984fc820e27867cb75f772e7a54ab`，前端首页为 `9c60cad33e403b12478c6fea263e4e6b5ccd5cbd7253fc012f6385313d9c1b0f`；三次活动队列均为 0，migration 55 条逐字不变，PID `1942220`、零重启、Evolution 关闭。切换证据清单 `2d3377e78f7d1cbfbb9dbe739cbfc09db26991f6b3bd3f7862966ef524fe4ea7`；正式路径隔离冒烟双健康、69 项静态资源、随机目标库零变化并清理，清单 `a4c52d38d0b9593d44d3eaac843cbaac25d1a44f5402c977d50402f6f5d564ce`。详见 [生产发布记录](production-release-2026-07-27-sr129130.md)。

### HC-027 SR-132/SR-141 评审投影与实时失效后续实施证据

- 覆盖边界：本批只结算 SR-132、SR-141，不回写 SR-129、SR-130、SR-173 的既有部署结论，也不关闭整个 HC-027。
- 状态：`production-deployed / deterministic-verified / post-deploy-router-websocket-verified / production-identical-browser-lagged-verified`（2026-07-28）。新增服务端权威 `GET /api/operation-knowledge/review-queue`：固定当前 workspace、`domain=user_operations` 与未归档 `draft|active` 生命周期，返回可重叠 `reviewCategories`、逐类 `counts` 和实际 `effectiveFilter`。默认待初审草稿不再因只读 active 而不可达，archived 与外租户行不进入结果。
- 维度下钻：`dimension` 必须命中 active DomainProfile 的 coverage key；只对 `business_topics` 做规范化后的精确匹配，不扫描正文猜语义。`CoverageDimension.review_topic_aliases` 可在行业配置编辑，key、display name 与显式 alias 在整个 Profile 内规范化后全局唯一；未知维度 400，避免跨行业硬编码和一个主题同时命中多个维度。
- 实时收敛：WebSocket `lagged` 与 `revised` 统一广播 `wikiChunksInvalidated`。Inspector、知识树和评审队列收到后先清空旧快照，再以 generation fence 和单在途、至多一次尾随重拉恢复；失败持续显示错误，不继续展示陈旧事实。本地修复、关系解除、回滚等写入口也走同一失效事件；保留原细粒度 `wikiChunkRevised` 兼容事件，不更换 HTTP+WS 协议或引入前端数据平台。
- 验证：前端评审/失效专项 3/3、行业配置专项 20/20、生产 `tsc && vite build` 通过；Rust Profile 主题唯一性与 DEFAULT 五维兼容单测各 1/1。部署前 `tests/sr132_review_queue.rs` 真实 Cookie Router/随机 Mongo 1/1继续有效。Wave1 正式 Router 进一步验证 pricing 只返回 pricing 行、不混 capability 行，未知维度 400，外 workspace 队列为空；Chunk create/patch/lock/unlock 原始 WebSocket 事件均到达。正式后端 SHA-256 `5df573cf5aef14e5919e13157c5213d58e24219cc424098610fc5ee7f29a558b`、PID `2086752`、零重启，内外健康与 69 项静态资源通过，临时数据零残留。
- 完成边界：SR-132 的部署与正式 Router门已关闭；SR-141 又在与当前生产逐文件一致的 69 项制品上完成真实 Chrome/WebSocket `lagged` 注入。1 条浏览器 WebSocket 注入 2 次 `lagged`，旧快照在重拉完成前即撤下，503 后不回显，下一次失效恢复新快照，队列读取严格为 3 次。探针硬隔离且 `productionRequests=0`；详见 [Wave1 生产发布记录](production-release-2026-07-28-wave1.md)、冻结清单 SHA-256 `d16d0ce7f529c279fee16374107eeba990921b6f867135a74a5370a8f62b677a` 与 `audit/hc027-final/final-adjudication.json`。

### HC-032 进程级缓存数据库隔离后续实施证据

- 覆盖边界：本批只结算 SR-174，不自动结算 SR-004、SR-126、SR-128、SR-176、SR-178，也不把本地真实 Mongo 结果解释为已部署或整个 HC-032 已关闭。
- 状态：`working-tree-wired / deterministic-verified / local-real-mongo-verified / deployment-pending`（2026-07-19）。每个 `Database::connect` 获得进程内单调 identity；同一 `Database` 的 clone 共享 identity 与生命周期 token，独立连接即使使用相同 workspace id 也获得独立 Taxonomy/DomainProfile cache。缓存 registry 与 taxonomy 初始化 guard 均以数据库 identity 分片，并用 `Weak` 生命周期清理已释放测试数据库的进程内状态。
- 生产接线：所有 Taxonomy 读取、DomainProfile 读取及后台写后失效入口都显式传入当前 `Database`；失效只作用于该连接对应的缓存。保留现有 30 秒 TTL、HTTP/worker 调用关系与纯查表 API，不引入新缓存服务，也不依赖串行测试规避问题。
- 验证：registry 单测 1/1 证明同 identity 共享、不同 identity 隔离且失效不跨库；`tests/sr174_cache_database_isolation.rs` 使用两个随机 Mongo 数据库和相同 workspace，分别写入独有 profile/taxonomy，在 A、B 均预热后按 A→B、B→A 交错读取，生产 `load_active_domain_profile` 与 `normalize_target_stages` 始终只见本库数据；生命周期 guard 修正后的最终强化红线 1/1 通过（55.76 秒），测试前后 `wechatagent_test_*` 数据库清单为空。`cargo check --lib`、`cargo fmt --check` 与定向 `git diff --check` 均通过。
- 未完成边界：当前仍是未部署工作树版本；SR-176 已在后续独立批次补真实入口与专用 hard gate，SR-004 的其余 CI 分层、SR-126 的 Knowledge 真实入口闭环及 SR-128/178 的动态任务正向见证仍须分别修复和验证。

### HC-032 租户隔离真实入口证据后续实施证据

- 覆盖边界：本批只推进 SR-176，不自动结算 SR-004、SR-126、SR-128、SR-178，也不把三类代表性入口外推为全仓所有 CRUD 均已逐端点验证。
- 状态：`working-tree-wired / deterministic-verified / local-real-router-mongo-verified / hard-gate-wired / actions-run-pending / deployment-pending`（2026-07-19）。新增 `tests/sr176_real_route_isolation.rs` 以真实 `wa_session` Cookie 穿过 session middleware 和生产 `api_router`，不提高 Handler 可见性、不复制生产授权 helper。
- 真实入口矩阵：读取面以 workspace A 会话访问 workspace B Contact 得 404、本租户 Contact 得 200 且返回正确 wxid；写入面以 workspace A 会话更新 workspace B 独有 Product 得 404，目标名称保持不变，同时本租户同名 Product 更新成功且 workspace B 同名 Product 不变；认证面直接插入真实过期 `AdminSession`，`lookup_session` 返回 `SessionExpired`，同一 Cookie 请求 `/api/auth/me` 得 401，而有效 Cookie 得 200。
- 证据治理：`workspace_isolation.rs` 与 `products_workspace_isolation.rs` 的名称和注释已明确降级为 collection/filter/index 局部约束，不再声称覆盖未调用的 Handler；旧 auth 用例已改为真实过期记录并在断言前清理随机数据库。保留这些纯 DB 测试，不为每个 CRUD 复制完整矩阵。
- 验证：SR-176 真实 Router 红线 1/1 通过（23.22 秒）；修正后的真实过期 session 用例 1/1 通过（31.01 秒）；两条 CI 列表机械计数均恰好为 1；专项 `cargo check`、`cargo fmt --check` 与定向 `git diff --check` 通过；最终 `wechatagent_test_*` 清单为空。
- 合并门：`.github/workflows/ci.yml` 新增 `tenant-isolation-security` PR/push hard gate，无 `continue-on-error`，先机械要求两条目标测试各恰好存在一次，再只运行这两条 ignored 红线；全量 integration 继续作为 soft 诊断线。当前工作树尚未推送，故 GitHub Actions YAML/Ubuntu/testcontainers 真实运行仍待证；本机缺少 `actionlint` 与可用 YAML parser，已完成 job 边界、测试计数、命令列表和差异静态检查，但不得记为远程绿色。
- 未完成边界：当前未部署，也未修改或重启测试服务器服务；正式结算仍需授权分支上的 hard gate 真绿及统一部署后回归。HC-032 的 SR-004、SR-126、SR-128、SR-178 继续开放。

### HC-032 Knowledge 真实证据后续实施证据

- 覆盖边界：本批只推进 SR-126，不自动结算 SR-004、SR-128、SR-178，也不把 catalog/关系纯约束测试外推为维护 Agent 闭环。
- 状态：`working-tree-wired / deterministic-verified / hard-gate-wired / local-real-mongo-blocked / actions-run-pending / deployment-pending`（2026-07-19）。离线召回评测不再把金标 chunk id 注入 mock；它写入相关条目和高静态分无关干扰项，以生产 `list_catalog` 的首项驱动 open/cite，金标只在返回后计算 recall@1。维护闭环真实调用 `chat_turn -> chat_apply -> verify_operation_knowledge_chunk -> knowledge_agent::answer`，并断言 draft/needs_review 中间态、verify revision、verified 后 grounded citation。Worker 以封闭 `committed|noop|needs_manual|failed` verdict 持久化单步业务结果，failed 保存原因，只有 committed 的真实产物进入待审池。
- 验证：三个硬门测试二进制联合 `cargo check` 通过；`knowledge_task_worker` 确定性契约 6/6 通过；定向格式与差异检查通过。真实召回测试二进制成功生成并启动，但本机没有 Docker CLI/socket，且 `TEST_MONGODB_URI`/`MONGODB_URI` 均未配置，故在 testcontainers 创建 Mongo 容器时以基础设施错误退出，业务断言没有执行；该结果不得记为通过或失败。
- 合并门：`.github/workflows/ci.yml` 新增无 `continue-on-error` 的 `knowledge-evidence-gate`，机械要求召回、真实 Chat 闭环和三条 Worker outcome 测试各恰好存在一次，再按 exact name 运行五条 ignored 红线。全量 integration 继续作为 soft 诊断线。当前工作树尚未推送，故 Ubuntu/Docker Actions 真绿仍待证。
- 未完成边界：当前未部署，也未修改或重启测试服务器服务。SR-126 的代码与门禁修复已完成，但只有在专用 hard gate 真绿并完成部署后回归后才能改为 fully verified；HC-032 仍因 SR-004、SR-128、SR-178 及各批次远程证据缺失而保持开放。

### HC-032 动态能力正向见证与 CI 分层后续实施证据

- 覆盖边界：本批推进 SR-004、SR-128、SR-178；不自动结算 SR-126/174/176 的远程证据，也不把本地类型检查、构造的 checker fixture 或 YAML 静态解析解释成真实模型、GitHub Actions 或部署后绿色。
- 状态：SR-004 为 `working-tree-policy-wired / deterministic-verified / actions-run-pending`；SR-128 与 SR-178 均为 `working-tree-wired / deterministic-verified / hard-gate-wired / real-model-run-pending / deployment-pending`（2026-07-19）。共享 `CapabilityEvidence` 从测试入口记录 `attempted/llm_calls/branch/artifacts/assertions_run/verdict/skipped_reason`，Drop 在普通早退时保留 `inconclusive`、瞬时端点错误显式写 `infra_skip`、panic 写 `failed`；只有真实调用、非空目标产物和实际执行的断言同时存在才可提交 `pass`。
- SR-128 正向见证：Knowledge K2/K3/K6/K7/K10、Quality Q3/Q4、Smoke T3 与 Recall 跨行业/维护/缺口闭环共 11 个 case进入固定 manifest。K2 必须真实走关系跳转；视觉/提案/自动审定必须反查产物；Recall 必须满足最低 case/round 覆盖并以真实维护写链闭环。零调用、零产物、零断言、early return 或重复 outcome 均不能通过。
- SR-178 正向见证：Cross-domain 完整双域弧/同输入差异/身份探针、Principal Channel/Relay、Planner/Wake 主动触达、Dynamic Adversarial、Digital Twin peer/formal 与 Roleplay Arc 共 11 个 case进入同一 manifest。全 fallback、零回复、零 task、零 escalation 且无明确安全 fail-closed、空 relay、端点 transient 或身份生成失败均留下非 pass；Principal Channel 只接受真实 pending请示或明确安全/政策/必填字段阻断，预算耗尽不冒充合规终态；Relay 不再把零调用抬成 1。
- CI 与防陈旧：nightly `real-llm-redline` 扩为 7 个串行 shard（含 Roleplay Arc），每个 shard 在 rust-cache 后先清空 ledger，只上传本 shard 本 run/attempt 的结果。`skip-gate` 下载所有 artifact 后由 `check-capability-outcomes.py` 固定核对 22 个 case，并校验 schema、case id、SHA、GitHub run id/attempt、正调用/产物/断言与唯一性；旧缓存和旧 run不能冒充当前证据。
- SR-004 分层策略：新增 `check-ci-gate-policy.py` 并由 baseline hard gate执行。它固定 baseline、Knowledge evidence、Tenant isolation、Frontend contract、T4、redline 与 skip-gate 共 8 个关键门（含 credential-probe）不得 `continue-on-error`；全量 integration及 smoke/recall/ops/quality/adversarial 六类长尾或波动套件必须保持 soft，其中五类真模型套件必须仅 nightly。检查器同时锁定 redline→skip-gate 依赖和 typed outcome汇总器接线，防止后续 YAML 漂移；没有把全部慢测升级成 PR 阻断门。
- 本地验证：七个 redline 测试 crate联合 `cargo check` 通过；22-case manifest/11-case witness映射通过；checker 的完整通过、缺失拒绝和陈旧 run拒绝三态通过；`rustfmt --check`、定向 `git diff --check`、PyYAML解析、CI redline结构和 gate policy `hard=8 soft=6 failures=0` 均通过。唯一编译告警是既有 `handle_principal_decision_relay` dead_code，不属于本批。
- 未完成边界：本批没有调用外部真模型、没有推送分支、没有运行 GitHub Actions，也没有修改或重启测试服务器。SR-004/128/178 只有在授权分支的 hard/nightly 门真实运行并复核 artifacts，且统一部署后回归完成后，才能改为 fully verified；HC-032 因这些远程证据及 SR-126/176 等既有 deployment/actions pending 继续开放。

## SR-001～SR-061

| SR | 第一轮：真实业务结论 | 第二轮：最小化/不过度工程结论 | 决策项 |
|---|---|---|---|
| SR-001 | 已确认安全事件：GitHub API 当前可复现两个仍可达的历史提交树包含同一非占位 LLM 凭证；先前记录的第三个短哈希当前无法解析，已排除出可复现计数。2026-07-30 最小合成鉴权探测仍被上游接受，且正式服务仍使用该值。当前受跟踪树旧值精确扫描为零；生产轮换器与运行手册已完成定向及 Linux 权限/锁验证，但尚无上游新凭证，生产切换、旧值撤销与历史处理仍是上线硬门。 | 先以候选凭证预检并原子轮换/撤销，再清服务器副本、Actions 日志和必要的 Git 历史；不先建设新密钥平台。 | HC-001 |
| SR-002 | 部署凭证历史暴露事实成立。仓库化只读预检器在正式服务器当前 102,259 个普通文件中确认 10 个副本，其中 `.env.e2e` 权限为 `0644`；正式克隆全部 5,117 个 Git blob 中另有 29 个命中（249 次）；25 个压缩载体中有 17 个命中（38 次），包括 16 个数据库归档和 1 个含 6 个环境文件成员的配置包。`/opt` 下 101 个 Git 仓库中只有 `/opt/wechatagent` 为同源克隆。预检器本地联合门 28 项中 26 通过、2 项仅在 Windows 跳过 POSIX 权限语义，Linux 专项 8/8；正式预检扫描无错误，准确返回三类现存载体 blocker，前后生产 PID、健康和重启数不变。 | 与 SR-001 共用轮换和副本收口；普通文件逐路径替换/删除，压缩备份按回滚策略重建或加密封存，Git blob 只随单独批准的历史改写处理；不在旧凭证仍承担生产认证时直接删除回滚材料。 | HC-001 |
| SR-003 | 已修复并完成确定性对账：Evolution 安装态唯一默认值为关闭；配置常量、`.env.example`、README、启动注释与回归测试一致，只有运维显式设为 true 后 runtime flag 才可进一步放行。 | 只统一默认契约，不重构 Evolution；自动发布仍由独立代码政策闸保持关闭。 | HC-002 |
| SR-004 | 工作树治理已实现并完成确定性验证，Actions 真跑待办：关键 baseline、Knowledge evidence、Tenant isolation、Frontend contract、T4、redline 与 skip-gate 固定为 hard gate；全量 integration 与波动真模型长尾保持 soft/nightly。 | 单个 policy checker 锁定 hard/soft 分层、依赖和 typed outcome 汇总，不把全部慢测升级为 PR 阻断门。 | HC-032 |
| SR-005 | 明文探测 Workflow 已移除，现行 Workflow 使用 repository secrets；完整动态审计确认 807 个当前可枚举 run 日志中的 69 个 CI run 在 2026-06-02 至 06-08 合计原值命中 1,795 次。313 个当前可下载 artifact 均零命中，1,933 个已过期 artifact 无法下载，fork 为 0。根因是历史 Workflow 的明文字面量 fallback 被复制到三个凭证别名；当前赋值已全部改为直接 Secret 或显式空值，并由 `workflow-secret-must-be-direct` CI 硬门防回归。冻结证据绑定的日志清理器 20 项联合测试及 69/69 真实只读预检通过，尚未删除日志；公开仓库仍未启用 Secret Scanning。 | 保留 GitHub Secret 注入与受跟踪文件 secret 硬门；新值经生产验证后同步并由最小真实模型 Actions 任务验值；经明确授权只删除 69 个命中日志，不建设自建 secret service。 | HC-001 |
| SR-006 | 已修复并完成确定性文档对账，待 Actions/部署复验：README 与架构绑定当前 commit + 未提交闭环工作树且明确不是部署验证；发送拓扑、Evolution 双闸、启动必填环境变量、baseline 350/33 与最多 14 个受监督 worker 已按源码同步。 | 只维护两份入口文档和源码对账证据，不建设文档生成平台；`git diff --check` 与 worker/默认值/发送链接线检查通过。 | HC-033 |
| SR-007 | 已修复并验证：m009/m034 active 优先，重跑可收敛为同 scope 唯一 current。 | 修正一次迁移选择规则并提供存量审计/补偿迁移，不改整个 Prompt 模型。 | HC-003 |
| SR-008 | 工作树已修复且真实副本集隔离验证通过，正式部署待验：ops 三表保留多版本历史但每 scope 只允许唯一 current；m048 收敛存量，partial unique 锁约束，读侧 fail-closed，publish/rollout/rollback 事务化。 | 复用现有迁移、Mongo partial unique 与副本集事务；不新增版本服务，也不保留隐式多-current A/B。 | HC-003 |
| SR-009 | 已修复并验证：m029 的查询、删除和回填均使用 `(workspace,account,wxid)`；归属未知行等待获批回填。 | 以 `(workspace,account,wxid)` 重跑校正；无需通用身份解析子系统。 | HC-003 |
| SR-010 | 已修复并验证：生产保护迁移写 `blocked` 且可在精确批准后原位重试为 `applied`。 | 增加 `blocked/pending` marker 和后置校验；不引入独立迁移编排服务。 | HC-003 |
| SR-011 | 已修复，待 Mongo/服务器真实 MCP 复验：账号型 MCP 公共 API 全部强制 `(workspace_id, account_id)`，账号凭证查询、调用日志、文本/媒体/名片发送、`chat_search` 与本地 post-hoc 核对均继承同一 workspace；非默认 workspace 缺显式账号 MCP URL/key 时 fail closed，不再回退默认租户凭证。Management、账号登录、联系人、Escalation、Gateway、Outbox 与 roster 调用点已迁移；共享素材按实际发送账号上传，私有素材强制账号一致，媒体恢复查询也按 workspace 过滤。 | 复用现有 MCP client，只把 workspace 变成必填能力并收紧 fallback。 | HC-004 |
| SR-012 | 已修复、进入正式 ELF并完成部署同源服务器真实 Mongo 动态闭环：两个 workspace 复用同一 account/wxid 时，debounce runner 只重载并修改目标 workspace Contact，外域完整 BSON 不变；roster single-flight 分别取得独立 permit。专项 2/2（14.43 秒），测试库 126→126、生产 PID/ELF/健康零漂移，共享 target 5,584 项基线恢复，构建残留精确清理。 | 保留现有进程内 debounce；崩溃恢复仍另见 SR-177。证据归档 SHA-256 `3a0b48cbdae214d5b8cd2523353979ff761d04d453a15b25f63496b70b08475a`；不外推关闭 SR-011 或 HC-004 其它项。 | HC-004 |
| SR-013 | 已修复，待 Mongo/真实模型/服务器复验：`LlmRegistry` 已改为 workspace→client/meta/generation 映射，启动加载所有 workspace 的 active provider，并拒绝同 workspace 多 active；Agent、Knowledge Agent/Catalog、视觉 Runtime 与 Prompt Critic 均按真实 workspace 获取不可变 snapshot，缺槽位 fail closed，不回退默认租户。管理端列表、active 编辑、激活与删除只影响目标 workspace；单实例内串行化变更，候选 client 先构造，DB 真命中后才替换运行时槽位，激活第二步失败会恢复旧 active 标记。 | 保留现有 client/trait 和测试 mock 注入，只把生产 Registry 与调用入口按 workspace 隔离；横向多副本一致性留部署演进处理。 | HC-004 |
| SR-014 | 已修复并验证：Cookie/JWT 受保护请求重读 ACL，移除或清空最后权限后立即 401；登录不接受空/脏 ACL。 | 敏感请求统一重读 ACL；JWT 用短 TTL/权限版本即可，不先建设完整 token revocation 平台。 | HC-005 |
| SR-015 | 已修复并验证：`app_id` 使用全局 partial unique index，重复值被数据库拒绝。 | 先查重再建 partial unique；无需额外路由目录服务。 | HC-005 |
| SR-016 | 已修复、进入正式 ELF 并完成部署后动态闭环：正式 ELF `f0ead4f7…cde9b` 的回环-only transient 实例在 UUID 随机库中实测 `login 401 → token 401 → correct login 429`，返回 `Retry-After=300`；Mongo 恰写三条 `login/token/login` 安全事件，原始账号/IP/密码字段和值均缺失，TTL 严格 90 天。测试前后正式 PID `2196929`、重启数 0、ELF 与健康响应逐字不变，随机库已删除。 | 保留进程内轻量防护；反向代理/多副本必须另设边缘全局限流。证据归档 SHA-256 `5146d63c…e10a`。 | HC-005 |
| SR-017 | 已修复并验证（当前单实例边界）：同目录 pending + Mongo CAS + 原子发布 + 路径锁 + 启动/周期 reconciliation；缺失、损坏与非法路径 fail-closed。多副本仍要求共享卷/对象存储。 | 已按最小单实例协议闭环；部署形态变为多副本时再升级跨副本对象生命周期，不提前引入对象存储。 | HC-006 |
| SR-018 | 已修复，待 Mongo/服务器复验：`generate_agent_json` 与 streaming 入口强制接收 workspace，cache hit、success、failure 日志均写调用方 workspace；所有生产调用点已迁移，缓存身份也包含 workspace 与对应 provider generation。 | 复用公共 LLM 日志入口，以必填 workspace 由编译器暴露遗漏调用点。 | HC-004 |
| SR-019 | 已修复，待环境编译/集成复验：生产 Gateway 在任何 LLM 前写 `started`，首份业务决策后 CAS 到 `running`，正常出口更新同一行，错误与 panic 只关闭开放信封并区分 `failed_before_decision/failed_after_decision`。 | 采用 started/running/terminal 单行信封与真实入口因果测试；不引入全链 event sourcing。 | HC-007 |
| SR-020 | 已修复并在 Linux 隔离环境动态验证：精确缓存按 workspace、provider/model、Registry generation、Prompt pack 与输入内容分片；缓存查找和未命中调用绑定同一 Registry 快照，热切换后不再返回旧 provider 产物。尚未部署。 | cache key 加 workspace/provider generation，激活时 bump；不引入分布式缓存。 | HC-008 |
| SR-021 | 已修复，待 Mongo/服务器复验：Reply 三层 prompt、published Soul、Reviewer、Reaction 与 Memory consolidator 均使用当前 contact/run workspace；公共 LLM 调用也继承相同 workspace，未保留隐式默认租户 Prompt/Soul 回退。 | 保持现有 loader，只把真实 workspace 贯穿各 Agent 入口。 | HC-004 |
| SR-022 | 已修复并完成确定性验证，真实模型复验与部署待办：任何可发送正文都由 Gateway 强制执行独立语义 Claim Gate，不信任 Reviewer 自报标记；实时 verdict 严格校验，模型/Schema 失败直接 safety hold。独立 verdict 与 Reviewer 结论保守 OR；需要证据时只有 verified knowledge、逐字段且逐 clause 完整匹配的同 workspace 目录事实，或明确领导授权可放行，否则清除 `should_reply` 并终结为 `blocked_unverified_product_claim`。Claim Gate 契约 12/12 通过。 | 复用现有 Reviewer、知识与目录背书，不做关键词大全或通用 NLP 规则引擎。 | HC-009 |
| SR-023 | 已修复并完成确定性验证，真实模型复验与部署待办：revision LLM 错误、超时或二审失败时，仅 `humanLike`、`emotionalValue` 或机械 `style_diverged` 的纯风格触发允许恢复已审核原稿；压力、边界/隐私、事实硬闸、未知触发与双 Reviewer 分歧均强制 `revision_failed`、Held 且 `should_reply=false`。revision fallback 契约 3/3 通过。 | 复用现有结构化分类和 finalize，不新增 Agent；安全与事实失败一律 fail closed。 | HC-009 |
| SR-024 | 已修复、正式部署并完成部署后同源真实 Webhook+`rs0` 闭环：同 `account_id` 跨 workspace 使用独立限流桶，限流事件只写解析出的 workspace，外域账号完整 BSON 不变。 | 写事件 API 强制 workspace 参数并迁调用点；进程内限流/缓存键同样纳入租户 scope；不重做事件系统。 | HC-004 |
| SR-025 | 已修复、正式部署并完成部署后同源真实 Dispatcher+MCP+`rs0` 闭环：旧实现 expected-red 证明外域同账号 sent 历史会错误触发本域日软上限告警；修复后查询以 `(workspace_id, account_id)` 聚合，外域历史既不延迟本域发送，也不产生本域软上限告警，本域 Outbox 进入 `sent` 且 `tools/call:message_send_text` 恰一次。正式 ELF `d0b7ffc6…e027b` 上线后精确回归 1/1，数据库/进程零残留，生产运行态不变。 | 查询加 workspace；日 cap 仍保持告警，不顺带升级硬门。发布证据见 `production-release-2026-07-30-hc004-sr025.md`。 | HC-004 |
| SR-026 | 已修复、正式部署并完成部署后同源真实 `rs0` 闭环：同一业务身份跨 workspace 可各创建一行，同 workspace 重复仍命中幂等 skip，外域 Outbox 完整 BSON 不变；v2 key、DuplicateKey 回读、m038 与唯一索引均绑定 workspace+account。 | key 和唯一索引纳入 workspace/account，并做兼容迁移；不更换 Outbox。 | HC-004 |
| SR-027 | 已修复、正式部署并完成部署后同源真实 Reaction 全链+`rs0` 闭环：`record_user_reaction → analyze → stop → cancel` 只取消同 workspace/account 的 Outbox，外域同 account/wxid Outbox 完整 BSON与事件数不变。 | cancel API 接收完整 tenant scope；与 HC-004 合并。 | HC-004 |
| SR-028 | 已修复并完成隔离真实 Mongo 验证，待部署：Reaction claim 原子写不可复用 token 与单调 generation；超时重领先撤销旧 token，结果提交以 `_id + analyzing + token` CAS 线性化。只有当前 owner 提交成功后才允许追加意图轨迹、写负例或执行停止 Outbox 取消，旧 owner 在任何副作用前返回。并发单飞与 ABA 重领红线 2/2 通过，证明旧停止结果不能覆盖新 buying outcome、不能重复轨迹或取消待发消息。 | 复用现有 decision review 文档的 owner/generation fencing；不因单进程现状搭建新队列。 | HC-010 |
| SR-029 | 已修复、已部署并完成动态闭环：后台整理使用 durable `running → committing` 线性化点、prepared payload 与幂等 reconcile；管理员手动入口创建 contact-scoped single-flight task，并经统一 claim/heartbeat/committing 协议执行。 | 不引入新队列或强依赖事务。本机真实 Mongo 2/2 覆盖手动任务独占/冲突零写及三个部分提交崩溃窗口的重复恢复；服务器候选与正式路径新哈希 `3a7d9bab...` 均通过随机库迁移、空队列、回环健康与真实 Cookie Router `200 sent/no_candidates`、active owner `409/零写`。正式 PID `1740029`、零重启、磁盘/运行中哈希一致，切换和部署后证据清单全部复验为 `OK`。 | HC-011 |
| SR-030 | 已修复，待 Mongo/服务器复验：Knowledge Agent 的 catalog/open_document/open_chunk/follow_relations/superseded 跳转统一携带“共享或当前账号”可见域，并逐跳校验 domain/account；模型二次筛选只能与入口筛选取交集，不能放宽状态、主题、wikiType 或未审核门。Chat 工具、apply 更新、关系写入、KnowledgeTask fix/retag、Cold Contact hooks 及 `/ask`/SSE 账号入口同步收口；跨账号目标按 NotFound/BadRequest fail closed。 | 复用现有 visibility scope 与 Agent；不复制第二套 Knowledge Agent。 | HC-012 |
| SR-031 | Fixed and deterministically verified; real-model replay and deployment remain pending. Evidence is accepted only when the selected chunk is opened, verified, non-contradictory, and the exact quote/anchor closes over the selected source. Fabricated quotes, wrong chunk ids, contradictions, and missing or mismatched anchors fail closed. The focused strict-evidence suite passed 5/5 on the current tree. | Reuse deterministic opened/quote/anchor validation; do not add a second evidence agent. | HC-009 |
| SR-032 | 已修复，待 Mongo/服务器复验：答案缓存 corpus signature 改为完整账号可见语料的 `_id + updated_at` 集合，不再依赖被截断的 catalog；缓存键同时纳入 workspace/account、provider id/model/generation、prompt-pack generation、规范化入口筛选、query 与轮数。写缓存前复查 provider generation，知识正文、关系、状态或版本修订都会令旧答案失效。 | 保留现有 5 分钟进程缓存，只强化身份和失效条件。 | HC-012 |
| SR-033 | 已修复，待服务器真实模型复验：Chat 工具循环创建单一绝对 deadline，并以同一 `timeout_at` 包住每次 LLM 与每个工具 await；慢末轮不能越过 30 秒后成功返回。只有模型真实返回 `decisionPhase=final` 才透传原业务 JSON；预算、失败连击或四轮耗尽统一生成带稳定 stop reason 的 final fallback。已补慢 LLM、四轮 tool_calling 和非 final payload 回归哨兵。 | 复用现有循环状态机，只增加绝对 deadline 与终态归一化。 | HC-012 |
| SR-034 | Fixed and verified in isolated real Mongo; deployment remains pending. Task claims use non-reusable owner tokens plus monotonic generations; terminal commit, Outbox bind/authorization, cancellation, reclaim, and prepared memory commit all require the current owner. Local owner 10/10, memory fencing 11/11, index 5/5, migration 3/3, and authorized-server Mongo redlines 4/4 passed. | Reuse the existing task collection and owner-scoped CAS; do not add a scheduler service. | HC-010 |
| SR-035 | 已修复，待 Mongo 动态复验与部署：授权过期不再裸发；先把 escalation 写成 `relay_state=terminal`/`authorization_expired`，再将中性收尾交给同一 fenced Task→Review→Outbox 协议。 | 终态 CAS 幂等；若送达终态已先提交，不再追加矛盾的过期收尾。 | HC-013 |
| SR-036 | 已修复、正式部署并完成部署后真实 `rs0` 闭环：Outcome task partial unique key 与 m017 去重分组均包含 workspace，m033 安全退役旧跨租户索引。部署后生产协议红线 1/1：跨 workspace 同 account/content 可并存，同 workspace 重复写命中 duplicate-key；UUID 库/测试进程零残留，正式运行态不变。 | 沿用现有 Task 模型，仅把 workspace 纳入业务键并做确定性迁移。证据归档 SHA-256 `784e141c…2735`。 | HC-004 |
| SR-037 | 已修复，待 Mongo/业务复验与部署：领导裁决只作为本次客户转述的冻结事实源；可泛化内容只生成 draft/needs_review，不直接写 verified 知识。 | 不增加第二套知识审核；沿用现有提案/人审协议。 | HC-013 |
| SR-038 | 已修复，待 Mongo 动态复验与部署：首卡先持久化冻结 intent，再按 escalation id+generation 物化 Outbox；只有 `sent` 对账写推送时钟，重试耗尽转 `delivery_failed` 并释放 pending 槽。 | delivery_unknown 保持人工核验，不自动重放；失败历史保留审计。 | HC-013 |
| SR-039 | 已修复，待 Mongo 动态复验与部署：timeout scanner 以 escalation generation CAS 开启下一代，Outbox source id 确定且幂等；dispatcher 在最后远端边界再次核验，旧 generation 取消。 | 不引入全局 scheduler；并发扫描回归锁定一代一条 Outbox。 | HC-013 |
| SR-040 | 已修复，待 m046 Mongo 动态复验与部署：pending partial unique 改为 `(workspace,account,contact,category)`；领导回复和列表匹配同时绑定 principal account。 | m046 先审计 canonical identity/重复，再只退役精确旧索引，未知形状 fail-closed。 | HC-013 |
| SR-041 | 已修复，待 Mongo 动态复验与部署：escalation 同行冻结 domain、policy version、完整 ask-human policy 与 principal account；scanner 不再读取后来变更的 current config。 | 不复制 domain 调度器；继续沿用现有 escalation worker。 | HC-013 |
| SR-042 | Fixed and deterministically verified; real-model replay and deployment remain pending. Every holding-reply scene applies the deterministic number/authorization guard, then a strict typed semantic safety verdict, and delivery goes through Outbox instead of a raw send path. Empty, forbidden, unauthorized numeric, malformed, and internally inconsistent verdict cases fail closed; the focused suite passed 8/8. | Reuse the existing deterministic guard, semantic review, and Outbox; do not add another review agent. | HC-009 |
| SR-043 | 已部署并完成服务器 `rs0` 动态矩阵：DomainProfile 版本、lineage current 与 workspace active 分别由 unique/partial unique 索引约束；draft append、current 切换和 active 切换使用事务/CAS，运行时多 active fail-closed。 | m051 在索引前只读审计 canonical identity、正版本、重复版本/current/active；部署后 26 条真实 Mongo/Handler 路径覆盖发布、激活、回滚、唯一 active、不可变 draft、Policy/联系人联动。 | HC-014 |
| SR-044 | 已部署并完成服务器生产 validator 专项 1/1：动态 kind 只允许单个 canonical snake-case 路径组件，拒绝 dotted path、重复、系统保留 key 与 `_updated_at` 后缀；draft、发布、激活和 runtime load 复用校验。 | 保留开放行业维度，只封住 Mongo dotted-path 与系统字段 namespace，不改成行业闭集。 | HC-014 |
| SR-045 | 已部署并完成服务器真实 Gateway+Mongo 红线：Decision 只分类和 canonical 改写，Taxonomy candidate occurrence 仅由 Gateway 最终决策阶段写一次；单 run 未知值实际落库 `occurrences=1`。 | 保留既有候选集合与 fail-soft 语义，不合并 Decision/Gateway；部署后专项 1/1。 | HC-015 |
| SR-046 | 已部署并完成服务器真实 m050+multikey 索引红线：全历史版本回填 identityClaims；active alias 歧义首写前失败；Mongo E11000 阻断 alias↔alias/canonical 第二 owner。 | 不设计 alias 图；生产 m050 applied，partial unique multikey 索引形状已核对，专项 1/1。 | HC-015 |
| SR-047 | 已部署并完成真实 Gateway+Mongo 红线：BayesianPoint 持久化 `sourceRunId`；同 run/dimension 同值只落一个最高置信点，重试身份可幂等，冲突值继续由确定性红线锁定零计数。 | 不改贝叶斯模型或阈值；部署后持久化路径与 SR-045 联合专项 1/1。 | HC-015 |
| SR-048 | 已部署并完成服务器真实 Mongo 全库逐文档快照红线 1/1：Shadow/Simulation 使用只读 memory、knowledge route 与 taxonomy 路径，复用独立 ClaimGate、finalize 与状态动作策略；除 3 条 `run_mode=shadow` 成本日志外业务集合差异为 0，operator memory 使用时间也不刷新。 | 仅允许明确标记的成本/诊断日志，不复制 Simulation Agent；部署后确定性路径已闭环，真实模型/MCP 外部链路未据此冒充通过。 | HC-016 |
| SR-049 | 已修复，待本机 Mongo 动态与服务器真实模型复验：Prompt Shadow 源 run 与消息按 workspace/account/contact/message 完整作用域读取；proposal 绑定模板 id/version/content hash；baseline 与 candidate 在同一 provider generation、Profile、产品集、Taxonomy 深快照、评估时刻与准备上下文上各完整执行 Reply→Review→ClaimGate→Finalize，历史 run 分数不再充当 baseline。 | 不建第二套回放库：对实际依赖集合在准备前、baseline 后、candidate 后做内容指纹，任一漂移 fail-closed 为 `shadow_dependencies_changed`，不得计 completed；两侧预算独立，唯一有意变量是候选追加片段。`cargo check --lib`、`cargo check --test evolution_prompt_shadow`、定向单测与格式/diff 门通过；六调用 Mongo 集成已编译，本机无 Docker 未执行。 | HC-017 |
| SR-050 | 已修复，待新增迁移动态复验与部署：主动发送台账以不可变 `outbox_id` 锚定送达事实，`workspace/account/outbox_id` upsert `$setOnInsert` 幂等；正常送达、crash reclaim 与 timeout post-hoc 共用同一写入。近期历史、响应消息、联系人阶段、只读 API、聚合与索引均固定 workspace/account，消除同 wxid 跨账号归因。 | 存量无锚行继续兼容且不猜测归属；m041 在唯一索引建立前只读审计已声明锚点，对错型/空 scope/重复 `outbox_id` fail-closed，绝不自动删并历史。`cargo check --lib`、`cargo check --test send_ledger_integration`、定向单测 19/19 与格式/diff 门通过；既有服务器隔离 Mongo `sr050_*` 3/3 保留，新 m041 冲突零改写用例已编译、本机无 Docker 未执行。 | HC-010 |
| SR-051 | 已修复，真实模型容量恢复后待复验：旧 `quoted_product_ids` 仅保留兼容，不再参与授权；独立 ClaimGate 从最终回复提取目录声明，每项必须携带完整 clause 的精确 `sourceQuote`，服务端逐字段核对 active catalog，并反向覆盖回复中每个 active 产品名/SKU clause。初稿、revision 改写稿与管理发送均走同一门，schema/模型调用失败 fail-closed hold。 | 不引入关键词大全或第二套审核模型。确定性覆盖有效 ID+错价、A/B 串价、一真一假、多 clause 漏报、同产品第二个错价、伪造 quote、币种/SKU 冲突及无固定价/SKU 产品；ClaimGate 12/12、Review 100/100、`cargo check --lib`、格式与 scoped diff 通过。真实 NVIDIA 端点仍因 `503 ResourceExhausted` 未执行到业务断言，不记 fully verified。 | HC-009 |
| SR-052 | 已修复，动态 Mongo/Actions/部署待验证：名片跨过持久化 `send_started_at` 后发生 timeout、进程崩溃、网络/HTTP 歧义或缺少可信业务回执时，均进入 `delivery_unknown` 且不可再次 claim；无权威名片查询时不以“无本地成功日志”推断未送达。共享文本/媒体/名片回执采用成功、明确失败、不确定三态，冲突信封也停止自动重放。 | 回执三态纯单测 1/1；最新集成二进制链接并枚举 46 项，五条 `delivery_redline_*` 全部存在；CI hard gate 已锁定恰好五条。动态红线因本机无 Docker/Mongo且测试机 SSH key 认证失败未执行，不记 fully verified。 | HC-010 |
| SR-053 | 已修复，待 Mongo/副本集动态复验与部署：Soul 内容改为 append-only 版本；编辑只追加 draft，发布在事务内归档旧 published 并切换唯一指针，历史版本可重新发布回滚；运行时按 workspace 严格要求恰好一个 published，缺失/多指针 fail-closed。启动仅补缺且不覆盖运营 Soul，显式 reset 追加内置版本并记录认证 actor。 | m042 在唯一索引前全量校验并确定性收敛历史多 published，重复版本零改写失败；唯一版本索引和 partial published 索引锁住不变量。前端保存后以 PUT 返回的新 draft id 接续发布，任何保存异常清空旧目标。`cargo fmt --all -- --check`、`cargo check --lib`、`cargo check --test sr053_soul_versions`、Rust 定向单测 2/2、前端身份回归 3/3、`npm run build` 与 scoped `git diff --check` 通过；5 条 Mongo/副本集用例已编译并枚举，本机无 Docker/mongod/TEST_MONGODB_URI 未执行，不记 fully verified。 | HC-018 |
| SR-054 | 已修复、服务器真实 Mongo 验证完成，待正式切换：裁决 CAS 同行持久化 `relay_state=pending` 与确定性 `relay_task_id`；task 以 escalation `_id` 幂等物化，worker 补偿中断 intent。客户转述确认送达或授权过期后同行写 `relay_state=terminal + at/reason`。 | Awaiting 由 escalation id owner 集合维护，单条终结不会误清其它等待。m047 只认 pending、新协议未终结 relay、或 legacy pre-Outbox `pending/retry/running` task；生产克隆首轮演练发现旧 `outbox_enqueued` 已可能送达却未回写 task，收紧后第二轮迁移演练 owner 0→0。授权服务器随机副本集库运行 HC-013 两组动态集 29/29，Rust/前端/格式/全目标类型门与隔离 release/前端构建通过；测试库及两轮演练资产已清理，生产服务与数据库全程未切换。 | HC-013 |
| SR-055 | 已修复，待 Mongo/副本集动态复验与部署：Prompt POST/PUT 仅追加 draft，手工发布事务性归档旧 current 并提升目标；历史 system/manual/evolution 行不再物删。运行时只认唯一 `current_version=true + status=active`，坏流 fail-closed，不再按 contact/locale 隐式 A/B。启动对齐与 Evolution 发布/回滚同步维护同一指针/status 协议。 | m043 在首写前全表校验并收敛合法旧多-active 行，重复版本、split/多 current 或 malformed 行零写失败；partial unique current 索引锁住唯一指针。Rust 定向单测 4/4、库与三个集成目标编译、前端 6/6、生产构建和格式门通过；动态用例因本机 Docker daemon 不可用在建容器前退出，未记通过。 | HC-018 |
| SR-056 | 已部署并完成服务器真实 `rs0` 精确版本红线：v1 active + v2 inactive 事务切换到 v2，workspace active 始终唯一，两条不可变历史均保留；不存在 v99 失败且指针零变化。运行时多 active 继续 fail-closed。 | 正式 m044 为 applied，版本 unique 与 workspace active partial-unique 索引形状正确；专项 1/1，随机库 126→126，服务零漂移。 | HC-014 |
| SR-057 | 已部署并完成服务器真实 `rs0` 事务红线 3/3：成功审批同行提交 signal、`staff_confirmed` outcome 与审计；重复审批零增量；负金额首写前失败并保留 pending；审计拒写使三处整体回滚。 | 普通手工成交与审批复用同一 prepare/persist 真相源；随机库 126→129→126，服务零漂移。 | HC-019 |
| SR-058 | 已部署；关系、Taxonomy 与疑似成交三类动态均证明请求体伪造 `reviewedBy` 不能覆盖认证管理员；Management actor 保持闭集。 | 私有 `ReviewActor` 切断任意字符串进入审核 helper，不建设独立 IAM；三类持久化路径均已动态验证。 | HC-019 |
| SR-059 | 已部署并完成关系审核真实事务红线：成功审批同行提交建议终态与联系人画像；联系人 validator 拒写发生在建议 CAS 后时事务整体回滚，建议仍 pending、审核字段为空、旧画像不变。 | 保留其它画像键并同步真实更新时间；真实 Router+`rs0` 专项 1/1。 | HC-019 |
| SR-060 | 已部署并完成真实周期约束红线：终态历史不占槽，下一 pending 周期可创建；同周期第二条 pending 由 E11000 拒绝。正式 m045 applied，partial unique 索引形状正确。 | 审核历史继续保留，不擅自增加 TTL；专项 1/1，随机库已清理。 | HC-019 |
| SR-061 | 已部署并完成服务器真实 Cookie Router+`rs0` 事务红线 2/2：候选审批合并既有 canonical 返回 200/`mergedIntoExisting=true`，从 current v3、历史最大 v9 追加 v10/previous v3，alias 与候选终态同事务提交；故障注入证明字典写失败时 claim 回滚且可修正重试。 | 完整继承显示名、描述、状态、优先级、终态与再激活字段；认证 actor 覆盖伪造请求 actor，不建设平行审核服务。 | HC-015 |

## SR-062～SR-100

| SR | 第一轮：真实业务结论 | 第二轮：最小化/不过度工程结论 | 决策项 |
|---|---|---|---|
| SR-062 | 已部署制品观测并完成服务器真实 Cookie Router/Mongo/回环 MCP 红线：动态目录递归移除 `message_send_*`，执行兜底再次拒绝；主动文本只经 typed Gateway→Review→Outbox。 | 复用现有生产发送链，不建第二套发送审批；HC-020 动态 2/2，正式服务零漂移。 | HC-020 |
| SR-063 | 已部署制品观测并验证：所有真实副作用由代码层默认要求确认，LLM 自报 low/无需确认不能放行；只读、`media_get` 与 provider 连通测试为窄白名单，未知工具 fail-closed。 | 保留静态 effect 表，不引入策略 DSL；真实计划入口红线证明写工具停在确认门。 | HC-020 |
| SR-064 | 已部署制品观测并通过服务器恢复红线：冻结 plan hash、命令 lease/token、逐调用 durable intent/partial unique 与 owner CAS；`executing` 崩溃遗留只收敛 `execution_unknown` 且 MCP 零重放。 | 复用 command/tool 表；对未知远端结果保守停机，不假造 exactly-once。 | HC-020 |
| SR-065 | 已部署制品观测并通过真实 Cookie Router/Mongo 红线：LLM 仅起草成交 payload；确认端点绑定当前认证管理员，最终只写一条 `staff_confirmed` 且 `marked_by=hc020-admin`。 | 复用既有成交真相源与显式确认，不新增成交系统。 | HC-020 |
| SR-066 | 已完成授权服务器真实 `rs0` + localhost MCP mock 五条投递红线 5/5，待真实模型 T4 与部署：Dispatcher 在 `send_started_at` 前执行最终 owner/cancel CAS；边界前取消为零 MCP 调用，越过远端边界后成功回执恰好一次收敛 `sent`，不可核验回执收敛 `delivery_unknown`，均不自动重放。首轮旧夹具缺 scoped MCP 账号导致 1/5，只修复测试前置事实后完整重跑通过；生产 fail-closed 账号门未放宽。 | 保留诚实的 best-effort 取消与不确定送达语义；越过网络边界后绝不承诺物理撤回。 | HC-010 |
| SR-067 | 已修复并在正式制品中观测，确定性与服务器真实 Mongo Handler 验证完成：疑似成交作为统一收件箱第九源，pending 集合、过滤列表和 summary 同 workspace 一致；终态/外租户排除，确认与驳回复用可信事务 Handler。 | 不合并底层集合；前端专项 16/16、生产构建、服务器动态 1/1。因无可复用认证会话，本轮不声称生产 Cookie HTTP 交互通过。 | HC-019 |
| SR-068 | 已修复并有回归断言：统一待审汇总按来源保留查询错误，返回 `complete/partial/error`、失败项 `null`、`errors[]`；任一来源失败时 `total=null`，不再伪装成零积压。 | 复用现有聚合端点与前端 typed 契约；不引入新监控平台。 | HC-007 |
| SR-069 | 已修复、确定性验证完成、待部署：生成/优化共享 camelCase DTO 与后端投影契约，契约快照、前端专项与生产构建通过。 | 只保留一套正式 DTO；不维持错误 payload 的双协议兼容。 | HC-018 |
| SR-070 | 已修复并通过授权服务器 `rs0` 动态红线，待部署：AI 生成/优化只追加 `draft` 候选；联系人绑定和 Agent 运行时只接受 `published`，人工保存或设默认才显式发布。服务器 draft 绑定完整 Contact BSON 零写拒绝 1/1，draft 发布切默认 1/1。 | 复用现有 Playbook collection/version 与管理 UI，不建设独立 CMS。 | HC-018 |
| SR-071 | 已修复并通过授权服务器 `rs0` 动态红线，待部署：账号级 partial unique index 锁住唯一默认；空账号首默认由唯一索引原子仲裁，替换已有默认以 expected version 和副本集事务同时降级旧默认、发布并提升目标。错误账号/旧版本零写 1/1，原子切换后恰一默认 1/1。 | 唯一索引 + 窄事务解决默认指针，不建设中央配置服务。 | HC-018 |
| SR-072 | 已正式部署并完成真实 `rs0` 双入口红线、生产克隆升级演练和独立部署后审计：current 状态机下缺失/损坏/非 active Policy 在 Reply、Revision、Shadow 与管理发送统一 fail-closed；Prompt pack bootstrap/reset/启动对齐同源 reconcile。生产 9 态/0 Policy 已精确补为 9/9，正式 ELF `155bb77c…2ce67`、PID `2517121`、零重启，Outbox/MCP 红线为 0。 | 保留“核心指针原子 + 附属步骤可见重试”，用运行时安全不变量封住短暂写失败，不扩成四集合大事务。 | HC-014 |
| SR-073 | 已部署并完成服务器副本集动态矩阵：编辑只追加不可变 draft；publish/activate 以精确 `_id` 操作，并在事务内要求目标状态/current 身份仍匹配，确认后内容不能被原地替换。 | 用现有不可变版本行身份和 CAS 绑定确认对象；发布、激活、回滚及 draft 身份路径均由真实 Handler 覆盖。 | HC-014 |
| SR-074 | 已部署并完成真实 Cookie Router+`rs0` 红线 1/1：Operation Domain reset 追加内置默认新版本并 CAS 切换 current，旧 current 与全部历史保留，最终恰一 current；Policy 对账结果显式返回 complete/partial。 | append-only 新版本替代 delete-many 重种，不引入第二套维护服务。 | HC-014 |
| SR-075 | 已部署制品观测并通过服务器真实 Handler/Mongo 红线：Campaign preview 仅计算受众并返回冻结身份，completed/canceled fail-closed 且完整 Campaign BSON 零写；草稿 PATCH 后按新 specVersion/specHash 预览。 | preview 保持纯计算，草稿创建/编辑走显式写端点；完整目标 11/11。 | HC-021 |
| HC-004/HC-021 closure | Campaign 创建在落库前校验解析后的 `(workspace_id, account_id)`；其它 workspace 的同名账号不能授权当前租户草稿。真实 handler + 独立 MongoDB 8.0 随机库 1/1 通过，错误 scope 返回 `NotFound` 且集合零写；不改变旧客户端省略 `accountId` 的兼容回退。 | 复用现有 `validate_account`，不引入第二套账号解析。 | HC-004 / HC-021 |
| SR-076 | 已部署制品观测并通过服务器真实故障恢复红线：首次 dispatch 以 CAS 冻结 generation/spec hash/意图/受众；逐目标 `campaign_sends prepared→enqueued` 绑定确定性 task，task insert 故障后由 reconciler 从同一 prepared intent 恢复并仅在全量就绪后完成 Campaign。 | 复用 campaigns/campaign_sends/tasks，不新建队列产品；真实杀进程级演练仍单列待办。 | HC-021 |
| SR-077 | 已部署制品观测并完成前后端闭环验证：草稿携 `specVersion/specHash`，字段变化使旧 preview 失效；再次预览前以 expected version PATCH 完整规格，dispatch 只接受同次冻结身份。 | 单一服务端草稿真相；前端 Campaign 定向 33/33、生产构建既有门通过。 | HC-021 |
| SR-078 | 已修复并有回归断言：Observability 响应统一返回 `asOf`，逐指标声明 `consistency=non_snapshot/cached_snapshot` 与窗口边界；查询显式限制到同一 `asOf`。 | 明示非快照一致性并尽量聚合；无需为看板引入强事务快照。 | HC-007 |
| SR-079 | 已修复，待环境编译/集成复验：联系人页按本页 wxid 一次聚合取最新入站，并增加与 workspace/account/direction/contact/sort 对齐的复合索引。 | 保留 Mongo 页内聚合；不物化、不预建分析仓。 | HC-007 |
| SR-080 | 已修复、正式部署并完成部署后真实 Cookie Router+`rs0` 闭环：单联系人启用先以 workspace+expected account 锁定 Contact，再以 Contact 的 `(workspace_id, account_id)` 查询账号并做 self-wxid 判断。部署后反例让外域同名账号的 self-wxid 恰等于目标联系人、本域账号 self-wxid 不同；启用仍成功且只写本域 Contact/事件，外域账号完整 BSON与事件数不变。HC-004 同源矩阵 7/7，所有 finalizer RC 为 0、UUID 库/测试进程零残留。 | 删除裸 account 查询，不引入额外身份抽象。正式 ELF `efe5e1e1…baf4`；证据归档 SHA-256 `c5c16d0e…cb4b`。 | HC-004 |
| SR-081 | 已部署制品观测并通过服务器当前完整 Handler/Mongo 目标：共享导入 helper 使用字段存在性 patch，缺失/null nickname/remark/alias 保留旧值；REST、deprecated search-import 与 Management 工具同源，非真人零写。 | 仅更新明确出现的字符串字段；当前 HC-022 目标 13/13。 | HC-022 |
| SR-082 | 已部署制品观测并通过服务器真实故障红线：先持久化不可 claim 的 `initial_profile_enrollment` intent，再 CAS Contact 为 managed，最后释放任务为 pending；task insert 被 validator 拒绝时零 managed 半提交，并发请求仅一条 active intent。 | 复用现有 agent_tasks committing/reconciler；不新建队列或要求跨集合事务。 | HC-022 |
| SR-083 | 已部署制品观测并通过服务器真实代际红线：画像写回要求 managed + enrollment token，并由 task claim generation 单调 fencing；disable/hide 旋转 token 后取消旧代任务，窄崩溃窗口可在同次重新纳管中退休旧代并创建新代。 | enrollment generation + CAS；真实 worker 杀进程演练仍单列待办。 | HC-022 |
| SR-084 | 已部署制品观测并完成前端/实现验证：联系人最近入站由逐联系人 `find_one` 改为当前页一次 `$match+$sort+$group` 聚合，失败仍 fail-soft 返回联系人列表。 | 当前页一次聚合；无需维护新的“最近消息”服务。 | HC-022 |
| SR-085 | 已修复，待 Mongo/服务器复验：Evolution 每轮动态枚举全部注册 `(workspace_id, account_id)`，实验、候选、Prompt Critic、Replay、显著性、复评、自动发布、事件与管理查询全链继承同一 scope；发布/回滚重读及事务状态推进使用 scope+状态 CAS，关联索引改为 scope 前缀复合键。 | 复用账号表作为 worker scope 真相源；管理端展示当前 workspace 全部账号，不引入中央调度平台。 | HC-004 |
| SR-086 | 已修复，待 Mongo/服务器复验：proposal 冻结不可变 base revision/hash；release 只在同 scope 当前 revision 仍匹配时 CAS 提交。 | proposal 保存 base version/hash，release CAS 校验；不重新实现完整 Git。 | HC-017 |
| SR-087 | 已修复，待 Mongo/服务器复验：rollback 只撤销仍为 current 且归属该 proposal 的 released revision，并按冻结 ID/hash 恢复基线；后续版本存在时 fail-closed。 | rollback 仅允许当前版本是该 proposal 产物，或创建新回滚 proposal；不物删历史。 | HC-017 |
| SR-088 | 已修复，待 Mongo/服务器复验：发布事务同时写产物、proposal 状态、审计事件和 protocol-v1 复评 intent，唯一索引保证 intent 幂等。 | 事务内写 audit/review intent，后台幂等处理；复用现有表。 | HC-017 |
| SR-089 | 已部署并完成服务器真实生成 Handler 专项 1/1：camelCase 归一化按字符索引，含 `客户Stage`、`éValue` 的模型 JSON 不 panic，候选正常落不可变 draft。 | 保留轻量 normalizer 和既有 wire 约定，不限制行业文本为 ASCII；末尾连续大写的已知转换限制另有测试锁定。 | HC-014 |
| SR-090 | 已部署并完成候选/正式路径验证：主列表返回完整 DomainProfile 版本；统一待审列表与 summary 同源包含未发布 draft 和待激活 current，富卡按精确 id 提供发布、激活和附属重试；候选烟测中 69 个静态文件逐字一致。 | 复用现有待审箱与 `ProfilePublishCard`，不新建审批中心。 | HC-014 |
| SR-091 | 已部署制品观测并通过服务器真实 Cookie Router+`rs0` 红线：Preview 冻结 Contact/Memory/Playbook/Domain 直接基线和写计划，hash 绑定身份+计划，Apply 以各对象 CAS/事务 guard 拒绝漂移。 | 只冻结直接受影响对象；不做全库快照或通用变更平台。 | HC-023 |
| SR-092 | 已部署制品观测并完成确定性验证：Guide 标签写权威 `manual_tags` 并记录 actor/time；未知或不支持建议进入 skipped，不再写废弃运行字段。 | 单一权威写层；不维护双写兼容。 | HC-023 |
| SR-093 | 已部署制品观测并完成前后端验证：服务端从 frozen plan 生成逐字段 diff、scope、影响人数和强确认要求；前端只展示 authoritative diff，Apply 独立重算 scope。 | 模型文本只作说明，不作授权真相；前端 Guide 专项 37/37。 | HC-023 |
| SR-094 | 已部署制品观测并完成真实 Cookie Router+`rs0` 动态闭环：手工编辑和 Guide 共用 typed runtime validator；合法 runtime 写入、legacy canonical 化、非法完整 BSON 零写、高风险 Guide 拒绝、workspace-wide 强确认、事务提交与稳定重放均通过。 | 复用一个 validator/allowlist 和既有 Guide 事务；不新增配置平台。首轮仅测试 Int32 oracle 过窄，修正为 BSON 整数等价后 1/1，全程未改生产代码。 | HC-014 |
| SR-095 | 已部署制品观测并通过服务器副本集事务红线：业务写、终态与 receipt 同事务提交；故障整体回滚可修正重试，同 hash 重放返回同一稳定 receipt。 | 详情读取另行刷新；不在 commit 后重建复杂响应。 | HC-023 |
| SR-096 | 后端已进入正式 ELF 并完成部署后动态闭环：正式 ELF `f0ead4f7…cde9b` 的回环-only UUID 随机库实测跨 workspace acquire 404 且零外域 Chunk，并发同 workspace acquire 收敛为 200/409，非 owner release 403，owner release 成功且重复 release 幂等；正式 PID `2196929`、重启数 0、ELF 与健康响应前后逐字不变，随机库已删除。正式前端仍把他人 presence 显示为“暂只读”并禁用 mutation，故整项尚未完成生产切换。 | 不引入 CRDT 或租约写权限。已从正式前端 383 项源码基线构建单运行文件最小候选：定向测试 5/5、生产构建、69/69 静态文件 HTTP 逐字烟测通过；候选尚未切换。后端联合证据归档 SHA-256 `5146d63c…e10a`。 | HC-024 |
| SR-097 | 已修复、已部署并完成正式 Router/Mongo 并发复验：Lesson `_id`/来源锚构成确定性一对一身份，Chunk、Lesson CAS 与审计同行提交；m055 marker 与双 unique 索引已在正式库生效。 | 不建通用工作流；部署前 Cookie Router+`rs0` 3/3与纯单测4/4继续有效，部署后双并发+重放收敛为一 Chunk/一审计且跨 workspace 不可见；Wave1 清单 `d16d0ce7…`。 | HC-019 |
| SR-098 | 已修复、正式部署并完成部署后运行态复验：active 场景按当前 DomainProfile 全部公式校验有限 0..10 金标；缺失或非法金标只能存 draft，存量脏 active 在模型调用前标 `unscored` 且不进入均值。Runner 只读取全局或当前账号场景，显式 contact 也按账号绑定。 | 前端专项 5/5、正式 Cookie Router+`rs0` 准入零写红线 1/1；正式 ELF `f0ead4f7…cde9b`、PID `2196929`、重启数 0。 | HC-026 |
| SR-099 | 已修复、正式部署并完成部署后运行态复验：Simulation 返回本次 task-local `RunBudgetSnapshot`，评测只累计自身子 run；成功与业务失败均计 token/call，未知 usage 明确 degraded 并停止后续场景，不再扫描共享生产 `agent_run_logs`。 | 生产日志污染隔离与失败 usage 停机红线 2/2；确定性 mock LLM 只证明计量不变量，不冒充模型质量门。部署后归档 `5f5f53e9…9fd9`。 | HC-026 |
| SR-100 | 已修复并有前后端契约：每项指标返回 `metricScopes`，区分 24h flow、current inventory、retained history 与 rolling cache，并附 `asOf/window/updatedAt`；界面按真实范围展示。 | 逐项声明窗口与口径；无需强行统一所有指标为一个窗口。 | HC-007 |
| SR-101 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：通用 Chunk create/PUT 与内容 Patch 统一经过服务端受控字段投影和 revision 生命周期；客户端提交的 active/verified/confidence 不生效，普通内容修改固定进入 `draft+needs_review`，只有专用人工 verify 可提升。 | 正式构建输入与运行 ELF 逐文件绑定；legacy PUT 不得迁移 scope/自验证、create 原子提交及 AI revision 强制待审均在正式源码测试二进制中通过。 | HC-024 |
| SR-102 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Split 只接受 Unicode 字符偏移与原因，子 Chunk 由服务端派生 scope/classification 并固定为 `draft+needs_review`；子项、revision、源归档与 catalog intent 同事务提交。 | Split 成功、验证中途失败全回滚及 legacy `newChunks` 整对象注入零写拒绝均真实通过。 | HC-024 |
| SR-103 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Split/Merge 在同一 Mongo 事务内创建派生 Chunk、写不可变 revision、归档源/目标并排 catalog intent，任一中段失败整体回滚。 | Split/Merge 成功与中途失败四条事务红线均在正式构建根生成的测试二进制中通过。 | HC-024 |
| SR-104 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：新 revision 保存完整 before/after snapshot；rollback 保留当前租户/身份/运行统计与锁，强制重新待审并按当前 schema 复验，legacy 无快照 fail-closed。 | snapshot 精确恢复与 legacy fail-closed 两条副本集红线真实通过；不引入补丁重放或事件溯源。 | HC-024 |
| SR-105 | 已修复并完成授权服务器 Rust 契约验证，正式部署待办：前端共享 DTO 固定 Patch/Split/Merge/Relate 为 `{patch:{summary}}`、`{offset}`、`{targetId}`、`{targetId,kind,note}`；后端 DTO 拒绝未知字段和旧 snake_case/整对象形状，关系类型收紧为六值闭集。 | 不新增兼容端点或长期维护双协议；DTO 契约 1/1、前端相关定向回归与生产构建通过，正式服务未切换。 | HC-024 |
| SR-106 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：repair apply 由服务端筛选 accepted patch，在同一 Mongo 事务内提交 AI draft revision 与 `knowledge_repair_applied` 事件；事件绑定 committed `revisionId`，事件拒写会回滚 Chunk、revision 与 catalog intent。 | 成功事件指向已提交 revision、事件拒写整图回滚两条真实 Router+副本集红线均通过。 | HC-024 |
| SR-107 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Auto-verify 仅在 revision committed 后累计 processed/status 并写 usage；revision 失败显式计入 `failed`，不生成伪成功 usage。 | 正式源码测试二进制的 auto-verify 矩阵 2/2 通过，覆盖 AI 不自动 verify 与只计 committed revision。 | HC-024 |
| SR-108 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Document PUT/PATCH 以 version CAS 只更新可编辑元数据并保留服务端字段；Chunk create 同事务写主行、create revision 与 catalog intent；Chunk/Document DELETE 为幂等可追溯软归档，Document 级联与子 Chunk archive revisions 同事务提交。 | create 成功/intent 拒写回滚、PUT/PATCH 版本与字段边界、Chunk 软归档、Document 级联成功/子 revision 拒写回滚均在正式源码矩阵中通过。 | HC-024 |
| SR-109 | 已修复、已进入正式 ELF 并完成部署后真实公网链路与副本集验证：摄取源保存和 Worker 执行共用 fail-closed 出站策略；仅接受无凭据 http(s)，每跳重新解析 DNS并固定到已验证公网地址，禁代理和自动重定向；响应按 kind 校验 Content-Type，流式限制 4 MiB。 | 正式226项构建输入同源测试 3/3：`example.com` 成功摄取、`httpbingo.org` 跨域重定向成功；公开 DNS 名解析到 `169.254.169.254` 时请求前拒绝；错误 Content-Type 与声明超过4 MiB均 fail closed。公共重定向服务对私网目标主动返回403，故不冒充“第二跳私网30x”动态证据；逐跳重解析仍由正式源码循环与确定性门绑定。测试零数据库残留，正式运行态不变。 | HC-025 |
| SR-110 | 已修复、正式部署并完成真实 `rs0` 与部署后闭环：Revision 全链强制 workspace；m039 首写前验证父 Chunk与归属。正式库保留 scoped key 的历史索引名，新启动按完整选项语义复用，不重跑 backfill，也不创建冲突 canonical 名称。 | m039 红线 3/3；正式库 94 条 Revision scope 完整，启动日志明确记录两条历史名称索引复用。同 key 非等价选项仍 fail-closed。HC-004 其它租户项继续开放。 | HC-004 |
| SR-111 | 已修复、已进入正式 ELF 并完成部署后同源副本集动态验证：会话身份冻结 `(workspace, session, account, owner_admin)`；history/apply/discard/task/SSE 全链校验账号与管理员归属。Chat apply 在单一 Mongo 事务内认领 pending assistant turn、写 Chunk、create/patch revision、审计事件与稳定 `apply_result`；并发冲突和重放回读同一回执，陈旧 Chunk snapshot fail closed。 | 正式构建根测试 4/4：AI create 强制 `draft+needs_review`、并发/重放 exactly-once、错账号/错管理员零写、陈旧 snapshot 零写。历史首条测试缺 `app.cleanup()`，外层只识别并删除其唯一 UUID 库后恢复基线；没有把测试基础设施残留当作业务通过。正式 PID/ELF/健康前后不变。 | HC-025 |
| SR-112 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Preview intent 绑定 workspace/account/owner、稳定 candidateId 与 canonical hash；Apply 只消费服务端封印候选，在单一事务提交 Document、Chunks、create revisions、catalog intents 与稳定回执。共享 PDF/图片/自动摄取同样使用稳定业务身份与事务内核。 | 正式226项构建根生成的测试二进制 5/5 通过：并发 Apply/重放 exactly-once、第二候选失败整批回滚、错管理员/错 hash 零写、共享摄取并发重放 exactly-once、catalog intent 拒写整图回滚；每条测试自清理随机库。 | HC-025 |
| SR-113 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Chunk Patch DTO 拒绝未知字段，服务端白名单仅接受内容字段；workspace/account/document/domain/status/integrity/confidence/provenance/anchors/actor/lineage 均不可表达，内容修改由 revision 内核强制重新待审。 | 托管字段注入通过真实 Router 零写拒绝；与 SR-101 的 legacy PUT scope/self-verify 拒绝共同证明客户端不能越过服务端字段投影。 | HC-024 |
| SR-114 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：普通 revision、Chunk CAS、父 Document desired generation 与 durable catalog intent 同事务提交；冲突、主行拒写或 intent 拒写整体回滚，no-op 只留审计 revision 且不排 catalog。 | 并发 CAS、主行拒写回滚、audit-only no-op、create/repair/split/merge/rollback 的 revision 与 catalog intent 红线均在正式源码矩阵中通过。 | HC-024 |
| SR-115 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Catalog Worker 使用 owner、不可复用 token、单调 generation、lease/heartbeat 和有界重试；快照与 job 终态同事务，只有当前 desired generation 可提交，旧 owner/旧代次被 fencing，缺父文档收敛 discarded。 | 正式源码测试二进制 4/4 通过：过期 claim 与乱序代次收敛、双 Worker 单次提交、legacy target=0 升级、缺父文档终态；与其余 HC-024 矩阵合计29/29，29个 UUID 库已精确删除。 | HC-025 |
| SR-116 | 已修复、正式部署并完成候选及部署后同源 `rs0` 动态闭环：成交追认按 `(account_id, contact_wxid)` 分组，并以 workspace+account+wxid 读取 Contact；两个账号复用同一 wxid 时，只有实际成交账号的知识使用计入 hit。 | 候选与部署后矩阵中的成交归因红线各 1/1，完整矩阵各 4/4；随机库与测试进程零残留。正式 ELF `efe5e1e1…baf4`、PID `2264975`、`NRestarts=0`。 | HC-004 |
| SR-117 | 已修复、已进入正式 ELF 并完成部署后副本集动态验证：Ingest source 使用配置 generation、不可复用 claim token/单调 generation、owner、lease 与 heartbeat；配置更新撤销旧 claim，所有终态按完整配置快照与有效 claim fencing，知识图和 checkpoint 同事务提交。 | 正式源码测试二进制 4/4 通过：legacy migration 保留 claim、配置更新后旧结果零产物、过期 lease 仅新 owner 可提交、双 Worker 单 claim/单知识图。正式 `import_worker.rs` 与当前树只差未使用变量警告修正，行为逐字同源；测试均自清理随机库。 | HC-025 |
| SR-118 | 已修复并完成确定性验证，待部署：诊断页按 persisted `{documents}` 与 live `{item:{documents}}` 真实包络运行时解析；合法空数组显示 0，HTTP 失败、缺字段或错误类型显示“不可用”并计入失败横幅，刷新失败不保留旧数字伪装当前值。 | 不改 Catalog 后端与统计口径；前端局部 parser fail-closed。定向回归 3/3、TypeScript 关联目标与 scoped diff 门通过。 | HC-035 |
| SR-119 | 已修复、正式部署并完成候选及部署后同源 `rs0` 动态闭环：Digest Prompt、LLM 审计、run id、报告与分析读写均使用真实 workspace/account；定时 helper 真实枚举全部持久账号并逐账号失败隔离；Chunk health 同时读取当前账号私有知识与 `account_id=null` 的 workspace 共享知识，不读取其它账号私有行。 | 真实生产 helper 的双账号枚举和共享 Chunk 可见域红线各 1/1，连同 SR-116/152 候选与部署后矩阵各 4/4；正式数据库基线、69 项静态资源与服务健康均通过。部署保持 `KNOWLEDGE_DIGEST_ENABLED=false`，未把代码上线扩大为运营启用。证据归档 SHA-256 `86ce5ad5…cbb6`。 | HC-004 |
| SR-120 | 已修复、已部署并完成部署后真实模型业务验证：Digest 生产 `RunBudget` 直接读取 `KNOWLEDGE_DIGEST_RUN_TOKEN_BUDGET/MAX_LLM_CALLS`，同一有效值进入运行闸与 `budgetSnapshot`；启动配置拒绝非正数及异常大值。 | 复用现有 RunBudget，不增加第二套预算。非默认 `12345/3` 接线与边界单测 2/2继续有效；部署后唯一 HC-028 硬门 1/1，能力证据 `llm_calls=2/artifacts=6/assertions=18/pass`。 | HC-028 |
| SR-121 | 已修复、已部署并完成真实 Mongo 与部署后真实模型验证：同一日报行以单调 `attemptGeneration` 领取重算代次，只有最新代次可切换 current 成功快照；失败/超预算只更新 `latestAttempt*`，保留上次成功 cards/status，首次失败仍显式可见，晚到代次审计为 `superseded`。服务器 `rs0` 两条恢复红线 2/2；部署后闭环生成并提交 1 张 Digest 卡后继续完成派工。 | 不建版本平台或新集合；复用日报唯一行和 Mongo CAS。单元素对象边界只恢复具备完整判别字段的单卡对象，封闭枚举不放宽，正反回归 2/2；部署后真实模型硬门再次 1/1。 | HC-028 |
| SR-122 | 已修复、已部署并完成真实副本集与部署后真实模型业务验证：Task 以不可复用 claim token、owner、单调 generation、attempt 和 120s lease 原子领取，20s heartbeat 只为当前未过期 claim 续租；pending 与过期/缺 lease 的 running 均可回收，旧 owner 的 intent、outcome、终态和业务写全部由 token+generation+有效 lease fencing。恢复按稳定 `stepId` 跳过已有 outcome；`add_chunk/retag/dismiss` 的非确定准备结果先持久化为 intent，业务副作用与唯一 step outcome 在同一 Mongo 事务提交，失权或 outcome CAS 失败整体回滚。取消与终态清除 claim 字段，账号级 add 产物保持任务 account scope。 | 不引入新队列或工作流平台，复用现有 task、Chunk/revision、日报集合与 Mongo 事务。副本集红线覆盖过期重领、旧 owner 零副作用、同 step 恰好一次及账号域；部署后硬门中 Task 收敛为 completed，并提交唯一 committed repair outcome。 | HC-028 |
| SR-123 | 已修复、已部署并完成真实 Mongo 与部署后真实模型业务验证：每步持久化封闭业务 verdict `committed|noop|needs_manual|failed`，Rust `Ok` 不再等同成功；任务只在全部 outcome 为 committed/noop 时进入 `completed`，任一 failed/needs_manual 均进入 `failed` 并写 typed `error_kind`。服务器完整 Worker 目标 3/3；部署后硬门再次验证 completed 对应 committed `fix_chunk`、非空 `repairDraft.patch`（2 字段）且原 Chunk 未自动应用。 | 保留 fail-soft“记录失败后继续下一步”，但禁止把失败改名成功；复用现有 Task/Session 行。部署后真实模型业务门 1/1，MCP 零调用。 | HC-028 |
| SR-124 | 已修复、正式部署并完成部署后真实 Cookie Router+`rs0` 闭环：Digest `cardId` 由 `account_id+report_date+card语义` 稳定派生；直接 dismiss 强制 workspace+account+date+cardId，fenced Worker 的事务提交强制 Task workspace+account。部署后同 cardId 双账号红线分别覆盖直接 Router 与 Worker 路径，非目标日报完整 BSON 不变；连同 SR-116/119/152 扩展矩阵 6/6，所有 RC 为 0、UUID 库/进程零残留。 | 复用现有日报、Router 与 fenced Task 事务，不新增卡片平台。正式 ELF `efe5e1e1…baf4`；证据归档 SHA-256 `784e141c…2735`。 | HC-004 |
| SR-125 | 已修复、已部署并完成授权服务器真实 Cookie Router+Mongo 与部署后真实模型业务验证：Digest API 为当前成功快照返回 `reportId/currentGeneration/reportHash` 与逐卡 `cardHash`；Chat assistant turn 持久化 `digest_dispatch_candidate` 封印，确认请求必须回传 `sourceTurnIndex+candidateHash` 并命中同 workspace/account/session 的来源 turn。画布直派与 Chat 确认都由服务端重新读取当前日报并重建 action/summary/reportDate/targetChunkId，客户端不能扩大选择或覆盖目标。服务器 `rs0` 三条公开路由红线 3/3；部署后真实模型闭环经 Cookie Router 生成封印候选并以该 candidateHash 创建、执行唯一 Task。 | 复用现有日报、chat turn、task 与 Session 行，不冻结整份日报到新平台。隔离候选证据清单 SHA-256 `bba45706c94aa4f3d770908ede815e8954008bae3188473edf11492a2b709029`；部署后硬门 1/1，12 项清单 SHA-256 `8337dad337b8454852c1853c3bc20a8400017719d406a0fd2fdb348513ed3d9b`，部署与业务回归门已关闭。 | HC-028 |
| SR-126 | 工作树已修复并完成类型/确定性验证，Actions、真实 Mongo 与部署待办：代表性召回、Chat apply/verify/answer 闭环及 Worker committed outcome 已改走生产入口，金标不再注入 mock。 | 保留纯函数测试，只把少数红线与闭环 case 接入 `knowledge-evidence-gate`；本机无 Docker/Mongo 时不冒充动态通过。 | HC-032 |
| SR-127 | 已修复并完成确定性验证，待部署浏览器复验：Knowledge Ask SSE 在 Agent 错误时发送独立 typed `failed{code,message}`，详细内部错误只写日志；前端显式消费 failed，连接 error 与未收到 answer 的 close 也都显示可重试错误，主动取消独立处理，正常 answer 不被后续 close 误判。 | 保留 SSE 与现有 Agent；不暴露堆栈。前端失败终态/先成功后失败/无租户死控件回归 3/3、`knowledge_ask_stream_e2e` 目标编译和 Rust lib 编译通过。 | HC-036 |
| SR-128 | 工作树已修复并完成确定性治理验证，真实模型与 Actions 待办：11 个 Knowledge/Quality/Smoke/Recall case 必须记录正调用、目标产物与实际断言；早退、零产物或零断言不能提交 pass。 | 复用共享 `CapabilityEvidence` 与固定 manifest；瞬时基础设施问题显式 `infra_skip`，不把模型波动一律误判业务失败。 | HC-032 |
| SR-129 | 已修复、已部署并完成部署后复验：Cockpit 只发送 camelCase `confidenceThreshold/humanAuditSampleRate/limit`，后端 DTO `deny_unknown_fields`，旧 snake_case 或未知字段不再被静默忽略；前端回归锁定实际请求体。 | 共享 DTO 且只保留一种 wire 命名；后端/前端生产构建、最小候选与正式路径冒烟均通过。 | HC-027 |
| SR-130 | 已修复、已部署并完成真实副本集 OCC 与部署后复验：审核请求冻结 `accountId + chunkId + expectedUpdatedAt + operation=update`，前端只消费匹配目标/版本的顶层 `draftPreview`；Apply 陈旧快照返回 Conflict，Chunk/revision/成功审计/receipt 全部零写。 | 不引入新会话系统；复用现有 Chat turn 与 Mongo 事务，把对象身份和版本令牌贯穿 preview→apply。服务器红线 1/1，正式路径双健康、69 项静态资源和随机库零变化通过。 | HC-027 |
| SR-131 | 已修复、已部署并完成真实副本集与部署后常驻 Cookie Router 验证：前端严格解包 `{item}`，以详情 `id + version` 冻结编辑身份并用 request generation 丢弃迟到响应；保存只发送 `version + dirty metadata`。后端新增 `deny_unknown_fields` 的三态 PATCH DTO，白名单限定可编辑元数据，JSON `null` 可清可选字段，缺失字段保持不变；no-op 不写 BSON/不升版本，dirty 更新与陈旧 version 由 Mongo CAS 隔离，租户、原文、hash、索引、生命周期和 catalog worker 字段均不可由请求覆盖。 | 不改文档存储模型，不删除兼容 PUT；新增窄 PATCH 路由。前端 4/4、真实 Cookie Router+`rs0` 红线 1/1、隔离候选双健康与 69 项静态资源通过；部署后常驻 Router 状态为 `200/200/200/422/409/200/200`，no-op/未知字段/stale 均完整 BSON 零写，临时文档/会话/管理员精确清理。切换证据 SHA-256 `600db09053d90c6d46eb54768762578400a9cb9427f04859c25774d7e2f4367a`，部署态 Router 证据 SHA-256 `dc77dc21564dad3cb83953a84df008744921f221e9143a384d61d17110dd6bf7`。 | HC-034 |
| SR-132 | 已修复、已部署并完成正式 Cookie Router/Mongo 复验：服务端 review projection 固定 workspace/domain 与未归档 `draft|active` 范围，返回可重叠类别、计数和 effectiveFilter；active DomainProfile 精确 alias 驱动维度，未知维度 400。 | 保留现有评审页和 Chunk 生命周期；部署后 pricing/capability 精确分流、外 workspace 空队列与未知维度拒绝通过，69 项静态资源和零残留复验通过。 | HC-027 |
| SR-133 | 已修复，待 Mongo/服务器复验：事务状态/revision CAS + partial unique 锁住 scope/gate 唯一 current 和 proposal 唯一历史产物；m040 对重复存量 fail-closed。 | proposal release做状态+base version CAS并给 override唯一键；与 Evolution发布簇合并。 | HC-017 |
| SR-134 | 已修复，待 Mongo/服务器复验：Planner 每轮动态枚举全部注册 `(workspace,account)` 并逐 scope 跑六段扫描；Cold/Silence 动态枚举全部注册 workspace，候选副作用仍绑定联系人自身账号。单 scope/segment 失败隔离，不再固定默认租户。 | 复用账号表作为 worker capability scope 真相源；不新增中央调度平台。 | HC-004 |
| SR-135 | 已修复、已部署并完成授权服务器真实副本集与部署后闭环：Planner 六段、Cold Contact 与 Silence Signal 统一进入确定性 intent、事务化 task/event/signal 与 UTC 日桶配额边界；重复 intent 不耗配额，段级与总 cap 均持久化。首次 32 并发红线发现跨集合独立读取会跨过 winner commit、把原子提交误判为 `1/2`；身份探测现改为同一 `snapshot` 只读事务，真实持久 `1/2` 仍 fail-closed。 | 复用 AgentTask、BehaviorSignal 与 Mongo 事务，不建设中央调度平台。修正版测试 ELF SHA-256 `7a186e6952db68588b1cf69e554c730af59adb79c7a3fab7e657795fb8561573` 在本机 `rs0` 运行 7/7 红线通过，126 个历史测试库前后逐字一致；正式 release SHA-256 `11d9b6fd943eb67b48f9a4b5d4fa13c2e50e1612899c77d7478d7723cdb36954` 已切换，配额 TTL 索引生效。Planner/Cold/Silence 仍保持默认关闭，未把“部署”擅自扩大为“启用”。 | HC-029 |
| SR-136 | 已修复、已部署并完成授权服务器真实副本集与部署后闭环：ImportJob claim 单调递增 generation 并生成不可复用 token；heartbeat、progress、terminal、取消传播与 stale reclaim 全部绑定冻结 owner，旧 worker/旧 scanner 不能覆盖新执行。m056 先全量验证 generation，再只补 missing/null 并保留 token/timestamp。 | 复用现有 ImportJob lease 与 Mongo CAS，不引入新队列或通用工作流。服务器完整测试 ELF SHA-256 `db389e4d7b65e948fcf7639b4215b5ae1095758feee5eddd6d1ecbc02d33fed1` 的迁移幂等与 ABA fencing 红线 2/2；正式切换后 m056 为 `applied`（现有 ImportJob 0 行、升级 0 行），新 ELF 磁盘/运行哈希一致，PID `2166141`、`NRestarts=0`、双健康与 69 项静态资源通过。 | HC-029 |
| SR-137 | 已修复、正式部署并完成真实 `rs0` 与部署后闭环：BehaviorSignal模型、构造器、唯一键与查询索引包含 `account_id`；m039 对缺账号旧行按唯一当前 Contact 归属回填，歧义/零归属首写前 fail-closed，已有历史账号不依赖已删除 Contact。 | m039 红线覆盖精确回填、二次幂等、历史身份、孤儿与歧义零写；正式库 3,206 条 Signal scope 完整、重复 scoped dedupe 组为 0，partial unique 索引形状正确。详见 [HC-026 / m039 发布记录](production-release-2026-07-28-hc026-m039.md)；HC-004 其它租户项继续开放。 | HC-004 |
| SR-138 | 已修复、已部署并完成正式 Cookie Router 拒绝路径复验：探测只在明确空库时 bootstrap，读错误首写前失败；显式 reset 精确认短语并拒未知字段，缺 body/错短语/未知字段均拒绝且四集合快照零变化。 | 不自动“自愈重置”，不新建配置平台；成功破坏性 reset 未在生产执行，跨集合原子恢复/快照仍独立开放。Wave1 清单 `d16d0ce7…`。 | HC-018 |
| SR-139 | 已修复并部署：除 CRLF 等价外把完整 `BEFORE/AFTER` 送语义审查，覆盖纯删除、重排、重复行删减和改写；补 Reply system/task 锚并禁止审查器自改。 | 确定性门通过且真实路径失败时安全降级；四个 provider 分别被 530/1016、超时、无成功日志与欠费阻断，成功真实模型判定仍待外部恢复，不记通过。 | HC-018 |
| SR-140 | 已修复，待环境编译/集成复验：Reply 装载 A/B 模板时把实际 selected version 记入 run-local 上下文，决策审计落库时覆盖“最高 active”推断；fallback 不伪造版本。 | 只修审计归因，不改变 A/B 分桶。 | HC-007 |

## SR-141～SR-183

| SR | 第一轮：真实业务结论 | 第二轮：最小化/不过度工程结论 | 决策项 |
|---|---|---|---|
| SR-141 | 已修复、部署并完成生产同构真实 Chrome/WebSocket 复验：Chunk WS `lagged`/`revised` 统一触发集合失效；旧快照先撤下，重拉 503 后不回显，后续 `lagged` 可恢复新快照。 | 保留现有 WS 和兼容事件；确定性专项 3/3、正式原始事件链及浏览器双 `lagged` 故障注入均通过。 | HC-027 |
| SR-142 | 已由 SR-142A 完成工作树修复与确定性验证，Mongo/部署待办：联系人快照绑定 account 与 generation，切号隐藏旧行并拒绝迟到响应覆盖。 | 仅使用联系人 store 局部身份与代次，不引入全局请求框架。 | HC-030 |
| SR-142A | Working-tree fix deterministically verified: contact snapshots bind account plus generation; stale account rows are hidden and late responses are discarded. Mongo/deployment verification remains pending. | Local store identity only; no global request framework. | HC-030 |
| SR-143 | 已部署制品观测并通过真实 Router/Mongo 红线：命令 DTO/确认请求绑定 `accountId + planHash`，错账号/错 hash 均 409 且零写；前端切号清计划并隔离迟到响应。 | 复用现有账号 store 与命令 CAS；Command Center 专项 14/14。 | HC-020 |
| SR-144 | 已修复并本机真实 Mongo 验证，待部署：MCP 密钥草稿绑定账号记录 ID+业务 accountId，切号同步卸载/清空明文与状态；请求冻结两种身份，迟到成功/失败不回写新账号。后端以 `_id + workspace_id + expected account_id` 原子更新，身份错配返回 Conflict 且旧密钥不变。 | 复用局部 account scope/generation，不引入全局秘密状态；URL 记录 ID与正文业务账号双重绑定。 | HC-030 |
| SR-145 | 原始扫码登录字段漂移已由 SR-145A 关闭并随整包部署：前端改读发布契约 `session_id/qr_data_url/login_page_url`，缺 session identity fail-closed；取消、卸载和新代次隔离迟到轮询。 | 修复保持为共享 login DTO 的独立小改；确定性契约 3/3 与 TypeScript 已通过，真实 MCP 扫码业务仍按 SR-145A 的外部链路边界单列。 | HC-030 |
| SR-145A | Fixed and frontend-verified: AccountLogin consumes `session_id/qr_data_url/login_page_url`, rejects missing session identity, and fences late poll responses after cancel/unmount/new generation. Tests 3/3 and TypeScript pass; deployment/real MCP login remain pending. | Shared published wire contract plus local generation; no login subsystem rewrite. | HC-030 |
| SR-146 | 已由 SR-146A 完成工作树修复与确定性验证，Mongo/部署待办：详情读取与六类写入绑定 account/contact/generation，服务端预读和最终 CAS 均匹配冻结账号。 | 复用局部 store identity 与现有 Handler，不引入请求状态库。 | HC-030 |
| SR-146A | Working-tree fix deterministically verified: detail reads and six writes bind account/contact/generation; server pre-read and final CAS both match the frozen account. Mongo/deployment verification remains pending. | Reuse local store identity and existing handlers. | HC-030 |
| SR-147 | 已部署制品观测并通过前后端与真实 Mongo 红线：共享导入和 batch-enable 服务端复用 `is_operatable_person` 最终闸，Roster 系统账号折叠只读不可选；拒绝项不建 Contact、不建任务并返回计数。 | 复用既有真人判定；前端联系人/Roster/Store 专项 22/22。 | HC-022 |
| SR-148 | 已修复并经本机及授权服务器真实 Mongo 验证，待部署：Playbook 列表、编辑草稿和请求代际绑定 account；切号同步隐藏旧面板并丢弃迟到响应。编辑、优化、设默认均携 `accountId + expectedVersion`，后端以 `_id + workspace + account + version` CAS，Management 入口复用同一协议并按生产写要求确认。服务器错误账号/旧版本完整 BSON 零写红线 1/1。 | 不引入全局请求框架；用 UserOps Store 局部 generation + 服务端身份/版本 CAS。SR-070/071 的发布态与原子默认指针已于 2026-07-26 独立完成服务器动态验证。 | HC-030 |
| SR-149 | 原始人工标签草稿错绑已由 SR-149A 关闭并随整包部署：保存冻结打开时 Contact 与 `expectedAccountId`，切账号/联系人后的 stale save 不发请求。 | 采用冻结实体与最终账号 CAS，不建设草稿同步系统；专项 Mongo 零写动态证据仍归 SR-149A 记录。 | HC-030 |
| SR-149A | Working-tree fix deterministically verified: tag save carries the frozen Contact and `expectedAccountId`; stale save after account/contact switch emits zero request. Mongo zero-write redline compiles but is pending execution. | Freeze the opened entity; no draft synchronization system. | HC-030 |
| SR-150 | 已部署制品观测并通过服务器真实身份红线：Preview/Apply 绑定 account/contact/generation；错账号、错 hash 与缺强确认均在 claim 前零写，服务端 lease/finalize 继续绑定冻结身份。 | 复用 Guide v3 lease/transaction；真实浏览器迟到响应交互仍待复验。 | HC-023 |
| SR-150A | Protocol v3 真实 Cookie Router+`rs0` 红线 1/1：错身份、错 hash、缺强确认完整零写；事务故障整体回滚，修正后提交，同 hash 重放返回稳定 receipt。 | 动态业务断言已实际执行；不把同进程故障注入外推为杀进程演练。 | HC-023 |
| SR-151 | 已由 SR-151A 修复并完成授权服务器真实 Mongo 红线，正式部署待办：直接启用与已托管画像保存都会提交冻结账号下的 published Playbook，服务端最终 CAS 保存 `playbook_id + playbook_version`；跨账号或 draft 绑定完整 Contact 零写拒绝。 | 复用现有 Playbook resolver 与 Contact profile 入口，不保留装饰性配置、不新增风格子系统。 | HC-018 |
| SR-151A | 已修复并通过授权服务器真实 Mongo 红线，待部署：直接启用与已托管画像保存提交冻结账号身份下的 Playbook；服务端只解析同 workspace/account 的 published 行并在 Contact CAS 保存 id+version。跨账号与 draft 绑定各 1/1 完整 Contact BSON 零写拒绝。 | 复用现有 Playbook resolver 与 Contact profile endpoint；不新增风格配置子系统。 | HC-018 |
| SR-152 | 已修复、正式部署并完成候选及部署后同源 Router+`rs0` 动态闭环：复盘列表与详情强制显式 `accountId`，缺账号返回 400、错误账号/Contact 归属返回 404，正确账号只返回该 workspace+account 的 Review 与关联 RunLog；前端两入口显式传账号。 | 同 wxid 双账号列表/详情红线在候选与部署后矩阵各 1/1，错误 scope 不泄露另一账号 review/run marker；完整矩阵各 4/4、零残留。 | HC-004 |
| SR-153 | 已由 SR-153A 完成工作树修复与确定性验证，Mongo 零写红线与部署待办：批量请求冻结 source/account/contactId/wxid，所有副作用前完成整批预检，最终 OCC 禁止复用旧 roster intent。 | 保留 roster 新建语义并以显式 source 区分；不把 selection 状态扩成同步系统。 | HC-030 |
| SR-153A | Working-tree fix deterministically verified: pool requests freeze source/account/contactId/wxid, complete-batch preflight precedes all side effects, and durable final OCC forbids upsert or reuse of a roster intent. Mongo Contact/Task/Audit zero-write redline compiles but is pending execution. | Keep roster creation behavior separate via explicit source. | HC-030 |
| SR-154 | 已修复并有回归断言：API 投影与 Planner 共用配置的停滞维度及 `{dimension}_updated_at`；写侧仅在该维度值真实变化且本轮确实写入时刷新，容器更新时间不再冒充阶段时间。 | 持久化/投影真实维度变更时间；不构建事件溯源。 | HC-007 |
| SR-155 | 已修复并本机真实 Mongo 验证，待部署：Operations 全量快照按 `accountId + generation` 隔离，切号当帧隐藏旧任务、事件、复核、运行日志与 LLM 明细，迟到响应不得提交。任务 DTO 返回实体 `accountId`；立即复核/取消动作冻结 `expectedAccountId`，REST 与 Management 最终 claim/状态 CAS 均按 `_id + workspace_id + account_id + status` 匹配。 | 不改变后台 worker 的普通调度 claim 协议；只收紧管理员任务写入口。错账号红线证明立即复核与取消均 Conflict，任务及关联 Outbox 完整 BSON 零变化。 | HC-030 |
| SR-156 | 已修复并有 Mongo 集成断言：LLM 成本总量由完整保留日志 `$group` 聚合，明细独立限 1～300 条并返回 `itemsReturned/itemsLimit/itemsTruncated`；界面明确标“保留日志全量汇总/最近 N 条明细”。 | 服务端全量聚合与明细分页分离，不再把样本冒充总量。 | HC-007 |
| SR-157 | 已部署制品观测并有前端竞态回归：Store 以递增 request generation 且同时核对 selected/requested/response campaignId 后才提交；看板对错位 report 隐藏明细并禁用导出。 | 不建立全局请求取消层；浏览器乱序交互复验仍保留。 | HC-021 |
| SR-158 | 已部署制品观测并有前端回归：CSV 单元格先规范化控制字符，再对首个非空白字符为 `=+-@` 的值前置单引号，随后执行 RFC 4180 转义；覆盖四类前缀、前导空白/制表和逗号公式。 | 不引入第三方报表系统；真实浏览器下载打开复验仍保留。 | HC-021 |
| SR-159 | 已修复并本机真实 Mongo 验证，待部署：Outbox 列表、loading/error 与取消确认均绑定 `accountId + generation`；切号当帧隐藏旧快照，A/B 乱序响应及确认期间切号均不得提交。取消正文携冻结 `expectedAccountId`，REST 与 Management 共用核心在 pending/in-flight 两个 CAS 分支均按 `_id + workspace_id + account_id + status` 匹配。 | 只关闭跨账号旧快照取消；in-flight 已跨远端发送边界后的取消/送达竞态仍归 SR-066/HC-010。错账号红线证明 pending 状态不变、in-flight 不写取消请求且均零新增审计。 | HC-030 |
| SR-160 | 已修复并本机真实 Mongo 验证，待部署：素材列表、草稿和文件表单按 `accountId + generation` 隔离，切号重挂载并丢弃迟到响应；私有资产动作冻结实体 `expectedScope=account + expectedAccountId`，共享资产冻结 `expectedScope=workspace` 并显式标“全账号共享”。后端审核、元数据、换文件、启停、删除五入口共用作用域校验，最终 Mongo 写 CAS 精确匹配 `account_id`（共享仅匹配 `null`）。 | 不把共享资产绑定到当前账号，也不取消 workspace 共享能力；错 scope 红线证明审核、元数据、启停、删除均 Conflict，完整 BSON 与审核审计零写。 | HC-030 |
| SR-161 | 已修复并本机真实 Mongo 验证，待部署：成交页选择、草稿、联系人列表与成交记录请求均绑定 `accountId + contactId + generation`；切号当帧隐藏旧表单并丢弃迟到响应。人工成交/退款提交冻结 `expectedAccountId`，后端预读和最终 `$push` CAS 均按 `_id + workspace_id + account_id` 校验。 | 不改疑似成交审批或 Management 共用的内部成交核心；只收紧人工直登入口与最终持久化身份。错账号红线同时证明 `outcome_events` 与 `agent_events` 零写。 | HC-030 |
| SR-162 | 已修复，待真实账号业务复验与部署：每位决策人保存 `accountId`；后端 PUT 校验该账号存在且 wxid 属于该账号联系人，创建 escalation 时冻结发送账号。 | 保持 workspace 级策略，不下沉复制 account 级配置系统。 | HC-013 |
| SR-163 | 已修复，待前端部署复验：配置页维护 loaded/loadError 三态；GET 失败显示错误且保存按钮禁用，默认草稿不能覆盖未知生产状态。 | 不做离线配置缓存。 | HC-013 |
| SR-164 | 已修复，待前端部署复验：空决策链可保存为显式关闭；quiet hours 可清除，表单与后端均保留 `None` 语义。 | 不增加新状态或魔法空字符串。 | HC-013 |
| SR-165 | 已部署；确定性验证及服务器真实 Router+Mongo+Registry 拒绝路径动态通过：active Provider 编辑须先对精确有效草稿完成真实连通测试，成功后签发 10 分钟一次性 capability，绑定 workspace/provider/admin、完整 secret-resolved 配置与旧 `updatedAt`；PUT 还要求显式确认，并以完整旧身份和单调版本 CAS。缺许可与伪造/错版本许可均 409，完整 Provider BSON 和 Registry generation/provider/model 零变化。 | 前端草稿变化即销毁许可，迟到测试响应按 generation 丢弃；不建设发布平台。成功链已用当前模型和 3 个历史成功模型真实执行，但 4/4 均返回 HTTP 530，且本机/服务器 `127.0.0.1:9090` 不可达；故合法许可、成功热切、generation 增长及重启装载仍为外部端点阻断，不用 mock 冒充。19 项固化证据哈希通过，生产零扰动。 | HC-031 |
| SR-166 | 已部署并完成服务器真实 Router+Mongo 动态验证：timeout/maxRetries/retryBaseMs 使用显式三态；省略字段保持原覆盖，`null` 后三个 BSON 字段真实消失，响应 effective 值回落当前全局默认且来源为 `global_default`，数值仍写 Provider 覆盖。 | 不保存魔法空字符串、不复制全局默认到每行。既有 Rust 13/13、前端 6/6 与确定性门继续有效；部署后目标动态断言已实际执行。 | HC-031 |
| SR-167 | 已部署并完成服务器真实 Router+Mongo 副本集动态验证：视觉指派项关闭能力和删除均 409 且完整 BSON 零写；事务改派后恰一条具备视觉能力的目标指派；`workspaceId + isVisionActive=true` partial unique 索引直接拒绝第二条指派。 | 不做能力调度器、不自动猜选存量冲突赢家。既有 Rust Provider 13/13、索引 1/1、前端 8/8 与确定性门继续有效；部署后事务和唯一约束已实际执行。 | HC-031 |
| SR-168 | 已部署并完成服务器已发布二进制真实 Cookie Router+Mongo 动态验证：GET 完整投影 `priorityWeight=73/isTerminal=true/isReactivationTarget=true`；仅含 `label` 的 PATCH 返回 200，响应及 Mongo 读回均保持三个运行字段和 `currentVersion=true`。 | 前端冻结编辑基线并逐字段 dirty PATCH，后端沿用 typed DTO 与局部 `$set`；不把字典改成自由 BSON。随机隔离库与仅回环实例的三层断言通过，29 项证据哈希通过；正式进程、哈希、健康、重启数和正式库探针均零变化。 | HC-014 |
| SR-169 | 已修复并在正式前端制品中观测，确定性验证完成：共享 `ReviewQueue` 以对象 id 设置直属 React key，列表重排不复用上一对象草稿；每次接纳新列表提升 generation，动作前校验 generation、对象仍存在且不在刷新，陈旧闭包零请求。 | 不逐卡复制审核引擎。真实 Taxonomy 卡 `[A,B]→[B]` 与旧 generation 回归共 17/17，TypeScript、生产构建及 scoped diff 门通过；因无认证浏览器会话，部署后真实点击交互仍待办。 | HC-019 |
| SR-170 | 已部署并完成服务器已发布二进制真实 Cookie Router+Mongo 动态验证：管理 API 拒绝把历史自动发布子闸设为 true；env 父闸关闭和 workspace 父闸关闭时人工 release 均返回 400。两阶段前后 runtime flag/proposal 完整 BSON 一致，override、audit、review intent 与 release event 全部零写。 | 所有发布副作用复用同一父闸，当前自动发布另有恒关代码政策闸；前端既有定向测试锁定失败不乐观翻转、成功后才读回权威状态。随机隔离库35项证据哈希通过，正式进程与正式库 marker 零变化。 | HC-017 |
| SR-171 | 已部署并完成服务器已发布二进制真实 Cookie Router+Mongo 动态验证：种入近 7 天 25 个实验及25个候选后，`limit=20` 的浏览列表仅返回20项，但服务端聚合独立统计25/25、released=5、rolledBack=3，并返回 `complete=true/source=server_time_window/windowHours=168/experimentsScanned=25`。 | 服务端按完整时间窗聚合并返回 coverage，不从浏览列表推算、不拉全量到前端；前端既有定向测试对缺 coverage fail-closed。测试库保留，正式服务零扰动。 | HC-017 |
| SR-172 | 已部署并完成服务器已发布二进制真实 Cookie Router+Mongo 动态验证：账号 A 列表严格投影 text/media/referralCard、不可变对象 id、同账号可读元数据及恢复计数；跨账号私有素材只回显 id，账号 B 行不可见。错误 `expectedAccountId` 取消返回 409 且完整 BSON/事件零写，正确账号取消返回 200 并精确写一条同账号审计事件。 | 不复制完整媒体正文、不逐行 N+1；服务端按页批量回表并以 Outbox 账号过滤私有元数据。既有前端/契约 12/12、Rust 投影 12/12 等确定性门继续有效；随机隔离库35项证据哈希通过，正式进程与正式库 marker 零变化。 | HC-010 |
| SR-173 | 已修复、部署并完成生产同构真实 Chrome 断流复验：共享 SSE 重连器只累计连续未成功建连，原生 `open` 或业务事件即恢复预算；旧连接迟到事件隔离，terminal close 不重连。连续 7 次建连失败后 TaskRail 以 2 次权威详情读取收敛终态，终态后零轮询。 | 保留 HTTP+SSE，不换 WebSocket、不建永久轮询。专项 13/13、生产构建与生产同构浏览器故障门均通过。 | HC-027 |
| SR-174 | 已修复并有隔离真实 Mongo 证据，待部署：`Database::connect` 分配 clone 共享、独立连接不同的进程 identity；Taxonomy/DomainProfile cache、初始化 guard 与写后失效均按 identity 分片，并以 `Weak` 生命周期回收。 | 不依赖测试串行化、不引入新缓存服务。当前复验 registry 1/1、库与集成目标编译、格式和 scoped diff 通过；历史双随机库交错红线 1/1 证据继续有效。 | HC-032 |
| SR-175 | 已修复核心行为并完成确定性验证，待真实模型与部署：任何拟发送正文都必须取得严格 Reviewer 判定，自报低风险不能跳过；预算或本地兜底不能制造通过。实时 Reviewer 必须提供唯一、0..10 整数的六项评分及显式 approved/产品声明判断；缺失/畸形/越界/alias 歧义在主、第二 Reviewer 均 safety hold。实时 pressure/boundary 明确零分不再复用历史缺字段豁免，历史读侧仍兼容。 | 不改历史 DTO、不新增 Reviewer；低风险保留 light Reviewer 控制成本。严格 wire 6/6、必审契约 5/5、Review 107/107、`cargo check --tests`、格式与 scoped diff 通过；预算 PBT 类型检查通过但 Windows 链接超时，断言未执行。 | HC-009 |
| SR-176 | 工作树已修复并通过本地真实 Router+Mongo 红线，Actions/部署待办：代表性 Contact 读、Product 写和过期 Session 以真实 Cookie middleware + `api_router` 验证隔离；旧手写 filter 用例已明确降级为局部约束。 | 保留少量纯 DB 测试并增加专用 hard gate，不为每个 CRUD 复制全矩阵。 | HC-032 |
| SR-177 | 已修复并通过隔离真实 Mongo 红线，部署后业务复验待办：Webhook ACK 前持久化 pending handoff；恢复扫描与单飞 durable task 消除崩溃丢回复，后到消息刷新 task 并以 claim token/generation、decision binding 和 Dispatcher 复核阻止旧执行者提交或发送。 | 复用现有 messages/tasks/outbox，未引入 Kafka；即时唤醒只作加速，周期 worker 与 Mongo lease/fencing 是正确性边界。 | HC-010 |
| SR-178 | 工作树已修复并完成确定性治理验证，真实模型与 Actions 待办：11 个跨域、领导通道、主动触达、对抗与角色扮演 case 均输出 typed outcome，零调用/零产物/零断言及全 fallback 不得 pass。 | 复用共享 witness manifest 与当前 run/attempt 防陈旧 checker；明确安全 fail-closed 可作为业务产物，端点瞬时错误只记 `infra_skip`。 | HC-032 |
| SR-179 | 已修复并完成确定性治理验证，待 Actions/部署复验：三份历史任务表的 `[x]` 已降级为非权威 `[~]`，权威 JSON manifest 以六态精确覆盖 144/144 个任务 ID；`verified` 必须同时绑定冻结 commit、生产入口、测试 artifact 与非 soft CI job，当前保守为 0。 | 不建设需求平台；单个 Python checker + JSON + baseline 硬门。正向 144/144、漏项负例、伪造 verified 负例、CI policy 均通过；Actions 尚未运行。 | HC-033 |
| SR-180 | 已部署并完成服务器已发布二进制真实 worker+Mongo 动态验证：运行进程 env 总闸、历史自动发布总闸和 workspace 子闸全部实值为 true，且存在 eligible threshold proposal；真实 60 秒 Evolution tick 仍明确写出 `auto_released_count=0`。候选完整 BSON 不变，override、audit、review intent 与 release event 全部零写。 | 当前全部人工发布由代码政策硬闸强制；旧配置与决策实现仅为未来类型+方向白名单保留，现有全局布尔无法激活。随机隔离库32项证据哈希通过，正式进程与正式库 marker 零变化。 | HC-017 |
| SR-181 | 已完成部署后与同源动态闭环：已发布二进制真实 Cookie Router+Mongo 验证 active 读取、完整 scope 404 零写、软撤销审计与重复撤销单事件；与部署冻结实现同哈希的生产 helper 又在本机隔离 Mongo 完成 `record → revoke → no-injection → repeat-idempotent → re-add` 1/1。 | 不做复杂版本树；旧撤销行保留审计，同内容明确重加创建新 active 行。首次红线成功后发现测试漏清理随机库；补 `app.cleanup()` 后在全新 dbpath 再次1/1且测试库清单为空，旧诊断库保留未删，系统 MongoDB 服务与端口恢复原状态。 | HC-011 |
| SR-182 | 已部署实现完成确定性动态闭环：coreFacts 保持 cap 6，按结构化来源、重要度、置信度、更新时间和稳定键排序；容量淘汰进入 recent 并写最多20条跨轮 `coreFactEvictions`，显式 discarded 或重新升入 core 会清理旧归档，归档不重新注入 prompt。 | 不承诺无限 prompt 保留，也不建版本树。实现及测试与服务器冻结部署源码逐字同哈希；完整 `memory_card_invariants` 20/20、联合前端7/7及既有 TypeScript/rustfmt/scoped diff通过。该纯确定性契约不需要模型或浏览器链路。 | HC-011 |
| SR-183 | 已修复治理协议并完成确定性验证，待 Actions 与 v2 全量重跑：47 域工作流使用强 schema、逐 claim 定位证据、固定 `complete|inconclusive|failed` 槽位和冻结 run/model/commit/input-hash manifest，失败不再被 `filter(Boolean)` 静默删除。旧工件保留为历史线索，权威状态诚实固定为 `complete=0 / inconclusive=47 / failed=0`。 | 单个 JSON manifest + Python checker + baseline 硬门，不建设审计平台。正向门通过；漏域/伪造 complete/哈希漂移负例 3/3、v2 合法 complete/错误 locator 正反例 2/2、CI policy `hard=8 soft=6 failures=0`；尚未重跑 47 域，不能称事实底座。 | HC-033 |
